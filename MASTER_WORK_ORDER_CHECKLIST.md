# Master Work Order Checklist

**As of:** 2026-05-05 (night-shift runtime block COMPLETE — **WO-QUESTIONNAIRE-FIRST-RETIRE-LIVE-01 Phase 1 LANDED** behind `localStorage["lv_qf_live_ownership"]="1"` flag (~30 lines net across 2 edits) + **BUG-LORI-SWITCH-FRESH-GREETING-01 Phase 2 Slice 2a (Tier C + Tier D) LANDED** behind `HORNELORE_CONTINUATION_PARAPHRASE=0` flag with **17/17 unit tests passing** + **Timeline-context Phase A scaffolding VERIFIED** (LAW 3 isolation test 3/3 + negative-test confirmed + validator passes Janice seed pack + seed loader dry-run clean). Earlier night-shift docs banked: **CLAUDE.md design principle 8 LOCKED** + **PHASE_1_PLAN + PHASE_2_PLAN docs** + **BUG-LORI-ERA-EXPLAINER-INCONSISTENT-01 + BUG-LORI-DUPLICATE-RESPONSE-01 specs** + **MEMORY-APPLIANCE-FRONTIER-2026-05-05.md** forward-looking memo. Earlier same-day: BUG-LORI-REFLECTION-02 Patch C + SAFETY-INTEGRATION Phase 2 LANDED + harness lori_reply capture fix + WO-PROVISIONAL-TRUTH-01 Phase A LANDED + listener-arc polish + 5 BUG/spec WOs banked + TEST-23 v6 ran. **Tomorrow:** stack restart → confirm welcome-back fires for Mary on switch-back (no SYSTEM_QF preemption) → flip `HORNELORE_CONTINUATION_PARAPHRASE=1` → confirm Tier C era-aware greeting → re-run TEST-23 v8 against same narrators.)
**Active baseline:** `r5h-followup-guard-v1` = **78/114, v3=49/72, v2=43/72, mnw=2** — **locked 2026-05-03** after WO-EX-CASE-BANK-FIXUP-01 Phase 1 (case_110 spouse path normalization) verified flipped True. Delta vs prior `r5h-followup-guard-v2` (77/114): +1 across all three contract metrics, mnw stable, scorer-drift audit ZERO on the case_110 flip. parse_success_rate 95.6%, truncation_rate 0.0%, method dist llm=109/fallback=5. Same two pre-r5e1 mnw carryovers (case_035 / case_093). Naming note: suffix counter inverted (v1 numerically AHEAD of v2); future runs should use v3+. Earlier baseline `r5h` (70/104) remains the legacy-pack reference.
**SPANTAG state:** OFF (`HORNELORE_SPANTAG=0`). BINDING-01 PATCH 1-4 + micro-patch in-tree (`42accc9` / `3f3deba`); default-off after `r5g-binding-on-v1` master REJECTED (`c547ddb`). Both flags gated for next iteration cycle.
**Parent sessions:** Day-of-readiness — Reset Identity utility staged, scan_answer hardening landed, BUG-DBLOCK-01 closed. Lori-behavior reflection lane: BUG-LORI-REFLECTION-02 **Patch C LANDED 2026-05-05** behind `HORNELORE_REFLECTION_SHAPING=0` (3-layer runtime shaping: extract_concrete_anchor + shape_reflection Cases A/B/C1/C2/D + Layer 3 softened-mode 30-word cap; 21/21 unit tests green; default-off pending 2 clean golfball runs). WO-LORI-SAFETY-INTEGRATION-01 **Phase 2 LANDED 2026-05-05** (LLM second-layer classifier with composition rule + 11-case IndirectIdeationRedTeamMiniPack). **2026-05-04:** WO-PROVISIONAL-TRUTH-01 Phase A LANDED (read-bridge in `_build_profile_seed` reads both `profiles.profile_json` + `interview_projections.projection_json` provisional fields, lifted to ALL turn modes); listener-arc polish (ERA EXPLAINER + REFRAME RULE + GROUNDING RULE + operator-bubble retirement behind `LV_INLINE_OPERATOR_BUBBLES`); WO-HARNESS-V4-VISIBILITY-01 (Playwright `--start-maximized` + `bring_to_front()` across 3 harness scripts). TEST-23 v4 + v5 ran end-to-end: Mary recall_pre PASS on identity, Marvin recall_pre RED due to harness 0-char reply capture (NOT product). **2026-05-05:** harness lori_reply capture fix landed (full-text emit at top of `onAssistantReply()`); harness metric rename `era_facts_recalled` → `era_stories_in_readback`; extract.py `_NAME_STOPWORD_BLOCKLIST` + `_LLM_COMMENTARY_PATTERNS` regex set + `_is_llm_commentary()` wired into `_validate_item()`. **5 spec WOs banked across 2026-05-04/05:** BUG-EX-NAME-EXTRACTION-NOW-01, BUG-EX-DOB-LEAP-YEAR-FALLBACK-01, BUG-EX-POB-CORRECTION-WRONG-PATH-01, BUG-EX-LLM-COMMENTARY-AS-VALUE-01 (LANDED via extract.py patch), WO-LORI-MEMORY-ECHO-ERA-STORIES-01 (Phase 1c era-story recall). TEST-23 v6 in flight at this checklist refresh.

---

## Three lanes, separate posture

The repo runs three parallel lanes. They touch different code paths and get worked in parallel without blocking each other.

### Lane 1 — Extractor (regression-gated)

Synthetic eval bench: 104 cases, locked at 70/104. Climbs only when the extractor architecture earns it. Frozen baseline is `r5h`.

### Lane 2 — Lori behavior (parent-session-gated)

In-session listener behavior: safety responsiveness, interview discipline, memory echo grounding, attention awareness. Three pre-parent-session blockers in this lane.

### Lane 3 — MediaPipe / session awareness (post-parent-session polish)

Attention cue + adaptive silence ladder. Doesn't block first parent sessions. Scoped, parked behind Lane 2 closure.

---

## Lane 1 — Extractor (active baseline `r5h` locked)

### Active sequence

| # | WO | Status | Eval tag | Notes |
|---|---|---|---|---|
| 1 | **BUG-EX-PLACE-LASTNAME-01** | LANDED 2026-04-29 (`9b9e557`) | `r5h-place-guard` re-run unblocked by eval-script printer fix `4ac71f0` (#323) | Deterministic post-LLM guard at `extract.py` drops `.lastName`/`.maidenName`/`.middleName` candidates appearing only after place-prepositions. 6 new fixtures `case_105–110`. Smoke 15/15. Ships independent of SPANTAG. First eval run completed 110 cases cleanly (75/110, mnw 1.2%) but lost the report to a printer crash; re-run pending. |
| 2 | **WO-EX-BINDING-01** | **PATCH 1-4 LANDED + iterating.** PATCH 1-4 (`42accc9`) + micro-patch (`3f3deba`); `r5g-binding-on-v1` master REJECTED (`c547ddb`). | `r5g-binding-on-v1` (banked), next iteration TBD | Binding-layer fix. SPANTAG Pass 2 binding pre-normalizer + `_validate_item` re-entry + 9 `_FIELD_ALIASES` from r5g probe. Default-OFF after rejection at default-on. Probes `r5g-binding-probe-7cases` (3/7 primary 3/3) + v2/v3 confirmed micro-patch was real. Next move: additional binding rules / second eval cycle / scope re-evaluation. |
| 3 | **#144 WO-SCHEMA-ANCESTOR-EXPAND-01** | Lane 1 trial annotated | `r5h` (banked) | Lane 1 (`alt_defensible_paths` for case_033 + case_039) landed scorer-side; Lane 2 schema expansion gated behind Lane 1 evidence. |
| 4 | **#97 WO-EX-VALUE-ALT-CREDIT-01** | Spec landed | (planned) | Value-axis alt-credit under ≥0.5 fuzzy gate. Targets case_087 ancestry value drift. |
| 5 | SPANTAG re-enable evaluation | **GATED — paired with BINDING-01 second iteration cycle** | next BINDING tag | After next BINDING iteration: cycle stack with `HORNELORE_SPANTAG=1` exported in server env (`SPANTAG_PASS1_MAX_NEW=1024`, `SPANTAG_PASS2_MAX_NEW=1536` already in `.env`); rerun master eval; judge by Type A/B/C, not general gate. |

### Locked decisions

- `r5h` is the regression net. Climbing past 70/104 happens via real parent-session data once it accrues, not synthetic patching alone.
- SPANTAG default-off until BINDING-01 lands. Three patches from commit e579ed0 (value-coerce wrapper at extract.py:7006, HF_HUB_OFFLINE init at api.py:50, `.env` revert) are dormant when SPANTAG=0; they prove their value the moment SPANTAG re-enables.
- Two known `must_not_write` offenders are pre-r5e1 carryover (case_035 faith turn `education.schooling`, case_093 spouse-detail `education.higherEducation`); both are WO-LORI-CONFIRM-01 targets. Don't chase them in Lane 1 patches.

### Parallel cleanup (do not block main sequence)

- **WO-TIMELINE-RENDER-01 + WO-TIMELINE-CONTEXT-EVENTS-01 (NEW LANE — promoted to top of cleanup pool 2026-05-05; Phase A SCAFFOLDED night-shift 2026-05-05)** — **Phase A scaffolding banked night-shift** as 8 isolated dark/off files (NOT yet wired into main.py): `server/code/db/migrations/0005_timeline_context_events.sql` (full schema with CHECK enums + indexes) + `server/code/api/services/timeline_context_events_repository.py` (~470 lines: ContextEvent dataclass + add/update/get/query/soft_delete/promote_research_note + schema validation + JSON tag-arrays handling) + `tests/test_timeline_context_events_isolation.py` (LAW 3 build-gate; mirrors story_preservation_isolation pattern; AST-walks transitive imports; forbidden prefix list covers extract / chat_ws / prompt_composer / Lori-behavior services / story-capture / utterance_frame / peek_at_memoir) + `data/timeline_context_events/tag_vocabulary.json` (locked v1 region_tags + heritage_tags + community + cohort_period + experience taxonomy) + `scripts/validate_timeline_context_events.py` (~330 lines: per-entry schema validation + tag vocabulary enforcement + citation discipline regex + pack_kind shared_regional stricter rules) + `scripts/seed_timeline_context_events.py` (~210 lines: idempotent loader with --dry-run / --update-existing / --pack flags) + `data/timeline_context_events/janice_germans_from_russia_nd_prairie.json` (DRAFT seed pack with 7 starter entries — 4 ready / 3 marked operator_research_note pending citation upgrade) + `tests/test_timeline_context_events_repository.py` (skeleton with 30+ test names mapped). Chris reviews + decides what lands. WO-TIMELINE-RENDER-01 covers a read-only render endpoint (mechanical, era-grouped accordion) consuming existing data: `profiles.profile_json` + `interview_projections.projection_json` for facts (Phase A read-bridge already in place), promoted/confirmed `story_candidates` (migration 0004) for interview quotes, `photos` (migration 0001 — 6-table schema with EXIF + GPS + soft-delete + provenance) for photo pins, `photo_memories` for photo-attached narrator stories, and `timeline_context_events` (NEW table per pair WO) for operator-curated regional/cohort context. Narrator-visible from v1; operator-additionally-sees-citations toggle. Era derivation is render-time from date + narrator DOB via a new `lv_eras.year_to_era_id()` helper with Python/JS parity test. Decade-precision items render with uncertainty preserved (`~1940s` / `circa 1950s`) — locked rule: never silently midpoint. Behind `HORNELORE_TIMELINE_RENDER_V1=0` flag; endpoint 404s when off. WO-TIMELINE-CONTEXT-EVENTS-01 (renamed from WO-WORLD-EVENTS-TABLE-01 after Chris's "the table is `timeline_context_events`, NOT `world_events`" reframe — the value is locally meaningful memory scaffolding for THIS cohort in THIS region for THIS kind of life, not generic Wikipedia chronology). Schema + repository + JSON validator + tag vocabulary + Janice/Germans-from-Russia/ND-prairie initial seed pack. 8-value `source_kind` enum (local_oral_history / archived_newspaper / historical_society / academic / reference_work / web_resource / family_archive / operator_research_note). Operator-curated only — Lori does NOT write to either surface, not even via approval queues. Heritage tag taxonomy aligns with `docs/voice_models/VOICE_LIBRARY_v1.md` seven voices but is **extensible — no cohort gets force-mapped**. Pulls forward HorneHX/Lifeline architectural pattern (CalendarEvents + Media + EXIF + locations) without rebuilding the PHP codebase. Sprint 1 backend Phase A: ~half a session per WO. Both ride parallel; neither blocks parent sessions. **Locked principles (9):** Lori doesn't generate the timeline (she talks beside it); stored not retrieved; local-sourced first; citation discipline; no laundering (narrator anecdotes stay personal-truth pins; don't become shared regional history); heritage_tags extensible; voice library is context filtering NOT identity inference (Lori is a pattern-aware listener, not an identity-detector — heritage_tags are operator-recorded at intake, never inferred at runtime); table holds historical context packs, not generic chronology; **the table supports Lori — does not replace her** (DB/schema = memory + context; Timeline = visual scaffold; Lori = human-feeling conversational layer). Future composer-hook WO (parked behind BINDING-01 + parent-session readiness) will surface relevant context events to Lori's system prompt deterministically. Specs at `WO-TIMELINE-RENDER-01_Spec.md` + `WO-TIMELINE-CONTEXT-EVENTS-01_Spec.md`.

- **WO-EX-UTTERANCE-FRAME-01 Phase 0-2** — **LANDED 2026-05-02 morning + POLISH 2026-05-02 evening.** Pure-deterministic Narrator Utterance Frame / Story Clause Map builder at `server/code/api/services/utterance_frame.py`. Public `build_frame(narrator_text) → NarratorUtteranceFrame` returns clause-level decomposition (subject_class / event_class / place / time / negation / uncertainty / candidate_fieldPaths). Zero new deps (stdlib + reuse of `lori_reflection._AFFECT_TOKENS_RX`). 24 hand-authored fixtures (`tests/fixtures/utterance_frame_cases.json`) including case_111/112 dual-subject motivators (uf_001/002) plus polish fixtures uf_021–024 (lowercase Stanley/Spokane STT, aluminum plant specific phrase, stated feeling visibility). 16 unit tests + 4 build-gate isolation tests (negative-test-verified). CLI debug runner at `scripts/utterance_frame_repl.py`. Chat_ws observability log behind `HORNELORE_UTTERANCE_FRAME_LOG=1` (default-off, mirrors story-trigger gate, fires `[utterance-frame]` log line per narrator turn). **Polish patch (2026-05-02 evening)** added: lowercase known-place fallback (spokane/stanley/mandan/bismarck/montreal/oslo/hanover/norway → canonical capitalized when verbatim after place preposition), prefer-specific-workplace-object (place=plant + object=aluminum plant → place=aluminum plant), richer log shape with `obj=` + `feel=`. Polish driven by **WO-EX-UTTERANCE-FRAME-SURVEY-01** (new harness + spec at `scripts/run_utterance_frame_survey.py` + `WO-EX-UTTERANCE-FRAME-SURVEY-01_Spec.md`) which paired golfball traffic + api.log frame lines, surfaced AMBER findings, and sized the polish exactly. **Phase 0-2 ships zero behavior change** — frame is observation-only; consumer wiring (extractor binding Phase 3, Lori grounding Phase 4, validator Phase 5, UI surface Phase 6) opens after BINDING-01 second iteration evidence.
- **WO-EX-EVAL-WRAPPER-01** — single-command extractor eval workflow. Spec authored 2026-04-27. Eliminates the 6-step manual ritual for SPANTAG / BINDING-01 evals (refusal-on-failure gates for .env flip + stack restart + warmup probe + flag verification + inline post-eval summary). 3–5 hours implementation. Build before the next BINDING-01 eval cycle.
- **WO-EX-FIELD-CARDINALITY-01** — deferred separate lane per Architecture Spec v1 §7.3. Opens only if BINDING-01's minimal scalar guard proves insufficient.
- **WO-SCHEMA-DIVERSITY-RESTORE-01** — three-phase plan. Spec authored 2026-04-29. Phase 1 (template port from `/mnt/lorevox/ui/templates/`: 17 named diverse + 9 dopplegangers + manifest) = pure file copies, ~1-2 hrs. **Phase 1.5 LANDED 2026-04-29** (Janice pet pig + Christopher 5 lifetime pets + stepchildren placeholder + relation:"biological" on existing 3 children). Phase 2 (array-shape normalization adapter for spouse/marriage) ~1 day, gates Lorevox-template ingestion. Phase 3 (sensitive identity capture: genderIdentity, sexualOrientation, religiousAffiliation, spiritualBackground, culturalAffiliations[], raceEthnicity[] with visibility/provenance state) 2-3 days code + 1-2 days policy. Required before Hornelore can ingest Lorevox's diverse-shape templates without surprises. BUG-311 evidence (extractor binding hallucination on test-harness.js:65-98) reinforces Phase 3 sensitive-field guard rationale.
- **#142 WO-EX-DISCIPLINE-01** — run-report header. Live and working (visible in r5h-postpatch eval).
- **#141 WO-EX-FAILURE-PACK-01** — cluster JSON sidecar. Live and working.
- **WO-LORI-CONFIRM-01** — parked. v1 = 3-field pilot (`personal.birthOrder`, `siblings.birthOrder`, `parents.relation`). dateRange v1.1 reactivation unlocked by #111 corpus expansion (cg_019, cg_020, cg_021).
- **WO-EX-CASE-BANK-FIXUP-01** — umbrella spec at root. **Phase 1 LANDED 2026-05-02** (case_110 spouse path normalization, verified flipped True at r5h-followup-guard-v1 = 78/114, +1 across all three contract metrics). Phases 2 (case_075 alt_defensible_values for parents.notableLifeEvents) + 3 (case_042/045 pet species canon) parked. Pure data/scorer cleanup; zero extract.py touch.
- **BUG-EX-BIRTH-DATE-PATTERN-01** — spec parked at root. Deterministic post-LLM regex fallback for canonical birth-sentence templates. Targets case_049 (DOB recall miss on clean Spokane sentence). R4-H date normalizer is fine; the cure is regex layer, not Pass 2 prompt change. Sized: ~30 lines + 10 unit tests + 1 master eval = half a session.
- **WO-EX-FIELD-CARDINALITY-PETS-01** — spec parked at root (sub-WO of parent WO-EX-FIELD-CARDINALITY-01, narrowed to pets.* subtree where survey produced n≥2 evidence). Targets multi-entity pet sentences (sd_011 "barn cats + dad's horse Silver", case_046 "barn cats + Dusty"). Three rules: collective+named-individual split when species differ; collective-only preserved; named-only preserved. Post-LLM splitter, no Pass 2 prompt change. Implementation gated behind BUG-EX-SPOUSE-ALIAS-01 + master eval refresh.

### Standard eval block (when invoked)

```bash
cd /mnt/c/Users/chris/hornelore
./scripts/archive/run_question_bank_extraction_eval.py --mode live \
  --api http://localhost:8000 \
  --output docs/reports/master_loop01_<SUFFIX>.json
grep "\[extract\]\[turnscope\]" .runtime/logs/api.log | tail -40
```

Suffix rotates per-eval. Standard post-eval audit block per CLAUDE.md.

---

## Lane 2 — Lori behavior (pre-parent-session blockers)

Three WOs gate parent sessions. They land in this order; Phase 0/1 of each is the urgent slice.

### Active sequence

| # | WO | Status | Land before parents | Notes |
|---|---|---|---|---|
| 1 | **WO-LORI-SAFETY-INTEGRATION-01** Phase 1 | **LANDED** (`8aab2c2`) + **default-safe fallback hardening LANDED 2026-04-29** (chat_ws.py L206-228) | DONE | Chat-path scan_answer hook live. Mirrors interview.py:269-307. Persists segment flag + softened mode + WS overlay event + operator notify + forces turn_mode=interview. 2026-04-29 hardening: when scan_answer raises, force turn_mode=interview anyway + emit `[chat_ws][safety][default-safe]` log marker. Stress-test #325 pending. |
| 2 | **WO-LORI-SESSION-AWARENESS-01** Phase 1 | **Import-fix LANDED 2026-04-27 evening** (chat_ws.py L163, L180, L181). Peek-at-Memoir consultation + warm composer remain. | YES | Three broken late imports (`from api.X` → `from ..X`) fixed and AST-verified. Eval-safe (chat_ws not exercised by extraction eval). Peek-at-Memoir backend may not exist — see parity audit Risk #2; verify before Peek consultation work. |
| **2a** | **WO-QUESTIONNAIRE-FIRST-RETIRE-LIVE-01 Phase 1** | **LANDED 2026-05-05 night-shift** behind `localStorage["lv_qf_live_ownership"]="1"` legacy reactivation flag | DONE — verify after stack restart | Two edits totaling ~30 lines net: dispatcher flag-gate at `ui/js/session-loop.js:117-129` (both `questionnaire_first` + `clear_direct` cases route through `_qfLegacyLiveOwnership` IIFE check; default-OFF goes to `_routeWarmStorytelling` with `[QF-RETIRE]` log markers) + `_ssIsQF` welcome-back gate inversion at `ui/hornelore1.0.html:5179-5193` (now requires BOTH `_ssIsQF && _qfLegacyLiveOwnership` to suppress). `.env.example` adds doc block with operator commands. AST + node syntax checks green. Auto-resolves on next stack restart: BUG-LORI-SYSTEM-QF-PREEMPTION-01 + BUG-LORI-SWITCH-FRESH-GREETING-01 Phase 1 (basic continuation greeting via existing `interview.py:486-489`). Likely partial: BUG-LORI-MIDSTREAM-CORRECTION-01 + BUG-LORI-ERA-EXPLAINER-INCONSISTENT-01. |
| **2b** | **BUG-LORI-SWITCH-FRESH-GREETING-01 Phase 2 Slice 2a (Tier C + Tier D)** | **LANDED 2026-05-05 night-shift** behind `HORNELORE_CONTINUATION_PARAPHRASE=0` default-off flag | DONE — flip flag after smoke test | New `compose_continuation_paraphrase()` in prompt_composer.py + `era_id_to_continuation_phrase()` in lv_eras.py + `_build_opener_text()` 3 optional kwargs + flag-gated dispatch + `get_opener` query params + frontend wire at `hornelore1.0.html:5184-5256` reading `state.session.currentEra` + `IV_OPENER` opts param in api.js. **17 unit tests in `tests/test_compose_continuation_paraphrase.py` ALL PASS** (Tier selection / name resolution / determinism / graceful degradation / verbatim anchor). Tier D byte-stable with legacy `interview.py:486-489` template — default-off path produces identical output. **Tier C output shape:** *"Welcome back, Mary. Last time we were in your building years. Would you like to continue there, or start somewhere else?"* Special-case for `today` era. Slice 2b (Tier A + Tier B with story stub + unfinished thread) waits for WO-LORI-MEMORY-ECHO-ERA-STORIES-01 Phase 1c data feed. |
| **2c** | **BUG-LORI-SYSTEM-QF-PREEMPTION-01** **(SUBORDINATE TO QF-RETIRE 2026-05-05)** | **Auto-closes on stack restart after 2a lands.** Spec retained as implementation evidence. | n/a — auto-closes | Race-condition evidence (Mary 1s gap, Marvin 0s gap). Preemption race is structurally impossible if SYSTEM_QF doesn't fire on the live path. |
| **2d** | **BUG-LORI-ERA-EXPLAINER-INCONSISTENT-01** | **Spec banked night-shift 2026-05-05** | YES — verify after QF-RETIRE Phase 1 | Mary asked "what are the building years" three times across 90 seconds and got three different responses, none of which actually defined the era. Likely partially-resolves via QF-RETIRE Phase 1 (SYSTEM_QF preemption was a major factor). Phase 2 fix (only if Phase 1 doesn't auto-resolve): deterministic explainer-trigger detector OR new `compose_era_explanation()` deterministic composer with `_LV_ERA_DEFINITIONS` map. Cross-references BUG-LORI-DUPLICATE-RESPONSE-01 (same Mary transcript, related dispatch issue). |
| **2e** | **BUG-LORI-DUPLICATE-RESPONSE-01** | **Spec banked night-shift 2026-05-05** | YES — diagnosis-first | Lori produced bit-identical response to a prior turn (`transcript_switch_motgx.txt:18 → :31`). Three hypotheses ranked: LLM context shape too similar / response cache misfire / dispatcher misroute. Phase 1 = instrument response-hash log marker, measure repetition rate. Phase 2 = fix per diagnosis. Phase 3 = optional defense-in-depth Step 5.5 similarity guard with regenerate-once budget. May share root cause with BUG-LORI-ERA-EXPLAINER-INCONSISTENT-01. |
| 3 | **WO-LORI-SESSION-AWARENESS-01** Phase 2 + **WO-LORI-ACTIVE-LISTENING-01** | Specs landed (ACTIVE-LISTENING new 2026-04-29) — **land both together** | YES — interview discipline | ACTIVE-LISTENING is the discipline-rules + filter implementation for SESSION-AWARENESS Phase 2. LORI_INTERVIEW_DISCIPLINE system prompt block + runtime filter with intent-aware tiers (memory_echo 100w/1q, interview_question 55w/1q, attention_cue 25w/0–1q, repair 30w/1q, safety 200w/0q exempt). Two-layer defense (prompt block default-on; `_trim_to_one_question` runtime filter behind env flag). Six new metrics ride existing WO-LORI-RESPONSE-HARNESS-01 Test Type A. |
| 4 | **WO-LORI-SAFETY-INTEGRATION-01** Phase 3 | Spec landed | YES — operator surface | Bug Panel real-time banner + between-session digest. Pattern fires today but operator gets no signal. Highest-leverage new SAFETY work now that Phase 1 + Phase 2 are both landed. |
| 5 | **WO-LORI-SAFETY-INTEGRATION-01** Phase 2 | **LANDED 2026-05-05** | DONE | LLM second-layer classifier in `safety_classifier.py`. Composition rule: pattern wins on positive detection; LLM fills gaps (indirect ideation, passive death wish, oblique distress); fail-OPEN on parse failure (route to safety mode anyway). New `IndirectIdeationRedTeamMiniPack` test class with 11 cases asserting ≥80% routing acceptance gate, pattern-wins composition, fail-OPEN parse failure. |
| 5b | **BUG-LORI-REFLECTION-02 Patch C** | **LANDED 2026-05-05** behind `HORNELORE_REFLECTION_SHAPING=0` | YES — flip default-on after 2 clean golfball runs | Three-layer runtime shaping at `lori_reflection.py` (Locked Patch B principle: prompt-heavy reflection makes Lori worse; shape post-LLM in code). Layer 1 `extract_concrete_anchor()` priority cascade (proper noun phrases > kinship > content tokens, with pronoun blocklist). Layer 2 `shape_reflection()` Cases A (trivial pass-through) / B (no-echo + anchor → prepend) / C1 (echo too long, anchor inside → trim) / C2 (echo too long, no anchor → drop) / D (pass through). Layer 3 softened-mode 30-word cap runs FIRST. 21/21 unit tests green. Wired in `lori_communication_control.py` Step 3.5 + safety-path. Phase 4 (flip default-on) parked behind 2 clean golfball runs. |
| 6 | **WO-LORI-SAFETY-INTEGRATION-01** Phases 4–9 | Spec landed | YES — most | IDEATION/DISTRESSED prompt block additive to ACUTE rule (Phase 4), composer/WO-10C/memory-echo/family_truth integration (Phase 5), Friendship Line resource (Phase 6), LV_ENABLE_SAFETY cleanup (Phase 7), red-team pack (Phase 8), operator runbook + onboarding consent (Phase 9, NON-NEGOTIABLE). |
| 7 | **WO-LORI-RESPONSE-HARNESS-01** | Spec landed | NO — first iteration after parents start | Response-quality test harness. Bug Panel + Test Lab. Answers "Did Lori respond like a good interviewer?" (orthogonal to extraction evals). |
| 8 | **WO-PROVISIONAL-TRUTH-01** Phase A | **LANDED 2026-05-04** | DONE | Read-bridge in `_build_profile_seed` (chat_ws.py L791-839) reads BOTH `profiles.profile_json` (canonical) AND `interview_projections.projection_json` (provisional pendingSuggestions + fields), lifted out of memory_echo branch to ALL turn modes. Closes the Mary cross-restart memory-loss class — narrator gives Lori POB mid-session, operator hasn't yet promoted, Lori reads it back on next memory_echo turn AND avoids cold-asking on identity restart. Phase B (write-back symmetry / projection_family hydration on restart) parked. |
| 9 | **WO-LORI-MEMORY-ECHO-ERA-STORIES-01** Phase 1c | **Spec banked 2026-05-05** | YES — closes era-click vs era-story-readback gap | Three-layer architecture for era-story recall in memory_echo readback behind `HORNELORE_MEMORY_ECHO_ERA_STORIES=0`: (a) `archive.add_turn()` accept `current_era` parameter; (b) `peek_at_memoir.summarize_for_runtime` add `recent_turns_by_era` field; (c) `compose_memory_echo` render era-grouped story stubs under "What you've shared so far". Per ChatGPT diagnosis: era-click navigation works (Lane E 2026-05-03 evening); era-story RECALL is genuinely missing. ~250-line spec; phases + acceptance gates + risk/rollback. |
| 10a | **BUG-UI-API-BASE-RESET-01** (filed 2026-05-05 evening, **DOWNGRADED same day**) | Spec banked, status DOWNGRADED to production-hardening | LOW — likely harness/bootstrap artifact, not production root cause | Initial diagnosis flipped this to "actual root cause" based on UI server 404 logs. Reverted same day after Chris noted those 404s appear only during Playwright cold-restart (real browser sessions show API working correctly — manual transcripts confirm). Discipline note in spec: don't promote a single log signal to root cause without checking real production traffic. Spec retained for the production-hardening question (should API base have a hard-coded fallback regardless?), but not blocking. |
| 10b | **BUG-UI-POSTRESTART-SESSION-START-01** | **REOPENED with refined hypothesis space** | TBD pending manual reproduction | Three new hypotheses replace the original four (Playwright timing differs from real-user reload / WebSocket not rebound after backend restart / narrator-room session resurrection logic depends on page-events Playwright skips). First action is a **5-minute manual cold-restart test**: open real browser, run a session, wrap, restart backend, refresh — does narrator state restore? If yes → harness-only bug, low priority cleanup. If no → real production bug, top of Track 1. **Don't write code until that's known.** |
| 11 | **WO-PROVISIONAL-TRUTH-01 Phase E** (PROMOTED FROM PARKED 2026-05-05) | **Spec extended 2026-05-05** | YES — closes BB UI mirror gap | Frontend write-back symmetry: when `projection_json` or `profile_json` gains a value, mirror it into `state.bioBuilder.personal` via the existing BUG-312 protected_identity gate. Closes the v6 audit gap where BB state firstName=None despite identityPhase=complete + basicsComplete=true. Without Phase E, harness can't grade narrator identity correctly AND operator-facing BB UI looks broken. Files: projection-sync.js + state.js + app.js. ~half to one day. |
| 12 | **BUG-LORI-MIDSTREAM-CORRECTION-01** | **Spec banked 2026-05-05** — **likely partially-resolves via QF-RETIRE** (verify after Phase 1 lands) | YES — load-bearing for parent sessions (narrators self-correct often) | Mary's "Actually we only had two kids, not three" came back `handled_well=False`. Lori didn't acknowledge the correction explicitly, didn't echo the corrected value, and the seed continued without reflecting the new truth. Three diagnostic hypotheses ranked (CORRECTION-mode not detected / response generated before truth lands / CORRECTION mode directive too soft). **2026-05-05 update:** SYSTEM_QF preemption was likely a contributing factor — corrections arriving in the same turn window as a SYSTEM_QF auto-prompt got hijacked. Re-test after WO-QUESTIONNAIRE-FIRST-RETIRE-LIVE-01 Phase 1 lands; if corrections still fail, the remaining work is the original three-hypothesis diagnostic. Composes with BUG-LORI-REFLECTION-02 Patch C runtime shaping + WO-LORI-ACTIVE-LISTENING-01. |
| 13 | **BUG-LORI-LATE-AGE-RECALL-01** | **Spec banked 2026-05-05** | YES — narrator-noticeable degradation | Both narrators pass early_inference age probe but fail late_age probe (Mary RED 86 miss, Marvin AMBER 76 miss). Conversation depth erodes age recall. Three hypotheses: profile_seed identity anchor rotation under context pressure / age-computation site-of-truth drift / LLM confabulation. Diagnostic plan probes hardcoded age block first to confirm/deny hypothesis A before code change. |
| 14 | **BUG-HARNESS-RECALL-DICT-FIELDS-01** | **CLOSED-VIA-DIAGNOSIS 2026-05-05** | n/a — bug does not exist | Authored from misread v6 evidence. Re-investigation found the current dataclass already uses renamed fields and `_to_serializable` calls `asdict()` correctly. v6 JSON shows old field names because v6 ran BEFORE Chris's rename commit landed. v7 JSON will carry the new field names natively. Three name-lists were in flight (audit / v6 JSON / current dataclass) — audit queried wrong keys. Spec stays in-tree as a record of "verify-before-patch" discipline catching a phantom. |

### Acceptance gates (per WO)

- **WO-QUESTIONNAIRE-FIRST-RETIRE-LIVE-01** Phase 1 — no SYSTEM_QF auto-prompt fires during a live narrator session across 30 minutes of provoked-question testing (recall, era-explainer, correction, story disclosure); existing welcome-back composer (`interview.py:486-489`) fires correctly on switch-back; operator-entered BB data appears in next memory_echo readback within one turn; narrator-volunteered facts still captured to provisional truth.
- **BUG-LORI-SWITCH-FRESH-GREETING-01** Phase 1 — switching to a narrator with prior turns produces basic welcome-back template, NOT cold-start identity-onboarding intro. Phase 2 — composer emits Tier A/B/C/D paraphrase per signal availability; `story_anchor` is verbatim narrator text; `warm_era_label` is the warm-label mapping; degrades gracefully to Tier D when inputs absent.
- **WO-LORI-SAFETY-INTEGRATION-01** — zero false negatives on suicidal_ideation across both chat AND interview paths; operator runbook in place; onboarding consent disclosure shipped
- **WO-LORI-SESSION-AWARENESS-01** Phase 1 — memory echo answers warmly without crashing; Peek-at-Memoir consulted when available
- **WO-LORI-SESSION-AWARENESS-01** Phase 2 — 30-turn dev session with zero `[lori][discipline] violation=` log entries (or all violations cleanly trimmed by filter); reference shapes preserved (memory echo not over-trimmed; soft invitation not blocked)

### Pre-parent-session blocker checklist

Use `docs/PARENT-SESSION-READINESS-CHECKLIST.md` as the canonical 7-gate list. The Lori-behavior items above are the new pre-parent additions to that checklist:

- [x] Memory echo import hotfix landed + regression test (2026-04-27)
- [x] WO-PROVISIONAL-TRUTH-01 Phase A read-bridge in `_build_profile_seed` (2026-05-04)
- [x] BUG-LORI-REFLECTION-02 Patch C runtime shaping live behind `HORNELORE_REFLECTION_SHAPING=0` (2026-05-05; flip default-on after 2 clean golfball runs)
- [x] **CLAUDE.md design principle 8 LOCKED** — "Operator seeds known structure; Lori reflects what is there. If the operator seeded it, Lori knows it. If Lori knows it, she does not ask for it as intake. Live Lori sessions must not auto-advance questionnaire fields." (2026-05-05)
- [ ] **WO-QUESTIONNAIRE-FIRST-RETIRE-LIVE-01 Phase 1** — `_ssIsQF` gate inversion at `hornelore1.0.html:5179` + audit/suppress all SYSTEM_QF live-path injection sites. Auto-resolves BUG-LORI-SWITCH-FRESH-GREETING-01 Phase 1 + BUG-LORI-SYSTEM-QF-PREEMPTION-01 + likely BUG-LORI-MIDSTREAM-CORRECTION-01 + likely BUG-LORI-ERA-EXPLAINER-INCONSISTENT-01. **TOP architectural blocker.**
- [ ] **WO-LORI-MEMORY-ECHO-ERA-STORIES-01 Phase 1c** — era-story readback in memory_echo + `recent_turns_by_era` data feed for continuation paraphrase composer.
- [ ] **BUG-LORI-SWITCH-FRESH-GREETING-01 Phase 2** — active-listening continuation paraphrase composer (depends on Phase 1c data feed).
- [ ] LORI_INTERVIEW_DISCIPLINE prompt block + runtime filter live
- [x] Safety scan wired into `chat_ws.py` (2026-04-27 Phase 1; default-safe fallback hardening 2026-04-29)
- [x] LLM safety second-layer live (2026-05-05 Phase 2)
- [ ] Operator notification surface live (Bug Panel banner + digest)
- [ ] IDEATION/DISTRESSED prompt block additive to ACUTE
- [ ] Friendship Line resource added
- [ ] Red-team pack passes (zero false negative on suicidal_ideation; 11-case `IndirectIdeationRedTeamMiniPack` ≥80% gate landed 2026-05-05 — full red-team pack pending)
- [ ] Operator runbook written
- [ ] Onboarding consent disclosure language added

### Lori-behavior parallel cleanup (added 2026-05-03 — DOCS/DATA banked, runtime wiring staged)

These compose with the parent-session blocker queue above. They share the comm-control wrapper at runtime and are gated independently behind their own env flags (all default-off).

| WO | Status | Files banked | Next phase |
|---|---|---|---|
| **WO-NARRATIVE-CUE-LIBRARY-01** (ChatGPT base + Claude Addendum) | Phase 1 DATA banked 2026-05-03 | `WO-NARRATIVE-CUE-LIBRARY-01_Spec.md`, `WO-NARRATIVE-CUE-LIBRARY-01_CLAUDE-ADDENDUM.md`, `data/lori/narrative_cue_library.json` (v2), `data/lori/narrative_cue_schema.json`, `data/qa/lori_narrative_cue_eval.json` (40 cases), `data/evals/lori_cultural_humility_eval.json`, `data/evals/sentence_diagram_cultural_context_cases_sd044_sd065.json` (22 cases), `docs/voice_models/VOICE_LIBRARY_v1.md` (7-voice corpus + ChatGPT structural integration including retired-metaphor note + Hearth/Field/Bridge/Shield operator-shorthand + anchor priority table + cue family map per voice) | Phase 2 = pure cue detector at `server/code/api/services/lori_narrative_cues.py` (no DB, no extractor imports, no LLM); 12-test minimum unit test pack; behind `HORNELORE_NARRATIVE_CUES=0` flag. Hard wall: cue library may shape Lori's next question/statement/silence but cannot write Bio Builder / projection / promoted_truth / family_truth / protected identity. |
| **WO-LORI-SENTENCE-DIAGRAM-RESPONSE-01** | Spec banked 2026-05-02 late evening | `WO-LORI-SENTENCE-DIAGRAM-RESPONSE-01_Spec.md` | Phase 1 = scoring-only (golfball + sentence-diagram-survey rollup metrics: anchor_present, multi_question, why_question, generic_or_inventive_reflection); behavior change waits until Phase 1 measures the gap. Phase 3 anchor injection behind `HORNELORE_LORI_ANCHOR_INJECTION=0`. Research-grounded in Alshenqeeti (2014) "always seek the particular." Composes with WO-LORI-ACTIVE-LISTENING-01 + BUG-LORI-REFLECTION-02 in the comm-control wrapper. |
| **WO-DISCLOSURE-MODE-01** | Spec banked 2026-05-03 | `WO-DISCLOSURE-MODE-01_Spec.md` | Per-entry schema enum (narrator_consented / narrator_offered_no_consent / system_inferred_no_consent / sacred_do_not_persist) carrying consent context from extraction → projection → Review Queue → memoir export. 4 phases. **SUPERSEDES WO-SCHEMA-DIVERSITY-RESTORE-01 Phase 3** (visibility/provenance state) once Phase 1 lands. **DO NOT WIRE TODAY** — Phase 1 schema work opens after the cue detector + Lori behavior wiring are stable. |

**Implementation order (locked per ChatGPT's staged plan):**

1. ✅ **2026-05-03 today:** docs/data banked. No runtime wiring.
2. **Next session:** Pure cue detector + isolation tests + eval harness. No Lori behavior change yet.
3. **After eval shows ≥80% cue accuracy + zero truth-write violations:** prompt_composer directive (small, data-bound, NOT a prompt-rule paragraph) + chat_ws wiring (default-off flag).
4. **Manual smoke test on 6 turns:** Hearth (yeast bread), Field (calluses), Bridge (name change), Shield (sacred silence), protective health language (no-pork-stomach), ordinary turn. Pass = no identity inference, zero truth writes, suppression markers honored.
5. **Only after all that:** WO-DISCLOSURE-MODE-01 Phase 1 schema work opens. Touches Review Queue + memoir export + Life Map — too big for one session, lives in its own lane.

**Three locked principles across all artifacts (do not relitigate):**

1. *The extractor extracts FACTS. Lori preserves VOICE.* These are different jobs.
2. *Lori is a pattern-aware listener, not an identity-detector.* Voice models are reference patterns for evals + operator education only. Runtime Lori does NOT classify a narrator by cultural pattern.
3. *"Remember, but never tell."* The Crypto-Jewish narrator's deathbed line that justifies `sacred_do_not_persist`. Lorevox is the system that makes sure the secret doesn't have to die with the next grandmother — but only if the system knows when not to ask.

---

## Lane 3 — MediaPipe / session awareness (post-parent-session polish)

Not parent-session blockers. Spec parked, design stable.

| # | WO | Status | Notes |
|---|---|---|---|
| 1 | **WO-LORI-SESSION-AWARENESS-01** Phase 3 | Spec landed | MediaPipe attention cue. Requires all 4 inputs (gaze_forward + low_movement + no_speech_intent + post_tts_silence) for `passive_waiting`. Tiered cue types (silent indicator → affirmation → soft offer → break offer). Veto rule: `engaged`/`reflective` blocks cue under WO-10C 120s mark. Per parity audit Missing #8: visual signals not currently routed to backend — wiring needed. |
| 2 | **WO-LORI-SESSION-AWARENESS-01** Phase 4 | Spec landed | Adaptive Narrator Silence Ladder. Per-narrator × prompt_weight rolling window (last 30 turns), p75/p90/p95 → tier1/tier2/tier3 with 25s hard floor. Cold-start <10 turns falls back to WO-10C 120s/300s/600s. Pacing FIT not measurement; no surface, no trend, no clinical scoring. |
| 3 | **WO-AFFECT-ANCHOR-01** | Spec landed (PARKED) | Multimodal affect anchoring via shared-clock fusion (Whisper word-timestamps + MediaPipe + light acoustic features + optional Tier 2 video). Blocked by BINDING-01 + parent-session readiness. |

---

## Forward-looking research memo (post-parent-session frontier)

- **`docs/specs/MEMORY-APPLIANCE-FRONTIER-2026-05-05.md`** — research memo (NOT a WO yet) named after Chris's "from: 'Can Lori hold continuity?' → to: 'Can Lorevox become a stable memory appliance?'" framing. Surveys seven concrete problem surfaces that compose memory-appliance posture: (1) kiosk mode, (2) lid-close recovery, (3) session resurrection (cold backend restart), (4) narrator-only startup, (5) timeline visible artifacts, (6) memoir continuity, (7) calm failure handling. Each surface gets named, scoped, mapped to existing infrastructure vs net-new build, and sized. Six-month roadmap proposal with sequencing recommendation. Out-of-scope list (no voice cloning, no auto-publishing, no cloud sync, etc.) prevents scope creep when the work begins. **Reference doc only — does not propose code changes for tonight, this week, or this month.** Becomes input to the next planning session AFTER parent sessions are running cleanly with Janice and Kent and Lori is holding continuity reliably.

---

## Open spec / unbuilt items (low priority backlog)

- **#171 WO-LORI-PHOTO-SHARED-01 acceptance tests** — schema/provenance/confidence/dedupe/geocode-null/template_prompt/selector/API vertical
- **#172 WO-LORI-PHOTO-INTAKE-01 Phase 2** — EXIF + real geocoder + conflict detector + review queue + flag
- **#173 WO-LORI-PHOTO-ELICIT-01 Phase 2** — photo_memory extraction profile + async scheduler + LLM prompts + flag
- **#246 WO-FACEMESH-01** — `face_mesh.binarypb` 404 + SIMD WASM crash loop
- **#35 V4 eval scope** — log density + intent-class + topic-shift axes (post-freeze)
- **WO-EX-NORMALIZE-EXPAND-01** (unwritten) — extend Patch H pattern to phone/address/format-sensitive fields
- **WO-EVAL-MULTITURN-01** — parked spec for multi-turn harness to validate WO-LORI-CONFIRM-01

---

## Closed (last 14 days, abbreviated)

| Item | Outcome |
|---|---|
| **BUG-LORI-REFLECTION-02 Patch C** (2026-05-05) | 3-layer runtime shaping at `lori_reflection.py` behind `HORNELORE_REFLECTION_SHAPING=0`. Wired in `lori_communication_control.py` Step 3.5 + safety-path. 21/21 unit tests in `test_shape_reflection.py`. Default-off pending 2 clean golfball runs before Phase 4 flip. |
| **WO-LORI-SAFETY-INTEGRATION-01 Phase 2** (2026-05-05) | LLM second-layer classifier in `safety_classifier.py`. Composition rule: pattern wins on positive; LLM fills gaps; fail-OPEN on parse failure. `IndirectIdeationRedTeamMiniPack` test class (11 cases, ≥80% routing gate). |
| **Harness lori_reply capture fix** (2026-05-05) | TEST-23 v5 surfaced Marvin recall_pre RED with 0-char reply (Mary's was 200 chars). Tracked to `_lv80MirrorStatus("ready")` gated by `_lv80IdleWasThinking` flag chain that misses memory_echo turns + 200-char `bodyText.slice(0,200)` truncation masking distinct replies. Fix: emit FULL-text `[lv80-turn-debug] {event:"lori_reply", reply_text: text, source: "onAssistantReply"}` at TOP of `onAssistantReply()` unconditionally. v6 expected to flip Marvin recall_pre RED → PASS. |
| **Harness metric rename** (2026-05-05) | `era_facts_recalled` → `era_stories_in_readback`, `era_facts_total` → `era_stories_total` in `run_test23_two_person_resume.py`. Log/print labels updated to `story_recall=N/7` with note "memory_echo readback content; NOT a measure of era-click navigation." Closes ChatGPT-surfaced confusion. |
| **extract.py BUG-EX-LLM-COMMENTARY-AS-VALUE-01 implementation** (2026-05-05) | `_LLM_COMMENTARY_PATTERNS` regex set + `_is_llm_commentary()` helper (length ≥ 12 guard) wired into `_validate_item()` after fieldPath aliasing, before conf parsing. Emits `[extract][COMMENTARY-DROP]`. Detects "(?:not\s+)?explicitly\s+(?:stated|mentioned)", "based\s+on\s+the\s+(?:provided|given|narrator's)\s+", and similar commentary-as-value patterns. |
| **extract.py `_NAME_STOPWORD_BLOCKLIST`** (2026-05-05) | Adds now/then/well/yes/today to rules-fallback name path. Drops candidates whose first token is in blocklist. Emits `[extract][NAME-BLOCKLIST]`. Closes BUG-EX-NAME-EXTRACTION-NOW-01 ("well, NOW my name is X" wrote "Now" as firstName). |
| **WO-PROVISIONAL-TRUTH-01 Phase A** (2026-05-04) | Read-bridge in `_build_profile_seed` (chat_ws.py L791-839) reads BOTH `profiles.profile_json` + `interview_projections.projection_json` provisional fields. Lifted to ALL turn modes. Closes Mary cross-restart memory-loss class. |
| **Listener-arc polish** (2026-05-04) | ERA EXPLAINER block + REFRAME RULE for IDENTITY MODE askBirthplace/askName + GROUNDING RULE for pass2a earliest_years branch in `prompt_composer.py`. Operator-bubble retirement gated behind `LV_INLINE_OPERATOR_BUBBLES` flag in `app.js` (lines 2712 + 5800). Per design principle 2 (no operator leakage). |
| **WO-HARNESS-V4-VISIBILITY-01** (2026-05-04) | 3 harness scripts gain Playwright `--start-maximized` + `--window-position=0,0` launch args + `no_viewport=True` + `bring_to_front()` calls in UI.boot(). Closes v3 issue where harness ran headless-shaped without visible browser despite `headless=False`. |
| **TEST-23 v4 + v5 runs** (2026-05-04) | Two-person resume canary. v5: Mary recall_pre PASS on identity, Marvin spouse_probe PASS (4 hits), Marvin late_age PASS, Marvin recall_pre RED (harness 0-char capture, NOT product), Mary post-restart RED on session_start (separate harness flow bug, parked for read-only audit). |
| **4 product BUG WO specs banked** (2026-05-04) | `BUG-EX-NAME-EXTRACTION-NOW-01_Spec.md` (LANDED via blocklist patch 2026-05-05), `BUG-EX-DOB-LEAP-YEAR-FALLBACK-01_Spec.md` (Mary's 1940-02-29 → 1940-01-01 — DOB normalizer rejects valid leap-year), `BUG-EX-POB-CORRECTION-WRONG-PATH-01_Spec.md` (Mary POB correction routes to residence.place not personal.placeOfBirth), `BUG-EX-LLM-COMMENTARY-AS-VALUE-01_Spec.md` (LANDED via _is_llm_commentary patch 2026-05-05). |
| **WO-LORI-MEMORY-ECHO-ERA-STORIES-01 spec banked** (2026-05-05) | Phase 1c era-story recall — three-layer architecture (archive era-binding → peek_at_memoir era-grouping → compose_memory_echo render) behind `HORNELORE_MEMORY_ECHO_ERA_STORIES=0`. Closes era-click-vs-era-story-readback gap surfaced by ChatGPT diagnosis. |
| **WO-BUG-PANEL-EVAL-HARNESS-01 Phase 1** (2026-04-29 evening, #332) | Read-only Bug Panel Eval Harness cockpit. New `server/code/api/routers/operator_eval_harness.py` + `ui/js/bug-panel-eval.js` + `ui/css/bug-panel-eval.css`. 4-card surface (Extractor / Lori Behavior / Safety / Story Surface) backed by `/api/operator/eval-harness/{summary,reports,report/{name},log-tail}`. `HORNELORE_OPERATOR_EVAL_HARNESS=0` default-off gate → 404. Smoke-tested against `r5h-restore` (PASS, 70/104, mnw 1.2%, age 0.89d). Phase 2 (run buttons) parked. |
| **prompt_composer warmth + legacy-language cleanup** (2026-04-29 evening, #330) | Four narrator-facing string edits: 273-TALK no-go softened with 988/AFSP context; memory-echo "What I'm less sure about" reworded warmer for elder narrators; NO QUESTION LISTS RULE narrowed to narrator-facing interview mode only; WO-10C "cognitive difficulty (dementia or similar)" replaced with "benefits from extra pacing support" framing per SESSION-AWARENESS-01 banned-vocab spec. AST parse green. |
| **Eval-script printer crash fix** (2026-04-29 evening, `4ac71f0`) | r5h-place-guard initial run completed 110 cases (75/110 pass, 1.2% mnw) but crashed in `print_summary._print_breakdown("By era")` from `None` keys + mixed legacy era names. Three patches: `_canonical_eval_era()` helper using `legacy_key_to_era_id`, None-safe `_breakdown` + `_print_breakdown`, JSON write moved BEFORE printer (so future printer crashes never lose reports). `print_summary` itself wrapped in try/except. |
| **BUG-EX-PLACE-LASTNAME-01** (2026-04-29 afternoon, `9b9e557`) | Deterministic post-LLM regex guard at extract.py drops `.lastName`/`.maidenName`/`.middleName` candidates appearing only after place-prepositions. 6 new fixtures `case_105–110`. Smoke 15/15. Master pack 104 → 110. r5h-place-guard eval queued (#323). |
| **scan_answer() default-safe fallback** (2026-04-29 afternoon, chat_ws.py L206-228) | When scan_answer raises, force turn_mode='interview' so ACUTE SAFETY RULE in prompt fires + emit `[chat_ws][safety][default-safe]` log marker. Closes silent-skip gap surfaced by code review. ~17 lines. AST parse green. |
| **WO-LORI-SAFETY-INTEGRATION-01 Phase 1** (`8aab2c2`) | Chat-path scan_answer hook live in chat_ws.py L196-274. Mirrors interview.py:269-307. Persists segment flag + softened mode + WS overlay event + operator notify + forces turn_mode=interview. Doc surfaced 2026-04-29 — was already shipped before this session opened; master checklist had not been refreshed. |
| **db.py PRAGMA busy_timeout=5000** (`8023501`) | Fixes safety-hook lock contention surfaced after SAFETY-INTEGRATION-01 Phase 1 landed. Already active. |
| **WO-INTERVIEW-MODE-01 Phase 1** (`f1e0612`) + narrator-room unified 3-column (`bc02b32`) | Focused 3-column interview view; Memory River removed from narrator room. |
| **r5h-restore master eval** (`66f41ae`) | SPANTAG flag fix verified; 70/104 baseline preserved at the post-BINDING-01-rejection state. |
| **WO-EX-BINDING-01 PATCH 1-4 + micro-patch** (`42accc9` / `3f3deba`) | SPANTAG Pass 2 binding pre-normalizer + `_validate_item` re-entry + 9 `_FIELD_ALIASES`. Probed against 7 cases (3/7 primary 3/3) at `8564840`; v2/v3 (`2ec7de5`) confirmed micro-patch real, not stochasticity. Default-on master `r5g-binding-on-v1` REJECTED (`c547ddb`). Code in-tree behind `HORNELORE_SPANTAG=1` flag, default-off; iteration cycle TBD. |
| **WO-CANONICAL-LIFE-SPINE-01 Steps 3a-fix → 8** (2026-04-29) | Eight atomic commits migrating frontend + backend from legacy era keys to canonical 7-bucket era_ids (earliest_years / early_school_years / adolescence / coming_of_age / building_years / later_years / today) with self-healing read/write. Live-verified on Christopher Horne narrator. Memoir Peek shows 7 sections with warm heading + literary subtitle; Life Map renders 7 buttons routed through Step 7 confirm popover; Step 8 TXT export uses `data-era-id` selectors. |
| **r5h-followup-guard-v1 baseline lock** (2026-05-03) | Master 114 = 78/114 v3=49/72 v2=43/72 mnw=2. +1 across all three contract metrics vs prior r5h-followup-guard-v2 (77/114). Directly attributable to WO-EX-CASE-BANK-FIXUP-01 Phase 1 case_110 spouse-path normalization (banked 2026-05-02 evening). Scorer-drift audit ZERO on the case_110 flip. parse_success_rate 95.6%, truncation_rate 0.0%. Same two pre-r5e1 mnw carryovers (case_035 / case_093). |
| **WO-NARRATIVE-CUE-LIBRARY-01 Phase 1 (data) + Voice Library v1 + Cultural Humility Eval Pack** (2026-05-03) | ChatGPT base + Claude addendum + Gemini's Hearth/Field/Bridge/Shield mnemonic (post-Kawa cleanup). 12 cue types + 6 suppression markers + 4 followup_modes + 40 base eval cases + cultural humility eval + 22 sd_044-sd_065 cultural-context cases + 7-voice corpus reference doc. Hard wall: cue library may shape Lori's next question/statement/silence but cannot write any truth field. Phase 2 (cue detector code) staged for next session. |
| **WO-DISCLOSURE-MODE-01 spec banked** (2026-05-03) | Per-entry schema enum (narrator_consented / narrator_offered_no_consent / system_inferred_no_consent / sacred_do_not_persist). Includes ChatGPT's Pipeline Rules per stage + Claude's live evidence + cost + sequencing. Supersedes WO-SCHEMA-DIVERSITY-RESTORE-01 Phase 3 once Phase 1 lands. NOT IMPLEMENTED today; schema work waits until cue detector + Lori behavior wiring stable. |
| **WO-LORI-SENTENCE-DIAGRAM-RESPONSE-01 spec banked** (2026-05-02 late evening, integrated 2026-05-03) | Anchor Lori responses in narrator sentence structure via utterance-frame anchor selection. Phase 1 = scoring-only. Research-grounded in Alshenqeeti (2014). Phase 1 (golfball + sentence-diagram-survey scoring metrics) ready for next session. |
| **BUG-312 protected_identity gate** (2026-04-29) | `ui/js/projection-sync.js` L91-130. Protected fields require trusted source for ANY write, not just overwrites. Fixes BUG-309 DOB regression upstream. Closed task #309 completed-via-#312. |
| **Pre-laptop-migration backup** (2026-04-28T23:40) | `/mnt/c/hornelore_data/backups/2026-04-28_2340_before-laptop-migration-canonical-reset/`. SQLite integrity OK; WAL checkpoint clean. |
| **BUG-311 reclassification** (2026-04-29) | Investigation traced "Lori-text leakage" to extractor span-binding hallucination on narrator-side text (test-harness.js:65-98). Belongs in WO-EX-BINDING-01 lane as additional Type C fixture. No code change; investigation-only. |
| `r5h-postpatch` eval | Confirmed `r5h` baseline preserved (70/104). Three e579ed0 patches dormant + clean when SPANTAG=0. |
| **SPANTAG default-on REJECTED** at v3 attempt | Net -39 cases. Field-path hallucination cluster confirmed as binding-layer issue. SPANTAG demoted to evidence pipeline; BINDING-01 promoted to top. |
| #95 SECTION-EFFECT Phase 3 | PARKED. Type A/B/C question typology LOCKED. Bumper sticker: "Extraction is semantics-driven, but errors are binding-driven." |
| #141 WO-EX-FAILURE-PACK-01 | Cluster JSON sidecar wired into master eval. |
| #142 WO-EX-DISCIPLINE-01 | Run-report header live. Confirmed working in r5h-postpatch. |
| WO-EX-SPANTAG-01 Commit 3 | Pipeline wired default-off. Pass 2 controlled-prior carries section + target_path only (era/pass/mode dropped per Q1=NO). |
| WO-MEDIA-ARCHIVE-01 | Document Archive lane shipped (PDFs / scanned docs / handwritten notes / genealogy / letters / certificates / clippings). |
| WO-LORI-PHOTO-* (PHASE 1 + Phase 2 partial) | Photo intake, EXIF auto-fill, Review File Info preview, batch upload, View/Edit modal, narrator-room lightbox, dedupe-by-file-hash. |
| WO-NARRATOR-ROOM-01 | Three-tab shell, narrator room layout, Take-a-break overlay, chat scroll stabilization. |
| WO-ARCHIVE-AUDIO-01 | Memory archive backend (per-narrator zip export, two-sided text transcript, narrator-only audio rule). |
| WO-AUDIO-NARRATOR-ONLY-01 | Per-turn audio capture (backend + frontend MediaRecorder). |

---

## Standing reminders

- Chris runs / stops the API himself. Don't include `start_all.sh` or `stop_all.sh` in command blocks.
- Cold boot is ~4 minutes (HTTP listener ~60–70s; LLM weights + extractor warmup another 2–3 min). This is normal, not a bug. Eval harness must wait for warmup or first cases time out.
- Every eval that follows a code change must report the standard post-eval audit block (in CLAUDE.md).
- Each WO must report the audit block before being called done (extractor lane); Lori-behavior lane uses its own acceptance gate per WO.
- When three agents (Claude/Gemini/ChatGPT) converge, act — don't re-argue.
- Sandbox can't run git. Commit blocks must be handed to Chris as copy-paste from `/mnt/c/Users/chris/hornelore`.
- Banned vocabulary in Lori-behavior WOs is structural (not stylistic). Pull requests against any Lori-behavior phase introducing scoring/classification/drift-tracking get rejected on the values clause alone.

---

## Quick file reference

| Kind | Path |
|---|---|
| Canonical extractor architecture | `docs/specs/LOREVOX-EXTRACTOR-ARCHITECTURE-v1.md` |
| Active extractor spec | `WO-EX-BINDING-01_Spec.md` |
| Parent-session readiness gates | `docs/PARENT-SESSION-READINESS-CHECKLIST.md` |
| Parity audit (this batch) | `docs/reports/HORNELORE_PARITY_AUDIT_2026-04-27.md` |
| Lori behavior — safety | `WO-LORI-SAFETY-INTEGRATION-01_Spec.md` |
| Lori behavior — session awareness | `WO-LORI-SESSION-AWARENESS-01_Spec.md` |
| Lori behavior — interview discipline (Phase 2 implementation) | `WO-LORI-ACTIVE-LISTENING-01_Spec.md` (2026-04-29) |
| Lori behavior — response harness | `WO-LORI-RESPONSE-HARNESS-01_Spec.md` |
| Lori behavior — reflection runtime shaping | `BUG-LORI-REFLECTION-02_Spec.md` (Patch C LANDED 2026-05-05) |
| Lori behavior — era-story readback | `WO-LORI-MEMORY-ECHO-ERA-STORIES-01_Spec.md` (2026-05-05) |
| Provisional truth (cross-restart memory) | `WO-PROVISIONAL-TRUTH-01_Spec.md` (Phase A LANDED 2026-05-04) |
| Harness visibility (Playwright) | `WO-HARNESS-V4-VISIBILITY-01_Spec.md` (LANDED 2026-05-04) |
| Extractor BUG specs (banked 2026-05-04/05) | `BUG-EX-NAME-EXTRACTION-NOW-01_Spec.md` (LANDED), `BUG-EX-DOB-LEAP-YEAR-FALLBACK-01_Spec.md`, `BUG-EX-POB-CORRECTION-WRONG-PATH-01_Spec.md`, `BUG-EX-LLM-COMMENTARY-AS-VALUE-01_Spec.md` (LANDED) |
| Timeline render lane (NEW 2026-05-05) | `WO-TIMELINE-RENDER-01_Spec.md` + `WO-TIMELINE-CONTEXT-EVENTS-01_Spec.md` (renamed from WO-WORLD-EVENTS-TABLE-01) |
| Voice corpus reference | `docs/voice_models/VOICE_LIBRARY_v1.md` (heritage_tags taxonomy source for timeline_context_events; v1 spine, extensible) |
| Phase 0 BUG specs banked (2026-05-05) | `BUG-UI-POSTRESTART-SESSION-START-01_Spec.md`, `BUG-LORI-MIDSTREAM-CORRECTION-01_Spec.md`, `BUG-LORI-LATE-AGE-RECALL-01_Spec.md`, `BUG-HARNESS-RECALL-DICT-FIELDS-01_Spec.md` |
| Provisional truth Phase E (PROMOTED 2026-05-05) | `WO-PROVISIONAL-TRUTH-01_Spec.md` (extended with Phase E — frontend BB state mirror sync) |
| Timeline scaffold design notes (REFERENCE ONLY) | `docs/drafts/timeline_scaffold_chatgpt_2026-05-05.md` (NOT paste-ready; bug audit appended; build from spec instead) |
| Schema diversity restoration | `WO-SCHEMA-DIVERSITY-RESTORE-01_Spec.md` (2026-04-29) |
| Multimodal affect (parked) | `WO-AFFECT-ANCHOR-01_Spec.md` |
| Lorevox parity audit (template universe) | `docs/reports/LOREVOX_TEMPLATE_PARITY_2026-04-29.md` |
| BUG-311 investigation (reclassification → BINDING-01) | `docs/reports/BUG-311_INVESTIGATION_2026-04-29.md` |
| Code review (2026-04-29) | `docs/reports/CODE_REVIEW_2026-04-29.md` |
| Eval bank | `data/qa/question_bank_extraction_cases.json` (104 master) + `data/qa/question_bank_generational_cases.json` (24 canon-grounded) |
| Latest eval | `docs/reports/master_loop01_r5h-postpatch.json` (+ `.console.txt` + `.failure_pack.json`) |
| Extract router | `server/code/api/routers/extract.py` |
| Safety detector | `server/code/api/safety.py` |
| Prompt composer | `server/code/api/prompt_composer.py` (ACUTE SAFETY RULE at L108–193) |
| CLAUDE.md | `CLAUDE.md` (read first every session) |
