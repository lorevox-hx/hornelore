# MASTER_WORK_ORDER_CHECKLIST

**Active as of:** 2026-07-28 (**WO-TRAVEL-DOC-GOOGLE-PHOTOS-PICKER-01 -- PHASE 2B, ACQUISITION STEP COMPLETE AND THEN CORRECTED IN FIVE PLACES. The ingest route is NOT built and is the next step.**) `server/code/services/google_picker/acquire.py` provides `max_bytes()`, `sniff_image()`, `download_original()`, `read_evidence_metadata()`, `staging_dir_for()`, `incoming_dir_for()` and `stage_original()`. **No route, no database write, no candidate, no UI, no flag, no schema, no migration, no dependency.** The module holds no DB handle and cannot create or decide a candidate -- which is what makes spec 12.2's required order structural rather than a comment: download to a temp file, validate, extract metadata, `candidate_create()`, take the id it ACTUALLY returned, then move the bytes into that id's directory. **Three collaborators were read first and two are traps.** `photo_intake/ingest.py` ends at `photo_repo.create_photo(...)`, a **photos** row -- reusing it would have minted photo rows for picked images and walked straight through the Phase 3 promotion wall; only the leaf helpers `sha256_file`, `extract_exif` and `classify_metadata_trust` are reused. `photo_intake/storage.py` is unusable twice over: `_safe_ext()` derives the extension from the CLAIMED filename and falls back to `.jpg`, the exact behaviour 12.1 forbids copying, and `store_photo_file` writes to the narrator archive layout rather than 12.5's `DATA_DIR/import_staging/<batch_id>/<candidate_id>/original.<verified_ext>`. **The provider is not trusted about content:** the declared `mimeType` is not an input to the sniffer at all. **Video is refused as `unsupported_content`, permanent, producing NO candidate row** -- keeps 12.3 intact, still a ruling Chris should confirm. **The cap is enforced during the stream** because the Picker reports no byte size. **Date falls back, location does not:** EXIF time -> `exif`; none -> Picker `createTime` -> `provider_metadata`, passed as a parameter so the route can withhold it; nothing -> `unknown`. Location: valid EXIF GPS -> `exif_gps`, **anything else -> null/null/`unknown`, always**. `baseUrl` and the token never reach a log line or an exception message. **THEN FIVE FIXES CHRIS ORDERED BEFORE THE ROUTE MAY BE STARTED, ALL LANDED.** (1) **`staging_failed` contradicted itself** -- it sat in `PERMANENT_REASONS` while both raise sites passed `retryable=True`, because `retryable` was a CONSTRUCTOR ARGUMENT and so a per-raise-site opinion able to disagree with the vocabulary. `AcquireError` no longer accepts one; `retryable` is derived from `reason` via `is_retryable()`. `staging_failed` and `hash_failed` are both **retryable**, and that is a product ruling: a locked file or a full disk is a fact about this machine, not an operator's verdict about the photograph. An unclassified reason fails **open** to retryable and says so. (2) **Staging is now genuinely same-filesystem and atomic** -- `download_original(..., batch_id=...)` writes into `DATA_DIR/import_staging/.incoming/<batch_id>/` instead of the system temp directory, because `/tmp` and `DATA_DIR` are different filesystems here and the final move had silently degraded from a rename to an interruptible copy. The `.incoming` reservation is STRUCTURAL: `_SAFE_ID_RX` requires a leading alphanumeric, so no accepted batch id can begin with a dot. (3) **An existing staged original now survives a failed replacement** -- write to a `.incoming-` scratch name inside the candidate's own directory, `os.replace` atomically onto `original.<ext>`, and only THEN remove a stale `original.*`. The old order deleted first, so an interrupted re-ingest could end with NO staged original. (4) **A hashing failure unlinks the temp file** -- `sha256_file` runs after the streaming loop's own cleanup, so a read error there would have left a complete, valid, unreferenced photo in the incoming directory with nobody holding a reference to it. (5) **Provider timestamps CONVERT their offset rather than trimming it** -- slicing to 19 characters turned `+02:00` into the same digits two hours wrong, silently. Converting to naive UTC rather than keeping the provider's wall clock is the deliberate half: `trip_photo_clustering._parse_dt` has no tz-aware branch and cannot read a numeric offset, so `+02:00` would drop the photo out of time scoring without a word. UTC wall time is NOT local capture time -- a photo taken at 01:30 in Rome is dated the previous day -- which is exactly why it is labelled `provider_metadata` with `date_precision='unknown'`. **A GREEN SUITE WAS DEFENDING THE TIMEZONE BUG:** `test_fractional_and_offset_provider_times_normalise` asserted the offset should be TRIMMED. It was deleted and replaced with four tests that assert the specification instead. **This is the SECOND time in this lane that the tests agreed with the code rather than the spec** -- the first was the EXIF-double swap -- and it is recorded as a pattern, not an incident. `tests/test_google_picker_phase2b_acquire.py` is **99 tests, executed green on the device**; 2A 38/38 green and `test_operator_picker` 15/15 green alongside it. `test_google_picker_phase1` cannot run here for an ENVIRONMENTAL and PRE-EXISTING reason -- it imports `fastapi`, which bare `python3` lacks and which the repo `.venv` reached through this mount also lacks (that venv is additionally missing `requests`); not caused by this change and not fixed, per the no-environment-work wall. `acquire.py` grew exactly one import, `errno`, for the EXDEV branch, and the exact-import-set wall test was updated to NAME why it arrived rather than loosened to stop noticing. 2A's no-route wall still stands. **NEXT: the ingest route.** It wires the 2A listing and this module into `POST /api/google-picker/sessions/{batch_id}/ingest`, calls `candidate_create` in the 12.2 order, **passes `batch_id` to `download_original` so staging stays atomic**, reports partial failures per 12.4, defines which failures make a batch wholly unusable, and fixes three now-false statements the router still carries -- the docstring claiming the listing call is unimplemented, `"phase": 1` on `/health`, and `"ingest_available": false`. **Reported, deliberately NOT fixed, different subsystem:** `tests/test_metadata_trust.py::ClusteringConsumesTrustTest::test_missing_trust_is_legacy_trusted` fails `None != 's1'` on `trip_stop_id` -- pre-existing, belongs to the trip-photo-clustering lane. 2C (doctrine doc, which must also fix the spec's stale "No Phase 2 code exists." line) and 2D (operator UI) untouched. **TOKEN EXPIRY:** the refresh token dies around 2026-08-03; a 503 `refresh_token_expired` is that, not a defect.

**Previously:** 2026-07-28 (**WO-TRAVEL-DOC-GOOGLE-PHOTOS-PICKER-01 -- PHASE 2A COMPLETE: THE MEDIA-ITEM LISTING CLIENT.**) Phase 2 was split into four reviewable pieces in spec section 12.8 after an external work order (WO-TRAVEL-001) proposed doing all of it at once; this session did 2A and nothing else. `picker_client.list_media_items(access_token, picker_session, *, page_size=100)` reads back the operator's Google Photos selection, pages `nextPageToken` to exhaustion, validates every item, and returns normalized dicts. **No database, no downloads, no staging, no route, no UI, no flag, no schema, no migration, no dependency.** It has no caller on purpose -- 2B wires it up, and a Phase 1 wall test now asserts that the router does not reference it. The Picker response shape was re-verified against Google's live reference before the code was written, confirming a second time that there is no GPS field and no byte-size field on a picked item, which is why 2B must download bytes rather than trust the provider. `base_url` IS returned (2B cannot download without it) and is never logged and never placed in an exception message. A partial listing is never returned quietly: a repeated page token and an endless token chain both raise rather than truncate. `tests/test_google_picker_phase2a.py` is 38 tests, needs no fastapi, and was executed green here rather than handed over unrun; the no-leak checks run against a real captured log stream and the collaborator wall is an exact `ast` import set. One Phase 1 test was moved rather than deleted: it had asserted the client has NO listing, and now asserts what listing must still not do. **NEXT: Phase 2B (acquisition and ingest) is a scope wall and was not started.** Carried into 2B: define exactly which failures make a batch wholly unusable -- an expired-before-listing or nonexistent Picker session may close the batch failed; a temporarily unavailable token, a timeout before the first page, and one item's `baseUrl` expiring after others ingested must leave the batch open and retryable. Also carried into 2B: **GPS is still in the plan and simply cannot exist in 2A**, because 2A has no downloaded bytes. Valid EXIF GPS -> latitude/longitude recorded with `location_source='exif_gps'`; no valid EXIF GPS -> both null with `location_source='unknown'`; **Picker metadata is never a GPS source.** `photo_intake/exif.py` already implements exactly this, plus a third state 2B must decide on -- `present_unparseable` for a GPSInfo block that exists but is corrupt or out of range, which has no enum value and no candidate column, so `match_reason` is its only home. And note the asymmetry: `provider_metadata` is a legitimate `taken_at_source` fallback but must never be a `location_source` in this lane, even though both enums permit it.

**Previously:** 2026-07-27 (**WO-TRAVEL-DOC-GOOGLE-PHOTOS-PICKER-01 -- PHASE 1 COMPLETE AND LIVE-PROVEN AGAINST GOOGLE.** Chris closed the web-stack/test lane ("No more environment/test work in this session") and named the next product lane: bring real Google Photos candidates into the evidence queue. The spec at `docs/wo/WO-TRAVEL-DOC-GOOGLE-PHOTOS-PICKER-01_Spec.md` recorded the four verified Picker API constraints that determine the whole architecture -- **no GPS field** and **no byte-size field** anywhere in the Picker response, a **`baseUrl` valid for only 60 minutes**, and a **refresh token that expires every 7 days** while the Google project sits in Testing publishing status. Chris answered all six section 7 decisions exactly as recommended and instructed: "Build Phase 1 only -- credentials, health, session create/poll/delete, no bytes and no candidates yet." **Phase 1 is written**: a new `server/code/services/google_picker/` package (`oauth.py` for token minting and presence booleans, `picker_client.py` for session create/get/delete) plus `server/code/api/routers/google_picker.py` with four routes behind a **double gate** (`HORNELORE_GOOGLE_PICKER` AND `HORNELORE_IMPORT_PROVENANCE`, both default-OFF, enforced as a router-level `Depends` so the 404 beats FastAPI's 422 body validation). Credentials are read from the environment only and `/health` reports **presence booleans, never values** -- not a prefix, not a length, not a masked tail. `POST /sessions` calls Google first and opens the batch second, releasing the picker session if the batch open fails, so an upstream failure leaves no debris. `DELETE /sessions/{batch_id}` ends the session **at Google only** and says `batch_deleted: false` in its own payload -- the no-DELETE rule on the evidence lane is intact. **No migration and no dependency**: 0037's `source` CHECK already permits `google_photos_picker`, `external_ref` was built for a provider session handle, and `requests` is already in both venvs. **Tests green and committed** (a65e4f1 code, c717042 docs); Phase 1 is not yet proven LIVE -- the lane stays dark until the flag and credentials are in `.env` and the API is restarted. **Phase 2 landmine recorded in the spec:** `_SECRET_KEY_HINTS` contains `session_id`, so a Phase 2 `match_reason` key named `session_id` or `picker_session_id` would raise `ExternalTokenError`; use `picker_session`. Phases 2/3/4 are untouched scope walls -- no bytes, no candidates, no `PROMOTABLE_SOURCES` change, no UI. **Identity boundary set as permanent doctrine** (spec section 10, plus a `## Google Photos Picker identity boundary` section in `CLAUDE.md`): the Google Cloud project / OAuth client owns app config and quota, the authorized Google account owns the source library and is routinely a *different* account, the operator drives the import, and the person/narrator + optional trip are the destination. A Google account is not a narrator; the app must never infer `person_id` or `trip_id` from it. Correction applied to the handoff text: `narrator_id` is **not** a third destination field -- it is the `photos` table's column name for the same identity the import lane calls `person_id`, compared directly at `import_repository.py:407` and `:1604`, and that comparison *is* the cross-person guard. The multi-operator future is documented as FUTURE DESIGN ONLY in `docs/wo/WO-LOREVOX-MULTI-OPERATOR-GOOGLE-AUTH-01_Spec.md` -- **no code**: no token tables, no encryption machinery, no connect/disconnect routes, no multi-user auth, and per-narrator Google credentials are forbidden permanently rather than deferred. **Naming decision awaiting Chris:** that doc is deliberately NOT called `...-PICKER-PHASE-2-MULTI-OPERATOR-AUTH-01`, because "Phase 2" of this lane already means the fetch/ingest phase in the committed spec, `CLAUDE.md` and this checklist; one word and it gets renamed.) **LIVE PROOF, 2026-07-27.** The lane is no longer dark. Chris created the Google Cloud project `Lorevox-2026`, enabled `photospicker.googleapis.com`, configured the OAuth consent screen as External/Testing with his own account as the single test user, created a **Web application** OAuth client with `https://developers.google.com/oauthplayground` as an authorized redirect URI (JavaScript origins left empty), minted a refresh token through the OAuth Playground against the picker scope, and installed `HORNELORE_GOOGLE_PICKER=1` plus the three `GOOGLE_PICKER_*` values in `.env`. Three live checks then passed in order. `GET /api/google-picker/health` returned `phase: 1`, both flags `true`, three presence booleans `true`, `credentials_complete: true`, `access_token_cached: false` -- **and no prefix, length or masked tail of any credential anywhere in the payload**, which is the credential rule surviving contact with real values. `POST /api/google-picker/sessions` with `person_id` `a4b2f07a-7bd2-4b1a-9cf5-a1629c4098a2` (Christopher Todd Horne) and no trip minted a real access token, created a real Picking session at Google, and opened `import_batch` **`202718fb-314e-40d0-b748-c6525fcdaf68`** with `source='google_photos_picker'` and the Picker session id in `external_ref`. Chris opened the returned `picker_uri`, selected photos in Google Photos, and `GET /api/google-picker/sessions/{batch_id}` returned `media_items_set: true`, `batch_status: open`, `ingest_available: false`. **The phase wall held under live conditions:** real photos were selected at Google and Hornelore downloaded no bytes, created no candidate rows, and did not touch `PROMOTABLE_SOURCES`. **Operational note, not a defect:** the project sits in Testing publishing status, so the refresh token expires every 7 days and must be re-minted in the OAuth Playground; the symptom is a 503 with reason `refresh_token_expired`. **Phase 2 (ingest/bytes/candidates) remains a closed scope wall and was not started.**

**Previously:** 2026-07-27 (**FIX-TRIP-DELETE-STALE-ASSERTION-01 -- open item 9 closed the same day it was filed. Test-only; no product code changed.**) `tests/test_trip_days_http_sequence.py` step 7 asserted that a bare `DELETE /api/trips/{id}` returns 200/204. That assertion predated WO-EVIDENCE-LIFECYCLE-TRIP-FORCE-01 Phase 2, so the suite was asserting the **absence of a shipped safety feature** -- the impact gate correctly refuses an unforced delete of a trip carrying 9 `trip_days` and 1 `trip_bio_suggestions` row. Rather than merely flip the expectation to 409, step 7 now drives the whole gate through real HTTP in three parts: **7a** unforced delete -> 409 with `requires_force`, `trip_id` echoed and `counts["days"] == 9`, then a follow-up GET proving the refusal modified nothing; **7b** `force: true` with a confirm id that does not echo the path id -> 422, again proving nothing was removed; **7c** `force: true` with the exact `confirm_trip_id` -> 200 with `deleted: true`, then GET -> 404. That converts a stale red into coverage the gate never had at the HTTP layer. **One real detail the web-stack alignment made unavoidable:** httpx's `.delete()` takes no `json=` argument, so the force bodies go through `client.request("DELETE", ...)`. Under the pre-alignment `.venv` this test would have been written against a different httpx generation. No product code, no router, no repository change; `narrator_delete_audit` already exists in `init_db()` so the force path's audit row writes into the per-test temp sqlite. Open item 9 is retired. Item 10, the serving `lxml` 6.1.1 vs pinned 6.0.2 discrepancy, stays open and untouched.)

**Previously:** 2026-07-27 (**WO-WEB-STACK-TEST-ENV-ALIGNMENT-01 LANDED -- the test web stack no longer lies. Environment only; no product code changed, no test changed, no model/GPU package touched.** The drift audit's section 4.2 called this the one live mismatch: `.venv` was running starlette 1.0.0 / fastapi 0.136.1 / pydantic 2.13.4 / httpx 0.28.1 under eight suites that drive `TestClient` against the real app, while `.venv-gpu` answers real requests on starlette 0.52.1 / fastapi 0.135.1 / pydantic 2.12.5 / httpx 0.27.2. A green TestClient result was therefore evidence about a framework generation production does not run. Nine package moves closed it -- the seven from audit 4.2 plus two the audit had wrong: `anyio` was filed as cosmetic when it is starlette's async substrate and the engine under TestClient's blocking portal (moved to the web-stack block at 4.12.1), and `sniffio` was absent from `.venv` entirely because httpx 0.28 dropped the requirement that 0.27.2 has, so it installs net-new at 1.3.1. A third audit error is recorded but deliberately not corrected: section 8 called `markupsafe==3.0.2` already aligned when `.venv` has 3.0.3 -- left at 3.0.3 and listed as known drift. Section 8's re-run block also listed seven suites, dropping `tests.boris_quality.test_phase3_facts_add_truth_v2`; all eight were run. **Deliverable:** `requirements-test.txt` at the repo root -- 30 pins, a web-stack block that MUST match `requirements-gpu.txt` exactly, and an explicit bottom section naming the eight transitives left drifted on purpose so a red suite after the install has exactly one possible cause. **Result, stated without inflation:** the downgrade produced ZERO behavioural differences. Baseline was 252 tests / 251 pass / 1 pre-existing failure; after alignment the same suites return the same counts. The value is not a bug fix -- it is that audit section 7's caveat *"a green result there is weaker evidence than it looks"* is now retired, and section 7 says `.venv` is authoritative for the HTTP/route/repository layer full stop. **The one red suite is red for its own reason and stayed red identically:** `tests/test_trip_days_http_sequence.py:201` asserts a bare `DELETE /api/trips/{id}` returns 200/204, but by step 7 the sequence has created 9 `trip_days` and 1 `trip_bio_suggestions` row, so `trip_delete_impact()`'s gate correctly returns 409 `requires_force`. The test predates the impact gate; its own comment still reads `# 7. DELETE -- no 500`. **The server is right and the test is stale.** Not edited here -- test edits were outside this package's scope wall, so it is filed as open work item 9 for a ruling. **Also recorded, not acted on:** a hang I called wrongly. I read `webstack_after.log`'s mtime as frozen on the `/mnt/c` mount and told Chris to Ctrl-C a run that was fine; his probe returned `Ran 1 test in 4.049s`. Root cause and the durable lesson -- do not infer process state from mtime or size on a `/mnt/c` path, and unittest writes dots to unbuffered stderr while summaries go to block-buffered stdout -- are in section 12 of the work order. **Scope walls held:** no torch/CUDA/peft/accelerate/bitsandbytes/transformers/tokenizers/huggingface-hub churn, no `.venv-gpu` change, no `requirements-gpu.txt` edit, and the serving `lxml` 6.1.1 vs pinned 6.0.2 discrepancy stays recorded as serving-env drift per Chris's explicit ruling. Execution record: `docs/reports/WO-WEB-STACK-TEST-ENV-ALIGNMENT-01.md`. Audit amended at sections 7 and 10.)

**Previously:** 2026-07-27 (**WO-POST-LORI-CLEANUP-AND-UNBLOCK-01 -- three lanes delivered as one package. Lane 2 CODE-LANDED + FORMAL-VERIFIED. Lane 3 CODE-LANDED + FLAG-LIVE + FORMAL-VERIFIED. Lane 1 is an audit and ships two documents.** Lane 1, `docs/reports/VENV_DRIFT_AUDIT_2026-07-27.md`: the `transformers` 4.55.4 vs 5.8.0 mismatch that opened the lane is ruled **defer**, because it is unreachable -- zero test files import `transformers` and `api.py` hard-imports `peft` at module scope, which `.venv` lacks, so `.venv` cannot execute transformers code at all. Downgrading would drag `huggingface-hub` and `tokenizers` with it. The rule is environmental: transformers-importing suites run in `.venv-gpu`. The audit's real find is unnamed drift that IS reachable -- `starlette` 0.52.1 -> 1.0.0 (0.x -> 1.x major) with `httpx`, `fastapi` and `pydantic` alongside it, under the eight suites that drive `TestClient` against the real app. Ruled **align now**; a proposed `requirements-test.txt` and its install/re-run blocks are in audit section 8, and the alignment is deliberately NOT executed. torch/CUDA and peft/accelerate/bitsandbytes are intentional divergence; eleven lazily-imported packages are deferred. Independent find: `requirements-gpu.txt` pins `lxml==6.0.2` while `.venv-gpu` runs 6.1.1, so the serving venv is off its own pin -- Chris's call. Audit section 7 states which environment is authoritative for which kind of test. Lane 2: `lv80ToggleNarratorSwitcher` was never renamed or removed -- it is alive at `hornelore1.0.html:6546`. The ReferenceError was a load-order race, confirmed in the browser: right after navigation the page showed `cardExists: true` with `toggleOnWindow: "undefined"` at `scriptCount: 25`, and at `scriptCount: 82` both functions were defined and the click threw nothing. Fixed in two small halves -- the inline `onclick` at line 2928 is feature-tested and falls back to `console.warn` (no native dialog), and explicit `window.*` mirrors make the global binding intentional. FORMAL-VERIFIED: a real mouse click opened the Narrators switcher with all four narrators, console clean of the ReferenceError. 11 tests in `tests/test_narrator_switcher_handler.py`. Lane 3: captured Travel Doc notes now have a surface the operator can reach them from. Read-only `captured_notes_review_list` / `captured_notes_review_counts` in `trip_repository.py`, flag-gated `GET /api/trips/captured-notes` in `trips.py`, and a Captured Notes tab in Travel Doc Lab that is exempt from the `st.trip` gate exactly as Evidence is. Promotion reuses the existing `PATCH /api/trips/location-notes/{note_id}` -- no second write route, no auto-promotion, `include_in_memoir=0` still the default, no archive reach, two-surface rule untouched. 24 tests in `tests/test_captured_note_review.py` against real sqlite, one class per acceptance line. **FLAG-LIVE + FORMAL-VERIFIED 2026-07-27 against the restarted stack, all twelve of Chris's points, no defect found and no code changed.** The 405 was the stale uvicorn exactly as diagnosed and the restart cleared it: `openapi.json` path count 225 -> 226 carrying `/api/trips/captured-notes` with a single `get`, and the route answers **200**. Driven from the `:8082` operator page against `:8000`, the tab paints from the wire -- `GET /api/trips/captured-notes` with `person_id`, and with `person_id,source_surface` when the surface filter is set -- reading `12 notes - 0 in memoir - 12 not in memoir` with `Captured from Travel Doc modal 4`, and the `promoted=true`/`false` filters returning 0 and 12. **The trip-gate exemption was proved in both directions**, not asserted: switching to Kent James Horne, who has zero trips, Evidence and Captured Notes render their own screens while Trip Plan, Photos, Story Notes, Travelogue, Draft and Lori all render *No trips yet for this narrator*, and Captured Notes shows an honest `0 notes` empty state. All 12 rows read `include_in_memoir=0` before any action -- **opening the screen promotes nothing**. Promoting note `9df82b33` (Bismarck Trip, `travel_doc_modal`) issued exactly `PATCH /api/trips/location-notes/{note_id}` and a re-read, **no second write route**, and the trip's `memoir-preview` went `story_notes` 0 -> 1 with that note's text; demoting returned it to 0 with the payload byte-length back to its exact pre-promotion 685 and SQLite back to `include_in_memoir=0`. **The archive boundary held, read straight out of the database rather than assumed:** `turns` 1397 -> 1397, `memory_archive_turns` 0 -> 0, `memory_archive_sessions` 0 -> 0, `trip_location_notes` 12 -> 12 -- promotion writes one flag on one row and touches nothing else, and `archive-writer.js` still logs auto-chain DISABLED per BUG-209. Console: 218 messages across the full round trip, **zero errors**, the one WARNING being the pre-existing `[bb-hydrate]` cross-narrator guard. The served `travel-doc-lab.js` was confirmed to be the committed build (cache-busted URL and a cache-bypassing fetch both 276,776 bytes containing `captured-notes`). Lane 2 was re-proven incidentally in the same pass -- a real mouse click on the active narrator card opened the Narrators switcher with all four narrators. **The package is now fully live-proven; the work order closes.** Live-check detail is appended to `docs/reports/WO-POST-LORI-CLEANUP-AND-UNBLOCK-01_CLOSEOUT.md`. Chris's two environment rulings stand and were honoured: the `lxml` serving-venv pin is **recorded as serving-env drift and not touched**, and Starlette/FastAPI/httpx/pydantic were **not downgraded inside this work order** -- the web-stack alignment pass is the next real work and is **its own deliberate package**, not slipped into this one. Incidental, under the fix-and-document standing order: sixteen test files whose `sys.modules` stubs made tests lie (bare `class _BaseModel: pass` plus a missing `fastapi.responses`, where whichever file loads first wins for the whole process) were swept, each rewrite `ast.parse`-gated; and `tests/test_trip_editable_fixes.py`'s four `FOREIGN KEY constraint failed` errors were proved pre-existing (re-run with the pre-sweep stub: identical four) then fixed test-only with a `_seed_photo()` helper, now 30 OK. `tests/test_trip_draft.py`'s fastapi ModuleNotFoundError was deliberately NOT stubbed -- that suite tests real fastapi. Broad sweep: 44 `test_t*.py` suites green; the six lane and guard suites green individually and as a 228-test single-process batch. Closeout: `docs/reports/WO-POST-LORI-CLEANUP-AND-UNBLOCK-01_CLOSEOUT.md`. No new flag; no schema, migration, capture-rule, archive-write or DELETE change; no package installed or removed. Scope walls held: no Picker, no Takeout, no archive rewrite, no Lori Review Assistant, no per-lane work orders.)

**Previously:** 2026-07-27 (**BUG-LORI-REASONING-LEAK-01 CODE-LANDED -- a response-guard detector miss, fixed as one regex.** Lori's own planning sentence (`The narrator is speaking in English, so I will respond in English too.`) was persisted as assistant content with the real greeting sitting in quotes behind it. The guard pipeline was already running on that turn -- `apply_response_guards` at `chat_ws.py:4375`, upstream of the modal archive skip -- so this was never an archive, export or Travel Doc wiring bug. `detect_meta_response_leak()` simply had no pattern for `respond IN <language>` (the 2026-07-07 preamble regex enumerates by/with/using) or for a third-person `The narrator is ..., so I ...` clause. Two narrow alternatives added to `_META_REASONING_RX` in `server/code/api/services/lori_response_guards.py`; no repair function, guard ordering or call site changed. The existing quoted-draft recovery returns the real greeting. 7 new tests in `tests/boris_quality/test_phase5_meta_response_guard.py`. NOT FORMAL-VERIFIED: the leak is stochastic, so live re-observation is opportunistic; `test_chat_ws_guard_failure` needs `.venv`.)
**Previously:** 2026-07-27 (**WO-MEMOIR-TRIP-STORY-LANE-01 CODE-LANDED, and WO-EXPORT-ARCHIVE-WRITER-BRIDGE-01 CLOSED NO-DEFECT before it.** The bridge work order was investigated and refused: the archive writer is healthy, and the one thing missing from the archive -- `travel_doc_modal` turns -- is deliberately excluded by the two-surface rule of 2026-07-09, locked by `tests/test_modal_archive_boundary.py`. No production code changed for it; the finding is `docs/reports/WO-EXPORT-ARCHIVE-WRITER-BRIDGE-01_DIAGNOSIS.md` at 1a7f3d8. The real gap it exposed -- promoted trip stories had no route into the narrator memoir -- is what WO-MEMOIR-TRIP-STORY-LANE-01 closes, by a DB read rather than an archive bridge. `_trip_story_sections()` in `server/code/api/routers/memoir_export.py` harvests `trip_location_notes` rows with `include_in_memoir=1` into their own `From your travels -- <trip>` memoir sections, gated on the default-OFF `HORNELORE_TRIPS`, never raising, never writing to the archive. 21 new tests in `tests/test_memoir_trip_story_lane.py`; `test_modal_archive_boundary` still 5/5 green. Trip DOCX export untouched. NOT FORMAL-VERIFIED: `tests/test_memoir_export_security` needs `.venv` (real fastapi), and no note has been promoted yet, so a live export currently yields no trip section by design.)

**Previously:** 2026-07-27 (**WO-TRAVEL-DOC-EVIDENCE-REVIEW-QUEUE-01 Phase 4 live smoke RUN AND PASSED, and the one thing it broke is now Decision 5 -- decisions are one-way, enforced in the repository.** Twenty-seven probes against the **serving** stack, driven from the `:8082` operator page against `:8000`, not from the unittest harness. Everything held -- batch and candidate creation, the `/queue` shape, trip filing and its 409 on a bogus trip, both kinds of hidden and the `include_hidden` restore, all four decisions, the 400 on `skipped`, the 409 on accepting with no `photo_id`, the 409 on promoting with no bytes, the 415 on `text/plain`, a real JPEG promoting to a `photos` row read straight out of SQLite and confirmed born `narrator_ready=0` / `date_approved_for_lori=0` / `location_approved_for_lori=0`, the candidate still `pending` after promotion, the re-promote returning the same id and creating nothing, and **405 on all three DELETE probes**. Blast radius: `photos` 16 -> 17, `import_batch` 0 -> 1, `import_candidate` 0 -> 5, with `trips` 2 / `people` 36 / `trip_photo_context` 51 unmoved. **One probe failed and it was the one that mattered:** re-deciding an already-`accepted` candidate as `rejected` returned **200** and set its `photo_id` to NULL, stranding the promoted `photos` row unreferenced and still unapproved -- the exact failure mode Decision 4 refused a DELETE route over, sitting open one route to the left. The invariant was written in `candidate_decide()`'s docstring and in the screen's own comment and **enforced in neither**; the UI's `c.state === "pending"` gate was the only thing holding it, and `grep` found zero server tests on the behaviour in either direction. **Chris ruled: strict guard, in the repository, 409 and nothing changes** -- *"The guard belongs in `import_repository.candidate_decide()`, not only in the UI. The UI already hides re-decision, but WO-3 Picker/Takeout or any future caller could hit the API directly."* Built as a new `CandidateAlreadyDecidedError` -> **409** (not `InvalidStateError` / 400: the payload is well formed and it is the resource state that refuses, same precedent as `BatchClosedError`), raised before any lookup or write so a refused re-decision touches nothing, and tested against **stored state** rather than against whether the new decision differs, because re-asserting the same decision is still a second review event that would rewrite `reviewed_at`. A **mirror guard** in `candidate_promote()` closes the same stranding from the other end -- a decided candidate with no photo cannot mint one -- conditioned on `not cand.get("photo_id")` so the duplicate-click promote+accept retry still answers 200; **that guard exceeds Chris's literal ruling and is flagged for reversion on request.** **One test's intent was deliberately inverted and it is called out rather than slipped in:** `test_rejecting_after_promoting_unlinks_but_keeps_the_photo` existed to *lock* the old 200-and-clear behaviour; it is rewritten and narrowed to the still-true pending case as `test_rejecting_a_promoted_but_still_pending_candidate_unlinks_it`. It is **not** a no-DELETE test, so Decision 4's *"do not change the no-DELETE tests"* does not cover it; `PromoteAddsNoDeleteTests`, `test_hide_is_reversible_and_the_queue_has_no_delete`, the route-count gate and `test_decided_rows_are_not_re_decidable_from_this_screen` are all untouched. Surface still **17 routes, zero DELETE** -- Decision 5 adds an error class and a guard, not a route. Suites: promote **62 -> 78**, routes **67 -> 69**, queue **56**, travel_doc_lab **150**. **Also read and written down rather than left as a suspicion:** `stored_rejected_count` reading 2 against a live `rejected: 1` is not counter drift -- `_refresh_batch_counters()` recomputes and deliberately groups `rejected + duplicate`, while `state_counts` reports the five states separately. **Flagged, not fixed, all outside this lane:** the on-disk archive store has had nothing written since 2026-07-23 22:59 while `turns` kept recording (BUG-209 disabled the client auto-chain and named backend `chat_ws` sole writer, and the travel-doc Lori surface never calls `archive.append_event()`) -- **no data is lost**, and it belongs to the Export lane, epic item 5; Lori's reply leaked its own reasoning into the persisted turn content (*"The narrator is speaking in English, so I will respond in English too"*), which is WO-5 build point 10 and will carry into any memoir built from that turn; and `lv80ToggleNarratorSwitcher is not defined` fires from a `div` `onclick` in `hornelore1.0.html`. **The live re-smoke RAN AND PASSED 2026-07-27 against the restarted stack**, all seven steps Chris specified, driven from the `:8082` operator page against `:8000`: a fresh `local_upload` batch and candidate, `POST /promote` with real PNG bytes returning **200** with the candidate still `pending`, `POST /decision` accepting with that `photo_id` returning **200**, then the same candidate re-decided as `rejected` returning **409** with the guard's own message, and after the refusal the candidate still `accepted`, `state_reason` still `resmoke accept`, `reviewed_by_user_id` and `reviewed_at` unchanged, `photo_id` still attached, and the **whole candidate row byte-identical before and after** the refused call. The promoted `photos` row was read straight out of SQLite read-only and is born `narrator_ready=0` / `date_approved_for_lori=0` / `location_approved_for_lori=0` / `needs_confirmation=1` / `deleted_at=NULL` -- not narrator-facing and not Lori-approved. A pre-flight probe on the previous run's photo-less rejected candidate confirmed the guard was loaded before any of this touched a promoted row. Blast radius `photos` 17 -> 18, `import_batch` 1 -> 3, `import_candidate` 5 -> 7. Both re-smoke batches retired closed + hidden per Decision 4.)

**Previously:** 2026-07-27 (**WO-TRAVEL-DOC-EVIDENCE-REVIEW-QUEUE-01 Decision 4 -- DELETE was raised, analysed, and REFUSED. The no-DELETE contract is now a written decision rather than an unexplained gap.** Chris asked for a delete route mid-build, reversing build point 12 of his own Phase 2/3 order, then refined it to *what would we actually delete, can it be soft and also permanent*. The answer was read out of the codebase rather than recalled: **the soft delete already exists and it is called `hidden`** -- `PATCH /candidates/{id}/hidden` and `PATCH /batches/{id}/hidden`, honoured by `queue_read()` in both forms, reversible via `include_hidden`, already on the screen as Hide / Unhide, pinned by eight tests. Every other mistake is already covered -- junk is `rejected` or `error`, a bad import is a hidden batch, a misfiling is `PATCH /candidates/{id}/trip` -- leaving exactly one gap, **a promotion you regret**, because hiding a candidate does not touch the `photos` row promotion created. That gap is also the trap: a permanent delete of a promoted candidate must refuse with 409 or handle the photo, or it strands an unreferenced, still-unapproved `photos` row -- the identical failure mode already refused for re-deciding. **Chris ruled: do not add DELETE; keep the lane reversible through hidden/unhidden; do not change the no-DELETE tests; do not add DELETE to the sanctioned route surface; do not delete candidates from the UI; do not solve smoke residue with a permanent API route.** Operator cleanup is `hidden` as product behaviour, manual DB work with a backup as Chris-only, and a future guarded purge tool if a maintenance work order ever wants one. **Product rule:** *"Evidence candidates are auditable intake records. Bad or unwanted ones are hidden, rejected, duplicate, or error. They are not deleted from the operator UI."* **Screen wording:** Hide, Unhide, Retire from queue, Remove from review view -- never *Delete*; audited, not assumed, and the Evidence panel already carried zero occurrences of "Delete", so **no code changed**. Surface still **17 routes, zero DELETE**, decorator count and `FlagGateTests._all_routes()` both 17. **Two long-standing open items closed:** the hidden `PHASE5-SMOKE` batches are gone -- read-only query returns `import_batch` 0, `import_candidate` 0, `photos` 16, `trips` 2, `people` 36 -- and the git hygiene gate is clear, Chris having reported 150 / 62 / 67 / 56 green and a clean tree. `.env` now carries `HORNELORE_IMPORT_PROVENANCE=1` (backed up first; gitignored, not in any commit) because his stack starts from a shortcut that an interactive `export` never reaches. **Phase 4 live smoke is the only phase of WO-2 still open.**)

**Previously:** 2026-07-27 (**WO-TRAVEL-DOC-EVIDENCE-REVIEW-QUEUE-01 Phases 3 and 2 -- promotion, then the screen. The Evidence Review Queue is visible and usable in Travel Documenter.** Chris closed all three decisions -- **trip-level placement with no migration 0038**, **`changed`/`skipped` do not become states**, **explicit promote route, option B** -- and opened Phases 2 and 3 together as one vertical slice, the single explicit override of the standing "a phase is a scope wall" rule, for this slice only. **Phase 3 was built first on purpose:** Decision 3 means a candidate has no `photo_id` until something promotes it, so "accept" is unreachable from a screen until the promote route exists. `candidate_promote()` + `POST /api/import-provenance/candidates/{id}/promote` + **62 tests**; the promoted photo is **born not narrator-facing and not Lori-approved**, promotion **does not decide** (the candidate stays `pending` and the existing decision route remains the only thing that writes a state), and promotion is **idempotent**, which is what makes the half-completed promote+accept safe to retry. Surface **16 -> 17 routes, still zero DELETE**; no new dependency. Then the **Evidence** tab in Travel Documenter -- candidates, counts, batch, trip with its date window, filename and external id, `match_reason` printed verbatim, state -- with all seven of Chris's row actions, in-panel drawers and **no native `prompt`/`confirm`/`alert`**. Lab suite **129 -> 150**. **Two collisions were caught before delivery, neither of which would have raised an error:** the CSS prefix `tdl-ev-` was already owned by the per-photo evidence panel (renamed to `tdl-erq-`), and `renderEvidenceRow` was already a module-scope function whose `var` hoist would have silently replaced it (renamed to `renderErqRow`). **One existing assertion was deliberately widened and left to fail first so the change is recorded, not silent:** the sanctioned-endpoint allow-list in `tests/test_travel_doc_lab.py` gained `/api/import-provenance` as a fifth prefix -- still a prefix allow-list, still failing the build on anything else, and the lane it admits is default-off, not narrator-facing and cannot delete. **Active baseline unchanged:** r5h-followup-guard-v1 (78/114, v3=49/72, v2=43/72, mnw=2). `HORNELORE_IMPORT_PROVENANCE` still **default-OFF**, and with it off the tab renders its own configuration panel rather than an error bar. **No narrator-facing change, no Lori change, no Picker, no Takeout, no migration, no schema, no `.env`, no DELETE.** **Phase 4, the live smoke against the serving stack, is the only phase of WO-2 still open** -- and the git hygiene gate is still open with it: Chris has not reported a green test run or a commit for Phase 1 since hitting the missing-`cd` error.)

**Previously:** 2026-07-26 (**WO-TRAVEL-DOC-EVIDENCE-REVIEW-QUEUE-01 Phase 1 -- the queue read, LANDED. WO-2 of the epic is open.** `queue_read()` in `server/code/api/services/import_repository.py`, `GET /api/import-provenance/queue` in the Phase 4 router, and `tests/test_import_provenance_queue.py` -- **56 new tests, all green on the first run**. Baseline **399 -> 455**; the routes suite held at **67**; the import-provenance surface goes **15 -> 16 routes** and still has **zero DELETE** on either lane. No migration, no `main.py` change, no UI file, no flag change, no schema. **`GET /candidates` is the table read; this is the page read** -- the same rows, but each carrying the batch it arrived in and the trip it is filed under, plus the counts behind the page, in one query instead of one-per-batch-per-trip, because a real Takeout import lands hundreds of candidates from a handful of batches. **Four rules the raw list does not hold:** `person_id` is a required argument and an unknown person raises rather than returning `[]`, since *this person has nothing* and *there is no such person* must not share an answer; a candidate inside a hidden batch is out even when its own `hidden` is 0, because otherwise batch-hide is a lie, and each row carries `batch.hidden` so the caller can tell which kind it is; `state_counts` covers the whole filtered queue and deliberately **ignores** the `state` filter while `total` honours it, so filtering the view cannot shrink the map; and `match_reason` round-trips **verbatim**, deep-equal, because 0037 made that column JSON so the queue could show the importer's reasoning rather than a summary of it. Order is oldest-first tiebroken on **`rowid`** -- the Phase 4 lesson applied, since `_now()` has whole-second precision and a whole import lands inside one second. Join columns are spelled out rather than `SELECT b.*` because `import_batch`, `import_candidate` and `trips` all have `id` / `person_id` / `hidden` / `created_at` / `updated_at`; two tests prove no alias leaks and no id is shadowed. Trip comes back with its **date window** on purpose (`start_date` / `end_date`), because the commonest review question is whether a photo's `taken_at` falls inside the trip it is filed under; an unfiled candidate gets `trip: null`, not five nulls. **It is a read** -- four tests assert the database is byte-unchanged across a queue read, that looking at a candidate does not promote it, that the queue materializes no `photos` row, and that it cannot serve a token because intake refuses to store one. **One edit to `tests/test_import_provenance_routes.py` was required, not cosmetic:** `test_route_count_is_the_count_the_gate_test_covers` asserts the route set equals the gate list, so the new route turned that suite red until it was added to `FlagGateTests._all_routes()` -- the guard working. It is listed there **without** its required `person_id` on purpose, so the flag-off answer is 404 rather than the 422 that would name the parameter and confirm the route exists. **THREE DECISIONS ARE CHRIS'S AND TWO OF THEM BLOCK PHASE 2, the screen.** (1) *Placement granularity* -- 0037 has no `proposed_trip_day_id` / `_region_id` / `_stop_id`, so filing is trip-granularity and the route does not invent a finer answer than the schema can hold; **recommendation: stay there and do not write 0038 yet**, because those columns hold what an *importer* proposed and there is no importer until WO-3/WO-4, so building them now means guessing a payload shape before its producer exists. (2) *`changed` / `skipped`* -- neither is a 0037 state and both are refused 400; **recommendation: neither becomes a sixth state** -- `changed` is a diff between proposal and save, which `state_reason` / `reviewed_by_user_id` / `reviewed_at` already record, and making it a state loses the accept; `skipped` collapses into `pending` (undecided) or `rejected` (decided no), and if what is wanted is *ask me again later* that is a defer flag, not a state, because every count and every `decidable_states` check keys on there being exactly five. (3) **NEW, not in the recorded prerequisites, and the largest structural gap:** `candidate_decide(state='accepted')` requires a `photo_id` of a `photos` row that **already exists** and is already owned by the candidate's person, and the repository deliberately never creates one -- that absence is how *intake is not approval* was enforced in Phase 3. **So nothing in the import lane materializes a photo, and the Evidence Review Queue can display, reject, mark duplicate, mark error and file to a trip, but cannot accept anything.** Phase 5's smoke only managed an accept because it pointed at one of Christopher's four pre-existing photos. Three options are written up -- (A) the decision route creates the photo, (B) a separate `POST /candidates/{id}/promote`, (C) the importer creates it at intake born unapproved -- **recommendation (B)**, the only one that does not weaken a guarantee already shipped and tested, with the honest caveat that a candidate holds an `external_id` or a filename and **not bytes**, so (B) likely ships as the route plus the local/manual path now and the Picker/Takeout fetch when those importers exist. If all three stay undecided, Phase 2 ships a queue that can triage but not approve -- still most of the work on a several-hundred-candidate Takeout, but it should be a choice, not a limitation discovered after the screen is built. Spec: `docs/wo/WO-TRAVEL-DOC-EVIDENCE-REVIEW-QUEUE-01_Spec.md`. **Phase 2 is a separate session -- a phase is a scope wall.**)

**Previously:** 2026-07-26 (**WO-TRAVEL-DOC-IMPORT-PROVENANCE-FOUNDATION-01 Phase 5 -- the live smoke, GREEN. The Import Provenance foundation is complete.** 66 assertions, 66 pass, run over HTTP against the **serving** stack from the operator UI origin rather than through the unittest harness, because the two virtualenvs disagree on FastAPI/Starlette (`.venv` 0.136.1/1.0.0 for tests, `.venv-gpu` 0.135.1/0.52.1 for serving) and the serving one is what answers requests. **A real defect was found and fixed inside the phase:** with the flag off, `POST /batches` and `POST /candidates/{id}/decision` answered **422 with the names of their required fields** instead of 404 -- FastAPI validates the body before calling the handler, and the gate was the first line *inside* the handler, so a disabled surface was disclosing both that it exists and what its schema is. The gate is now a **router-level dependency** (`dependencies=[Depends(_require_enabled)]`) with the per-handler calls kept, and two tests were added -- every body-taking route x (`{}`, junk, no body) -> 404, plus an assertion that `router.dependencies` is non-empty so it cannot be demoted back into the handlers. Route suite **65 -> 67**, baseline **397 -> 399**. Flag-off: all 15 routes 404 including on empty and malformed bodies. Flag-on: `/enums` returns exactly the shipped vocabulary -- **`skipped` and `changed` are absent, asserted**; the shipped states are `pending` / `accepted` / `rejected` / `duplicate` / `error`. The fake-data smoke covered three candidate shapes (Picker-style `external_id`, Takeout-style filename, manual), creation defaults, person inherited from the batch, oldest-first order stable inside a second, `match_reason` round-trip, **four token-shaped inputs refused 400 with the token text absent from the body and no row created**, accept against an existing same-person photo, reject, duplicate, hide/unhide preserving decision and reason, counters, close/reopen with a closed batch refusing 409, cross-person 409 with no partial apply, cross-trip refusals, and **no DELETE on either lane**. Read-only (`mode=ro`) fingerprints prove the blast radius: `photos` approval subset still `6c4c6a07e2...`, `trip_photo_links` still `e3b0c44298...`, all four photos still carrying April `updated_at`, `narrator_ready` still `1,1,0,1`, every `*_approved_for_lori` still 0, and photos/trips/links still 4/0/0 -- **no route created a photo or touched an approval flag**, and zero rows in either import table contain token-shaped text. Two false alarms were run to ground rather than reported as defects: alphabetized keys are `json.dumps(sort_keys=True)`, and `stored_rejected_count` 2 vs live `rejected` 1 is `rejected_count` counting duplicates by design. Deferred out of scope: the `.venv` / `.venv-gpu` dependency drift (a later harness/environment work order, per Chris) and the pre-existing `tests/` discovery contamination. Two hidden `PHASE5-SMOKE` batches and four candidates were left in place -- there is no DELETE route and removing them is Chris's action to run. Transcript: `docs/reports/phase5_import_provenance_smoke.console.txt`.)

**Previously:** 2026-07-26 (**WO-TRAVEL-DOC-IMPORT-PROVENANCE-FOUNDATION-01 Phase 1.1 + Phase 2** -- the identity side branch is closed by deletion and the import landing zone exists. **Phase 1.1 ran live against `/mnt/c/hornelore_data/db/hornelore.sqlite3` on 2026-07-26 and returned `VERIFY: CLEAN`, exit 0**, reproducing the rehearsal on every number; `people` is 37 -> 36 and one Christopher row remains. **Migration 0037 has since applied to the live database** (stack start, 2026-07-26 02:54:47 -- before the 03:00 wipe, so `hard_delete_person` cascaded *through* the new `photos.narrator_id` FK rather than around it) **and Phase 2 is verified at both layers**: schema (both FKs CASCADE, both new tables, `foreign_key_check` empty, `integrity_check ok`, forbidden columns absent, row counts exact) and operator path (13 photo links / 13 distinct photos / 0 null, the link -> photo -> person chain terminating on the surviving Christopher, thumbnails and images served, seven downstream trip endpoints 200, clean console, and **exactly one Christopher in the narrator picker**). **Phase 3 then landed the import repository** -- `server/code/api/services/import_repository.py` + 50 green tests -- putting the three rules a schema cannot state into procedure: candidates copy their batch's person and nothing may reach another person's trip or photo; every operator string is scanned for token shapes before it is written, while real Google Photos media ids and Takeout filenames still pass; acceptance requires an existing photo the person already owns, and the module neither creates photos rows nor sets approval flags. No `DELETE FROM`, no `DROP TABLE`. Baseline 279 -> 329. **Phase 4 then landed the minimal verification surface** -- `server/code/api/routers/import_provenance.py` + 65 green tests + registration in `main.py`: 15 routes under `/api/import-provenance`, gated default-OFF behind `HORNELORE_IMPORT_PROVENANCE=1` and answering 404 rather than 403 when off, with no database logic of its own, person/trip/photo boundaries re-checked at the route layer, every repository exception mapped to a deliberate 4xx so only real schema drift reaches 500, a refused token returning 400 without echoing the token, and no DELETE anywhere. The 0037 column names win over the Epic Plan's working names, and the plan's `needs_review` / `changed` / `skipped` are refused with 400 rather than quietly mapped. Building it surfaced a Phase 3 defect -- whole-second `created_at` made both list queries degrade to uuid order inside a second -- now tiebreaking on `rowid` and locked by three tests in the Phase 3 suite. Baseline 329 -> 397. Nothing is blocked; **Phase 5, the live smoke, is next.** **Chris cut the scope himself:** *too much work on this just delete the other Christopher and move on to what is supposed to be the real work.* Both open decisions are decided -- delete `e7fdb578` (`Christopher`), keep `a4b2f07a` (`Christopher Todd Horne`), no merge, no canonical pointer, no `merged_into`, no person-merge tooling; and Option B for `import_candidate`. **Phase 1.1 shipped `scripts/wipe_narrator_identity.py`**, a single-target allowlisted deleter whose `ALLOWED_TARGETS` holds exactly one id and whose `PROTECTED_IDS` holds `a4b2f07a`, Kent, Janice and Melanie. It **deletes by id and treats the display name as an abort-only condition, never a selector**; `--commit` requires `--i-acknowledge` plus a `--backup` that passes `integrity_check` and still contains the target; and the delete delegates to the application's own `hard_delete_person()` rather than to hand-written SQL. **Rehearsed four ways on throwaway copies of the live database** -- dry run, commit, verify-only, preflight re-run -- exit 0 at every step: **21 dependent rows destroyed** (photos 6, bio_facts 9, consent_attestations 2, profiles 1, interview_sessions 1, interview_projections 1, bio_builder_questionnaires 1), residue **0** across all 29 person-scoped pairs, orphan sweep CLEAN, `PRAGMA foreign_key_check` **empty**, and all four protected narrators intact with **unchanged** dependent-row totals (`a4b2f07a` 121, Janice 51, Kent 41, Melanie 5). **The honest caveat:** `audit_identity_preflight.py` still exits 1 after a perfect wipe because its verdict is global and five duplicate-name harness clusters remain (Walt x7, Trip Canary x5 and x4, Kent-harness, Esteban x3) -- all explicitly forbidden work. The preflight was **deliberately not modified**; `wipe_narrator_identity.py --verify-only` (exit 0) is the Phase 1.1 gate. **Phase 2 landed migration 0037**: `photos.narrator_id -> people(id)` and `trip_photo_links.photo_id -> photos(id)` both CASCADE, built on 0034's two-phase pattern with the orphan DELETE run FKs-ON so cascades fire, plus `import_batch` and `import_candidate` with `hidden`/`hidden_at`. **`import_candidate` has no `narrator_ready`, no `include_in_memoir` and no token columns -- the absence is how *intake is not approval* is enforced.** 37 new tests green; 539 green across the 20 affected suites; migration verified idempotent against two copies of the real database. **The destructive run is Chris's to make** -- the deliverable is tooling plus a WSL run block, never an agent-side write to `C:\hornelore_data`. Full transcript at `docs/reports/christopher_wipe_rehearsal_20260726.console.txt`.)

**Previously:** 2026-07-26 (**WO-TRAVEL-DOC-IMPORT-PROVENANCE-FOUNDATION-01 Phase 0 + Phase 1** -- recon and a read-only identity pre-flight for work order 1 of the post-unification epic. Recon, one script and docs; **no schema, no migration, no API, no test, no flag, no UI, not one line of application code**, and the 279-test baseline is unmoved. **Phase 0 found a fault line under the epic's foundation.** The Epic Plan's `import_candidate` carries both `person_id` and `photo_id`, but `photos.narrator_id` has **no foreign key** while `trips.person_id` references `people(id)`; six code sites equate the two by convention, and the load-bearing one returns an **empty list rather than an error** when they disagree, so photo clustering against a mismatched id reports success with zero matches. `trip_photo_links.photo_id` has no `REFERENCES` either and is scoped by trip rather than by owner, and `people` is flat with no merge tooling anywhere in the repo. **Phase 1 shipped `scripts/audit_identity_preflight.py` and -- unexpectedly -- ran it.** `C:\hornelore_data` is now a connected folder, so the live database was copied into the container (md5-verified both directions), opened `mode=ro` and audited there; the live file was never opened and nothing was written to it. **Verdict: IDENTITY SPLIT DETECTED.** Both Christopher rows carry data -- `a4b2f07a` has 13 photos, 6 photo_sessions, 3 story_candidates and both trips; `e7fdb578` has 6 photos and 9 bio_facts -- and the split is widening the wrong way, since all six of `e7fdb578`'s photos arrived on 2026-07-25 during the Phase 5/6 smoke while `a4b2f07a`, which owns every trip, has received none since 2026-07-13. The crossing-link and empty-cluster queries are clean today only because both trips and all thirteen linked photos sit on the same id; one trip created under `e7fdb578` fires the failure. Every orphan count is zero, so adding the missing foreign keys is measured-safe against today's rows -- but a foreign key cannot fix a reference pointing at the wrong one of two valid rows, so the Christopher question survives it and is promoted to its own phase. **Two decisions belong to Chris and both block Phase 2.** Full output at `docs/reports/identity_preflight_20260726.console.txt`.)

**Previously:** 2026-07-26 (**WO-DOC-RECONCILE-01** -- documentation reconciliation after the unification arc. Docs only; **no code, no tests, no config** -- not one file outside `README.md`, the unification spec, this checklist and `CLAUDE.md` was touched, and the 279-test baseline is unmoved. **The finding that prompted it:** `README.md` is the repository's front door and it still described the world as it stood on 2026-07-24 -- before Phases 1 through 6 landed. It named Travel Doc Lab depth as the active lane, carried no mention of `WO-TRAVEL-DOC-UNIFY-01` anywhere including the Work Orders table, and listed as an open MEDIUM issue a native `window.prompt()` in the evidence panel that the in-panel editor had already replaced -- verified here at **zero** executable `prompt`/`confirm`/`alert` calls in `ui/js/travel-doc-lab.js`, the six remaining occurrences all comments, and pinned by the surface gates. A second README issue was true but mis-scoped: the `travel-documenter.js` double-send defect now sits on a retired module unreachable from the shell, which a reader could not tell from the entry. Both corrected in place rather than deleted, because a known issue that was fixed and a known issue that moved off the operator path are different facts. **In the spec:** Phases 4, 5 and 6 carried full 'what landed' sections and a close-out but no status marker in the Phases index, so a reader skimming the index saw three open phases -- now `LANDED`/`CLOSED`; and Phase 6 Finding 1, the harnesses that could not run on this machine, is marked resolved by WO-HARNESS-DEPS-01. **Backlog moved from seven items to eight.** Item 5, the smoke-photo residue, is taken as *documented test rows* -- the second option it always offered -- because the live database lives outside the repository at `/mnt/c/hornelore_data/db/hornelore.sqlite3` per `.env`, which neither the container nor the device bridge can reach, so no agent-side pass can identify those six rows let alone clear them. New item 8 promotes Phase 6 Finding 4 -- `e7fdb578` is `Christopher` and `a4b2f07a` is `Christopher Todd Horne`, two person records that read as one human in every picker -- from curiosity to backlog, because Import Provenance Foundation writes provenance against a `person_id` and a split import across two narrators fails silently with both halves internally consistent. **Still open and needing Chris, not an agent:** whether `bio_suggestions` belongs in the force-delete gate at all. The evidence is now on the table -- `_TRIP_DEPENDENT_TABLES` already excludes `trip_themes` on the stated grounds that the gate keys on evidence lanes and themes are lightweight labels that must not break empty-trip semantics, and `trip_bio_suggestions` fails that same test on every count: written by `trip_timeline_bridge.sync_trip_to_life_record` on every create, one row, `status='suggested'`, derived from the trip rather than contributed to it. It is a backend change with a test consequence, so it is recorded, not taken.)

**Previously:** 2026-07-26 (**WO-HARNESS-DEPS-01** -- the Node Playwright toolchain is declared, pinned and documented, so both liveness harnesses now run where the code lives instead of container-only. Config and docs only; no `ui/`, `api/`, `server/`, schema, flag or test file changed and no test count moved. **The diagnosis was not a missing dependency.** `package.json` has declared `playwright` and `playwright-core` the whole time and is byte-identical on both copies; `.gitignore` line 61 excludes `node_modules/`, correctly, so the packages were never going to arrive with `git pull` and nothing in git could restore them once they were lost. **The browsers were already on disk and matched exactly:** `~/.cache/ms-playwright` holds `chromium-1208 chromium_headless_shell-1208 ffmpeg-1011 firefox-1509 webkit-2248`, and pulling `browsers.json` out of each candidate release maps that five-for-five onto Playwright **1.58.2**, the exact version the manifest names. Somebody did run `npx playwright install` properly once: the half that lives outside the repo survived and the half that lives inside it did not. **The caret was the active hazard** -- `^1.58.2` resolves to 1.62.0 today, which wants `chromium-1234`, so a plain `npm install` would have pulled a second ~500 MB browser set and stranded the one already there. All three Playwright entries are pinned exact, `@playwright/test` included because a mismatched one drags its own release in; tailwind's carets are deliberately untouched as out of scope, with `package-lock.json` committed to hold the rest of the tree still. **The root cause was documentation, not configuration:** the string `npm install` appeared in zero `.md` files in this repo and `node scripts/ui/` in zero, and `scripts/ui/README.md` documented one of that folder's five harnesses -- describing the Python toolchain that is *not* installed, in detail, while never mentioning either `.js` file. That README now covers both toolchains, both liveness harnesses, the exact-pin rationale, the `NODE_PATH` and `PLAYWRIGHT_CHROMIUM_PATH` overrides, and the WSL home-directory install that avoids writing `node_modules/` across `/mnt/c`. Also narrowed the stale Phase 4 plan line in the unification spec, which still read as though the retired Documenter and the dev harness were going to be deleted, per a review note.)

**Previously:** 2026-07-25 (**WO-TRAVEL-DOC-UNIFY-01 CLOSED** -- Phase 6 live smoke green on all thirty-seven steps with zero defects, so the work order closes rather than continuing. Live verification and docs only; **not one line of code was written** and no `ui/`, `api/`, `server/`, schema, flag or test file changed. Ref-based clicking was abandoned in the first few steps because refs churn on every re-render inside the workspace, so interaction ran through a page-side helper resolving elements by text and structure at call time, and every persistence claim was read back from `/api/trips/{id}/tree` server-side rather than off the DOM. Dialogs, listeners, sockets, console errors and non-2xx traffic came from an instrumentation spy installed before the mount -- including a `WebSocket.prototype.send` wrap, so the Lori modal scope was proved from the **actual outbound `start_turn` frame** rather than the on-screen label. The Travel Doc tab mounts the unified workspace directly with no toggle and no retired UI, and the shell's rendered text holds zero occurrences of *legacy*, *production Travel Doc*, *UI Lab*, *experimental*, *lab-only*, *removable* or *documenter*. Two regions and nine stops built, edited at all four levels, reordered, insert-before/after on a top-level stop and on a substop, a cross-region move through the edit drawer, substop arrows confined to their sibling group, and order surviving a full refresh. Upload `FileList` node identity held across a scope retarget; cluster stated its whole-library caveat. **The lifecycle census across three leave/re-enter cycles is the load-bearing proof:** read as deltas, every entry opened exactly what the previous exit closed -- never more than one live channel or one live keydown listener, and the Lori socket opened exactly once in the whole run. Zero native dialogs, zero console errors. **The force-delete gate rendered all ten impact lanes with `bio_suggestions` HOT at 1**, which is the Phase 3A finding reproducing live: production's nine-lane grid would have shown that evidence nowhere while the backend refused the delete. Arming was tested against a fifteen-case matrix -- case folding, prefix, suffix, one short and one long all refused; only the exact title, the exact id, or those with surrounding whitespace armed -- and the forced delete completed, with the narrator's trip list confirmed empty from the API. The dev harness still mounts; the retired Documenter is still served and still unreachable from the shell, its six remaining textual references all inside comments. 279 tests green; both Playwright harnesses PASS (14/14, 23/23). **Five findings, none a defect**, the most actionable being that **the two liveness harnesses cannot run on Chris's machine at all** -- `node_modules` is absent from the working tree, so they were run in the container after md5 proved the eight relevant files byte-identical, which is sound but leaves part of the proof chain unreproducible where the code lives. Seven-item post-unification backlog carried forward at the foot of the spec.)

**Previously:** 2026-07-25 (WO-TRAVEL-DOC-UNIFY-01 Phase 5 LANDED -- one coherent Travel Doc test surface. Tests and docs only; no `ui/`, no `server/`, no backend, no API, no schema, no flag change, nothing deleted. Added `tests/travel_doc_surfaces.py`, the single map of the eleven Travel Doc surfaces and which are on the operator path, and `tests/test_travel_doc_surface_gates.py`, which holds the cross-cutting doctrine that used to be asserted once per originating work order. Six suites retargeted onto the map -- thirty private `Path` literals and eleven private copies of the comment stripper and the shell-region extractors gone -- with **no test body moved**, so no coverage shifted. **Chris's premise was half true and that matters:** Phase 4 had already narrowed most of the assertions this phase expected to find stale. Exactly one stale thing existed and it was a *name*, not an assertion: `test_lab_does_not_import_production_module` was renamed `test_the_unified_module_never_loads_the_retired_one` with its assertion untouched. One real duplicate was narrowed so the module-wide `prodTravelDocUrl` claim has a single owner. The retired `travel-documenter.js` still carries exactly two native confirm calls and still pre-stringifies its bodies; that is **pinned at two** rather than repaired (out of scope) or deleted from the tests (which would hide it), and a separate gate fails the build if the module is ever re-attached to the operator path. `scripts/ui/run_travel_doc_mount_liveness.js` had been in no surface inventory at all and is now in the map and in the dialog scan. 279 tests green (was 262); twelve mutations, twelve killed -- one of which exposed a real hole in a brand-new gate, where matching the bare name `lvTravelDocMount` also matched the `typeof` guard above the call, so the gate would have passed a shell that checked whether it could mount and then never did. **Deliberate deviation from the Phase 0 plan:** the two suites were NOT concatenated into one `tests/test_travel_doc.py`. They guard two different modules that both still exist; the fold would produce a 125K file whose name claims a boundary it cannot hold and would move every line number in both. Coherence came from a shared map plus one doctrine file instead. The literal fold remains available as a mechanical follow-up commit if Chris wants it.)

**Previously:** 2026-07-25 (WO-TRAVEL-DOC-UNIFY-01 Phase 4 LANDED and smoke-accepted -- the legacy fallback is retired and the unified Travel Doc is the operator's only Travel Doc surface. Removal phase, front-end only; **no backend, no API, no schema, no flag change, and no endpoint or module deleted**. Out of the shell: the surface toggle and its whole support cast (`_LV_TD_SURFACE_KEY`, `_lvTravelDocSurface()`, `_lvTravelDocDestroyLegacy()`, `_lvTravelDocPaintSurfaceChrome()`, `window.lvTravelDocSetSurface`), the two legacy asset tags, the switch row, the `#lvTravelDocHost` div, the `.lv-td-surface-*` / `.lv-td-host-off` / `body.lv-td-focus` rules, and the route board's `prodTravelDocUrl()` deep-link foot-note. **Unmounting a surface is not deleting a module:** the old module, its stylesheet and its standalone page all stay on disk and that page still mounts it, pinned by a new `LegacyModuleStillExistsTest`. The dev harness `ui/travel-doc-lab.html` was **kept and labelled `DEV-ONLY`** rather than deleted, because it is the only caller of `lvTravelDocMount()` outside the shell and the only one that exercises the non-shell identity branch -- delete it and that branch rots untested. **Five on-path fixes the order did not name:** the `lv80SwitchPerson` teardown fallback was nulling the retired Documenter's marker and had become a silent no-op; the day photo picker's empty state was ungated, operator-visible, and pointed at a production Travel Doc that no longer exists for a capability Phase 3C moved onto this surface's own toolbar; the evaluation checklist still called itself part of a removable lab; the route-board deep link and its CSS rule; and three `body.lv-td-focus` rules that went unreachable when the Documenter left the shell path. **A gate that looked obvious and was wrong:** a file-wide `assertNotIn("legacy")` on the shell is invalid -- 19 unrelated occurrences live there -- so it is scoped to the Travel Doc panel and the `app.js` mount block, while a stronger gate became possible instead, because the shell now contains zero **raw** occurrences of the old module's name and can be asserted un-stripped. **Tests rewritten, not deleted:** five whose boundary inverted came out, thirteen went in, and seven more across two suites were narrowed so each keeps the half that still proves something and flips only the half Phase 4 retired -- absences are now *asserted*, so re-adding a `<script>` tag fails the build. 262 tests green (was 255); eight mutants, eight killed; the shell-mount liveness harness rewritten for one surface with a new `singleSurface` probe and its now-unrunnable negative controls marked as such rather than quietly dropped. Live smoke green on all fifteen of Chris's steps, including a three-round mount census showing opens 3 / closes 2, keydown +3 / -3, zero net sockets and one `.tdl-root` at every sample. Phase 5 test consolidation and the `travel-doc-lab.js` -> `travel-doc.js` rename are explicitly out of scope and stay parked.)

**Previously:** 2026-07-25 (WO-TRAVEL-DOC-UNIFY-01 Phase 3D LANDED and smoke-accepted — route order and route-row ergonomics now exist in the unified Travel Doc workspace, which closes the last surface where the legacy Documenter still held a workflow advantage. Production's board was reviewed against the port and had exactly four affordances the port lacked — region reorder, stop reorder, evidence badges on route rows, and a selectable region row — and all four are in; **nothing was retired**, and production has no drag-and-drop on either tile (grep-verified). **The load-bearing decision is that a stop move sends two ids, not a permutation:** production posts the whole sibling list to `/stops/reorder`, which 400s unless that list is exactly the current sibling group — a request precisely as stale as the tree it was built from, so any concurrent insert or delete refuses a move the operator can see is legal. The port calls `/stops/{id}/move` with `before_stop_id`/`after_stop_id` and lets the backend re-derive the group; `/move` **is** the existing API shape and is what production already uses for cross-region moves. Regions still post a permutation because `/regions/reorder` is the only door, so a refusal is surfaced in-panel and the bundle reloaded rather than dead-ending. A reorder never reparents: `region_id` and `parent_trip_stop_id` are echoed back unchanged, so a substop moves among its own siblings only. `routeBusy` serialises moves and disables every arrow while one is in flight; `routeError` is the in-board failure surface; both clear with the trip and with a deleted row. Making region rows selectable revived Phase 3C's `defaultScopeKey()` region arm, which could never fire because `st.routeSel` was written in one place and only ever as a stop. Evidence badges cost zero fetches. **The live smoke found a defect nineteen source-scanning gates could not see:** `api()` owns the request encoding and stringifies `opts.body` itself, and both new movers pre-stringified theirs, so every arrow press on the board answered 422 - a defect that lives in the relationship between a call site and `api()` rather than inside either one, which is why no static gate could reach it and why the smoke stays a gate rather than a formality. Fixed with a raw object at both call sites plus two new gates, one banning `body: JSON.stringify` file-wide while pinning that the encoding still happens exactly once inside `api()`. 255 tests green (was 236); nineteen gates mutation-tested rather than trusted, nineteen mutants killed. **A pre-existing red found and repaired:** Phase 2 commit `e9b792c` widened the `lv80SwitchPerson` block in `hornelore1.0.html` but the matching test-window widening was never committed, so the device baseline had really been 235/236 since Phase 2 and container-only runs were masking it — that repair ships as its own commit. Front-end only; no backend, no API, no schema, no flag change. **Live smoke green on all eleven of Chris's steps**, verified server-side through `/api/trips/{id}/tree` rather than off the DOM: region and stop reorder both directions, substops travelling with their parent, insert before/after on a top-level stop and on a substop, cross-region move, cycle rejection at both the UI and the API layer, order surviving a full page refresh, counts refreshing, both Phase 3B delete ladders, the Phase 3C `FileList` node identity across a scope retarget, the Phase 3A force-delete gate arming only on the exact title, a clean mount/socket census across three legacy round trips, and zero native dialogs and zero console errors. Phase 4 is a separate session.)

**Previously:** 2026-07-25 (WO-TRAVEL-DOC-UNIFY-01 Phase 3C CODE-LANDED — photo upload, source upload and photo clustering now exist in the unified Travel Doc workspace, so the legacy Documenter is no longer needed for intake. Built, not ported: the lab had zero `FormData` and zero file inputs, so `api()` grew a `FormData` branch ahead of its JSON branch and an intake module of roughly four hundred lines landed behind three new `st` fields. **Scope is an explicit drawer selection, not production's ambient `editorScope()` read** — production can silently retarget an upload between choosing a file and pressing Upload; the port names the destination in prose before the request. The load-bearing constraint is `FileList`, which script cannot write, so the drawer never repaints between choosing files and uploading, and a test pins that absence. Intake is not approval: no `include_in_memoir` and no `trip_day_id` is ever sent, day attach stays separate, evidence lanes stay hide-only, no new DELETE. **A same-scope bug found and fixed:** three suites used a string-blind comment stripper and went blind on `files.accept = "image/*"` — migrated to the repo's existing `strip_js_comments`, assertions unchanged, which is why that migration is its own commit landing before the Phase 3C code. 236 tests green (was 220); the sixteen new gates were mutation-tested rather than trusted; all twelve of Chris's live-smoke steps green across all three upload scopes, with no-auto-promotion verified server-side and zero native dialogs recorded by a spy. Front-end only; no backend, no API, no schema, no flag change. Phase 3D is a separate session.)

**Previously:** 2026-07-25 (WO-TRAVEL-DOC-UNIFY-01 Phase 3B CODE-LANDED — trip create/edit and region/stop CRUD now exist in the unified Travel Doc workspace. Four editor drawers plus an in-panel delete-review ladder; the empty-state copy bug is fixed; the `current` tab is renamed `trip` behind a `setTab` shim; insert-at-position (`insertContext`/`insertHint`) is preserved with a stale-context guard production never had. **Region delete fixes a production defect rather than porting it:** the backend 409s on a non-empty region unless forced, and production neither forces nor handles the 409, so its `window.confirm()` delete silently dead-ends — the port tries unforced first and escalates to a second review stage quoting the server verbatim. Zero native dialogs, proved by a spy across every destructive flow, not by inspection. Also wired `st.daysWarning`, which the Trip Plan tab read but nothing ever set. 220 tests green (was 205); all thirteen of Chris's live-smoke steps green, including a functional mount/socket/listener census (1 socket live, 1 channel live, all eight bundle endpoints fetched exactly once after a broadcast). Front-end only; no backend, no API, no schema, no flag change. Phase 3C is a separate session.)

**Previously:** 2026-07-25 (WO-TRAVEL-DOC-UNIFY-01 Phase 3A CODE-LANDED — the trip force-delete impact-review gate is now in the unified Travel Doc workspace: normal delete first, a 409 opens an in-panel review reading `e.body.detail`, force delete arms only on the exact trip title or trip id, and no native `confirm`/`prompt`/`alert` is used anywhere in the flow. The port renders **ten** impact lanes; production renders nine and omits `bio_suggestions`, which every trip is born with — so every trip is force-delete-only from birth and production shows an all-zero grid next to a refused delete. 205 tests green (was 192), and all ten of Chris's live-smoke steps passed on the running stack. Front-end only; no backend, no API, no schema, no flag change. Phase 3B is a separate session.)

**Previously:** 2026-07-25 (WO-TRAVEL-DOC-UNIFY-01 Phase 2 CODE-LANDED — the unified Travel Doc workspace now mounts directly in the shell's Travel Doc tab and is the default operator surface; the legacy Documenter stays reachable behind a temporary surface switch. Lab CSS scoped to `.tdl-root`, Lab branding gone from the operator path, exactly one mount ever live. 192 tests green plus a second headless proof `scripts/ui/run_travel_doc_shell_mount_liveness.js` (22 checks) with four negative controls actually run. Front-end only; no backend, no API, no schema, no flag change. Phase 3 is next and is a separate session.)
**Previously:** 2026-07-24 (WO-TRAVEL-DOC-UNIFY-01 Phase 1.1 CODE-LANDED — the mount can no longer be repainted after `destroy()`. Six guards at the file's six async choke points, plus the `document`-level keydown listener now unbound at teardown. 154 tests green; stale-callback behaviour proved in a real browser by `scripts/ui/run_travel_doc_mount_liveness.js`, with two negative controls confirmed red.)
**Previously:** 2026-07-23 (live-verification pass on the running stack — migrations 0034/0035, trip API baseline, Travel Doc smoke, and response-guard health all GREEN; INC-2026-07-09 response-guard outage closed; camera-consent ambush + extractor guards + trip-create day-count + public-lookup wording fixed. See README "Status as of 2026-07-23" and the CLAUDE.md 2026-07-14 changelog entry.)
**Earlier:** 2026-07-11 (doc-consistency pass — split "code landed" vs "flag live" vs "formal verification" statuses per Chris's audit; 2026-07-11 HIGH repo-review batch closed via commits ebe64af / cf62c49 / round-2 / round-3), 2026-07-02 (code-vs-checklist adjudication — ALL six build-sequence WOs verified LANDED in-tree; trip-lane conversation layer at 62/72→GREEN pending harness re-verify; Spanish-detection overfire class fixed), 2026-06-16 (post Phase 3+4+5+6+7.5 of QUESTIONNAIRE-BIO-FACTS-MIGRATE), 2026-06-14 (post-universal-pivot)

---

## Status legend (locked 2026-07-11)

Three distinct dimensions — do NOT conflate them:

- **CODE-LANDED** — the implementation exists in-tree, has unit-test coverage, and AST-parses clean. Verifiable by reading the code.
- **FLAG-LIVE** — the corresponding `HORNELORE_*` env flag is set to `1` in the running `.env`, so the code path actually fires in real sessions. Verifiable by reading `.env` + api.log startup line.
- **FORMAL-VERIFIED** — a written verification report exists in `docs/reports/` demonstrating the feature works under a target scenario (red-team pack, canary session, harness eval, live-narrator transcript). Verifiable by opening the report.

Something can be code-landed but not flag-live (behind a default-off flag). Something can be code-landed AND flag-live but not formal-verified (running in the wild but no written proof yet). "LANDED" without qualifier historically meant code-landed; from 2026-07-11 forward the word alone is deprecated — use one of the three explicit terms.
**Supersedes:** Pre-pivot checklist (archived at `docs/archive/handoffs-pre-pivot/MASTER_WORK_ORDER_CHECKLIST.md`)
**Read first:** [`docs/architecture/HORNELORE-UNIVERSAL-PIVOT-STRATEGY.md`](docs/architecture/HORNELORE-UNIVERSAL-PIVOT-STRATEGY.md), [`docs/architecture/LORI-RUNTIME-ARCHITECTURE.md`](docs/architecture/LORI-RUNTIME-ARCHITECTURE.md)

---

## Posture

Hornelore is Lorevox. The Horne family is tenant zero, not a special case in the architecture. Every WO from this date forward is written against the universal assumption: Lori must work for narrators she has never met. Pre-pivot WOs (114 specs, locked-narrator framing) are archived at `docs/archive/workorders-pre-pivot/` for traceability — they are not the active source of truth.

Interview default is moving from questionnaire-first to **oral-history-as-default**. Structured styles become operator-selectable overrides.

---

## Build sequence — one Cowork session per WO, in order

Each WO gets its own Cowork session with the WO spec as the brief. Do NOT start more than one at a time.

| # | WO | File | Closes / Introduces |
|---|---|---|---|
| 1 | SAFETY-LLM-CLASSIFIER | [`docs/wo/WO-LORI-SAFETY-LLM-CLASSIFIER-01_Spec.md`](docs/wo/WO-LORI-SAFETY-LLM-CLASSIFIER-01_Spec.md) | Closes Gate 5 (soft-trigger safety) — **LANDED (verified in-tree 2026-07-02):** `safety_classifier.py` 3-dim taxonomy + LLM layer + confidence floor + 44 tests; gated `HORNELORE_SAFETY_LLM_LAYER=0` |
| 2 | SOFTENED-MODE-PERSISTENCE | [`docs/wo/WO-LORI-SOFTENED-MODE-PERSISTENCE-01_Spec.md`](docs/wo/WO-LORI-SOFTENED-MODE-PERSISTENCE-01_Spec.md) | Closes Gate 6 (post-safety recovery) — **LANDED (verified in-tree 2026-07-02):** `lori_softened_response.py` 3-state machine + per-trigger caps (30/35/50) + 32 tests; gated `HORNELORE_SOFTENED_RESPONSE=0` |
| — | **Flag already flipped** | `HORNELORE_SAFETY_LLM_LAYER=1` + `HORNELORE_SOFTENED_RESPONSE=1` | ✅ **FLAG-LIVE 2026-07-05** (both `.env` values set). Remaining item is FORMAL-VERIFIED — red-team pack + softened-persistence harness against the live flag state. NOT a code-work blocker. |
| 3 | PHASE-9-DISCLOSURE-UPDATE | [`docs/wo/WO-LORI-PHASE-9-DISCLOSURE-UPDATE-01_Spec.md`](docs/wo/WO-LORI-PHASE-9-DISCLOSURE-UPDATE-01_Spec.md) | Consent disclosure edits — **LANDED (verified in-tree 2026-07-02):** three-tier disclosure + style descriptions in `docs/runbooks/SAFETY_OPERATOR_RUNBOOK.md` (docs-only WO) |
| 4 | STORY-FIRST-PHASE-1 | [`docs/wo/WO-LORI-STORY-FIRST-PHASE-1-01_Spec.md`](docs/wo/WO-LORI-STORY-FIRST-PHASE-1-01_Spec.md) | Oral-history behavior engine — **LANDED (verified in-tree 2026-07-02):** `reflection_grounding.py` + `story_momentum.py` + `thread_bank.py` + `question_hierarchy.py` + chat_ws wiring + 66 tests; REPORT-ONLY behind `HORNELORE_STORY_FIRST_PHASE_1=0` |
| 5 | ORAL-HISTORY-DEFAULT | [`docs/wo/WO-LORI-ORAL-HISTORY-DEFAULT-01_Spec.md`](docs/wo/WO-LORI-ORAL-HISTORY-DEFAULT-01_Spec.md) | Introduces `oral_history` style + makes it default — **LANDED AND ACTIVE (verified in-tree 2026-07-02):** style @ 90-word cap, system default in comm-control signatures, picker + 29 tests |
| 6 | BIO-BUILDER-UNIVERSAL | [`docs/wo/WO-LORI-BIO-BUILDER-UNIVERSAL-01_Spec.md`](docs/wo/WO-LORI-BIO-BUILDER-UNIVERSAL-01_Spec.md) | Four-tier Bio Builder — **LANDED (verified in-tree 2026-07-02):** `bio_fact_router.py` + `document_authority.py` + `bio_anchored_asker.py` + `bio_gap_map.py` + 3 creep defenses + 66 tests; gated `HORNELORE_BIO_*=0` |
| 6a | OPERATOR-NEW-NARRATOR-INTAKE-FORM-01 | (root WO, 2026-06-15) | 9-section intake form + Phase 2B orchestrator + Phase 2C modal — **LANDED** |
| 6b | QUESTIONNAIRE-BIO-FACTS-MIGRATE-01 | [`docs/wo/WO-BIO-QUESTIONNAIRE-BIO-FACTS-MIGRATE-01_Spec.md`](docs/wo/WO-BIO-QUESTIONNAIRE-BIO-FACTS-MIGRATE-01_Spec.md) | Phase 1 read swap + Phase 2 FE badges + Phase 3 write fan-out + Phase 4 primary_career bug + Phase 5 23-test pack + Phase 6 self-review + Phase 7.5 backfill readiness — **LANDED 2026-06-16**, Phase 7 live verify pending |
| 7 | MEMORY-EXERCISE-IMPLEMENTATION | (not yet drafted) | Specced in [`docs/architecture/MEMORY-EXERCISE-DECISION.md`](docs/architecture/MEMORY-EXERCISE-DECISION.md) |

---

## Superseded / history

The two docs below land for design-history traceability. Do NOT build from them.

- [`docs/wo/superseded/WO-LORI-SAFETY-PAST-TENSE-ACKNOWLEDGE-01_Spec.md`](docs/wo/superseded/WO-LORI-SAFETY-PAST-TENSE-ACKNOWLEDGE-01_Spec.md) — merged into #1 SAFETY-LLM-CLASSIFIER
- [`docs/wo/superseded/PRE-BUILD-ADDITIONS.md`](docs/wo/superseded/PRE-BUILD-ADDITIONS.md) — changelog of edits already folded into the strategy + WO specs
- [`docs/wo/superseded/REDESIGN-DOC-HEADER-TO-PREPEND.md`](docs/wo/superseded/REDESIGN-DOC-HEADER-TO-PREPEND.md) — header block for `WO-INTERVIEW-PROCESS-REDESIGN-01`; target doesn't exist in repo, header was not applied

---

## Parent-session readiness gates (locked checklist)

Inherited from pre-pivot work. Pre-pivot evidence in `docs/archive/`; post-pivot evidence as it lands.

| Gate | Code | Flag | Formal | Lane / note |
|------|------|------|--------|-------------|
| 1. DB lock fix | ✅ landed | ✅ live | ✅ verified | pre-pivot |
| 2. Atomicity discipline | ✅ landed | ✅ live | ✅ verified | pre-pivot |
| 3. Story preservation | ✅ landed | ✅ live | ✅ verified | pre-pivot |
| 4. Safety acute path | ✅ landed | ✅ live | ✅ verified | pre-pivot |
| 5. Safety soft-trigger | ✅ landed 2026-07-02 (`safety_classifier.py`, 44 tests) | ✅ live 2026-07-05 (`HORNELORE_SAFETY_LLM_LAYER=1` in `.env`) | 🟡 pending | Remaining: red-team pack on live flags; report → `docs/reports/`. Also `SafetyResult` NameError in `chat_ws.py` closed 2026-07-11 via `ebe64af` — no more silent no-op on trigger. |
| 6. Post-safety recovery | ✅ landed 2026-07-02 (`lori_softened_response.py`, 32 tests) | ✅ live 2026-07-05 (`HORNELORE_SOFTENED_RESPONSE=1` in `.env`) | 🟡 pending | Remaining: softened-persistence harness evidence on live flags (lockstep with Gate 5). |
| 7. Truth-pipeline observability | 🔴 not started | — | — | Scoped separately; not in the 6-WO sequence. Highest-priority unstarted lane. |

---

## Open work (locked 2026-07-23)

Priority order for what to build next. Items 1 + 2 (migration/trip verification, Travel Doc smoke) are **DONE** as of the 2026-07-23 live pass — the queue below is the remainder.

**✅ Closed 2026-07-23 (live-verified on the running stack):**

- **Migration 0034/0035 verification** — FK + orphan cleanup applied clean; 0 orphan trips; no FK-check errors.
- **Trip API live baseline** — bogus person_id → 422; create → auto-days; patch → renumber; delete → cleanup; real trips preserved; zero DB locks / FK failures / 500s. ND-incident class contained.
- **Travel Doc smoke (9 canaries)** — OCR text/textless, real+blocked lookup, approval ladder, Lori wording (no "I can see"/coords), capture scope. All green.
- **Response-guard health** — 0 `wrapper raised` in current boot; no first-person parrot. INC-2026-07-09 closed.
- **P1/P2 fixes** — trip-create surfaces `days_created` (+ Trips-tab message); public-lookup title suffix strip + spoken-context trim; camera-consent ambush; extractor affect-hedge + vague-temporal guards; narrator-label collision; modal-turns-as-life-story archive gate.

**Remaining open work (priority order):**

1. **C1b — end-to-end WebSocket safety-routing test** — indirect ideation → classifier → `SafetyResult` → segment flag → softened mode → operator-visible signal → safe reply. Not yet proven end-to-end; protects Kent & Janice. Highest-value open safety item.
2. **`.env.example` drift audit (NEW WO)** — codify a grep gate: `grep -oh 'os.getenv("[A-Z_]\+"' server/code/ | sort -u`. Compare against `.env.example`. Reconcile ~24 stale documented flags + ~30 code-referenced undocumented flags. Flag drift is becoming a real ops risk.
3. **TRUTH-PIPELINE-01 Phase 1 (Gate 7)** — observability stub across the five truth-write stages (`raw_turn_saved` / `archive_event_created` / `extract_fields_called` / `family_truth_written` / `projection_updated`) per harness turn. Highest-priority unstarted lane. Blocks the remaining 🟡 formal-verified marks on Gates 5 + 6 becoming ✅ (the harness needs turn-level truth-write visibility to distinguish a real bug from a harness coverage gap).
4. **`sysBubble()` narrator-dignity pass** — some operator-tone bubbles retired behind `LV_INLINE_OPERATOR_BUBBLES`; the full 28-call sweep is still open. Operator/status/debug strings must not write into narrator chat.
5. **Extraction Track D (measurement first)** — D1 Travel Doc binding-eval corpus (report-only, UI scope as expected binding), D2 `story_candidates` Path 2 (preserved story text → draft candidates, operator review, no auto-promotion), D3 `utterance_frame` first consumer (hints vs Travel Doc scope, report-only). No truth writes.
6. **QUESTIONNAIRE-BIO-FACTS-MIGRATE-01 Phase 7 live verify** — code + tests landed 2026-06-16; live verify pending. Either finish it or explicitly park.
7. **WO-E2E-HARNESS-SANDBOX-01 (NEW 2026-07-26, not blocking)** -- make the Playwright e2e suite run against an isolated disposable database, never the live Hornelore database. Found while building Phase 3 of the Import Provenance work order and **deliberately not fixed there**, because repairing and running it would have written harness rows into the database Chris had just reset. The suite is not merely stale, it is live-DB-coupled: `tests/e2e/lorevox-smoke-flow.spec.ts` POSTs a person, a profile and an interview session with **no `afterAll` and no cleanup at all**; `test_narrator_switch.spec.ts` creates three people and cleans up only if `afterAll` runs; `lori_behavior_surfaces.spec.js` targets `/hornelore1.0.html` when the real path is `/ui/hornelore1.0.html` and looks for a narrator not in this database; `lorevox-ui-audit.spec.ts` targets `ui/lori9.0.html` on port 8080, a file that does not exist and a port nothing listens on; and `package.json` has six scripts pointing at four spec files that do not exist. This is the same class of residue that produced the Walt / Trip Canary / Kent duplicate-name clusters. **Scope: e2e harness only** -- no product behavior change, no Travel Doc feature work, no identity cleanup. Supersedes the older "not re-run since the Playwright 1.58.2 pin" note, which understated the problem.
8. **WO-LORI-MEMORY-EXERCISE-IMPLEMENTATION-01 draft** — ADR at `docs/architecture/MEMORY-EXERCISE-DECISION.md` says the style stays and needs a real implementation. Draft the WO spec before starting code.
9. **~~Stale assertion in `tests/test_trip_days_http_sequence.py`~~ -- FIXED 2026-07-27, see the Active block. Kept here for the trail.** -- line 201 asserts `DELETE /api/trips/{id}` returns 200/204. The product deliberately returns 409 with a `requires_force` envelope when the trip has dependents, and by that point the test has created 9 `trip_days` and 1 `trip_bio_suggestions` row. This is the only red suite in the eight-suite TestClient set and it was red before the web-stack alignment, so it is not a compatibility problem. Two clean fixes: expect 409 and assert the envelope shape, or drive the force path (`force: true` + `confirm_trip_id`) and then expect 200. Test-only change; **independent of the parked `trip_bio_suggestions` decision**, because the 9 days fire the gate on their own. Not fixed inside WO-WEB-STACK-TEST-ENV-ALIGNMENT-01 because that package's scope wall forbade test edits.
10. **Serving venv is off its own pin: `lxml` (NEW 2026-07-27, ruled out of scope twice)** -- `requirements-gpu.txt` pins `lxml==6.0.2` while `.venv-gpu` runs 6.1.1. Found in drift audit section 5, explicitly excluded from the test-env alignment by Chris's ruling (*"Record it as serving-env drift only"*). Either bump the pin to match reality or reinstall the serving venv to match the pin -- but as a serving-env decision, not smuggled into a test-env pass.

## Post-unification epic, WO-2 -- WO-TRAVEL-DOC-EVIDENCE-REVIEW-QUEUE-01 (🔄 **OPEN 2026-07-26 -- all four phases are now RUN. Phases 1, 3 and 2 LANDED; Phase 4's live smoke PASSED 26 of 27 probes and the one failure became Decision 5, the one-way decide guard, which is built and green. The live re-smoke of the fixed path RAN AND PASSED on the restarted stack, so all four phases are now closed and the residue is retired.**)

Spec: [`docs/wo/WO-TRAVEL-DOC-EVIDENCE-REVIEW-QUEUE-01_Spec.md`](docs/wo/WO-TRAVEL-DOC-EVIDENCE-REVIEW-QUEUE-01_Spec.md)

Work order 2 of the 8 in Chris's Epic Plan, and the next product step in his
own ordering: Import Provenance Foundation (done) -> **Evidence Review Queue**
-> Google Photos Picker / Takeout -> Lori Review Assistant -> Export.

WO-1 built the landing zone. It can record an import and it can record a
decision, but there is no way to look at the pile. Everything downstream needs
this queue first: the Picker and Takeout are producers with nowhere to deliver
for review, the Lori Review Assistant assists a review that has no surface,
and export traces decisions that cannot currently be made through a UI.

| Phase | Status |
|---|---|
| 1 -- the queue read | ✅ **LANDED 2026-07-26.** `queue_read()` + `GET /api/import-provenance/queue` + `tests/test_import_provenance_queue.py`, **56 tests green on the first run**. Baseline **399 -> 455**; routes suite held at **67**; surface **15 -> 16 routes**; still zero DELETE. `person_id` required and never inferred, unknown person 409 not an empty queue; **both kinds of hidden** honoured, with a test proving reopening a batch is not the same as unhiding it; counts describe the whole queue and ignore the `state` filter while `total` honours it; order is oldest-first tiebroken on `rowid` across twenty candidates written inside one second; `match_reason` deep-equal verbatim; batch and trip inline with the trip's date window, `trip: null` when unfiled, no join alias leaking and no id shadowing. **Four tests prove it writes nothing** -- database byte-unchanged, looking does not promote, no `photos` row materialized, no token servable because intake refuses one. One required edit to `FlagGateTests._all_routes()` in the routes suite, listed without `person_id` so the flag-off answer stays 404 rather than a schema-disclosing 422. |
| 2 -- the screen | ✅ **LANDED 2026-07-27.** The **Evidence** tab in Travel Documenter, `ui/js/travel-doc-lab.js` (5788 -> 6432 lines, eight patch sites, no existing function body rewritten) + `ui/css/travel-doc-lab.css` (664 -> 713, appended only). Shows candidates, counts, batch, trip **with its date window**, filename **and** external id, `match_reason` printed key-by-key exactly as the importer stored it, `state` and its reason. Seven row actions: promote + accept, reject, duplicate, error, hide/unhide, file to trip -- all through in-panel drawers, **no native `prompt`/`confirm`/`alert`**, no DELETE. Lab suite **129 -> 150**. Two silent-breakage collisions fixed before delivery (`tdl-ev-` -> `tdl-erq-`, `renderEvidenceRow` -> `renderErqRow`) and one existing assertion deliberately widened after being left to fail. |
| 3 -- promotion | ✅ **LANDED 2026-07-27, and built BEFORE phase 2 on purpose.** Decision 3 means a candidate has no `photo_id` until something promotes it, so "accept" is unreachable from a screen until this exists. `candidate_promote()` + `POST /api/import-provenance/candidates/{id}/promote` (optional multipart `file`) + `tests/test_import_provenance_promote.py`, **62 tests**. The photo is **born not narrator-facing and not Lori-approved** (a runtime assertion, not a comment), promotion **does not decide** (candidate stays `pending`), and promotion is **idempotent**, which is what makes the promoted-but-not-accepted state safe to retry. Surface **16 -> 17 routes, still zero DELETE**; no new dependency. |
| 4 -- live smoke | ✅ **RUN 2026-07-27 -- 26 of 27 probes passed, and the 27th became Decision 5.** Against the **serving** stack from the operator UI origin, not the unittest harness. Batch + 5 candidates, the `/queue` shape, trip filing and its 409, both kinds of hidden and the `include_hidden` restore, all four decision states, 400 on `skipped`, 409 on accepting with no `photo_id`, 409 on promoting with no bytes, 415 on `text/plain`, a real JPEG promoting to a row verified in SQLite as born unapproved, the candidate still `pending` after promotion, the idempotent re-promote, and **405 on all three DELETE probes**. Blast radius `photos` 16 -> 17, `import_batch` 0 -> 1, `import_candidate` 0 -> 5, nothing else moved. **The failure:** re-deciding an `accepted` candidate returned 200 and nulled its `photo_id`. Fixed under Decision 5, and the live re-run of that path **passed all seven steps on the restarted stack**: promote 200 and still `pending`, accept 200, re-decide 409, candidate still `accepted` with its `photo_id` and its whole row unchanged, and the promoted photo read out of SQLite as born unapproved. |

**Three decisions were Chris's. ALL THREE ARE CLOSED, 2026-07-27** -- **(1) placement stays trip-level, no migration 0038; (2) `changed` and `skipped` do not become states; (3) the `photos` row is materialized by an explicit promote route, option B.** Each was ruled the way the recommendation below argued for. The reasoning is kept here because it is why the code looks the way it does, not because anything is still open:

**A FOURTH decision was opened and closed the same day. Decision 4 -- DELETE: REFUSED.** It is recorded in the spec because it was *proposed and rejected*, not because it was never considered; a later session that finds no DELETE route on this lane should read `## Decision 4` in the spec rather than assume the gap is an oversight and helpfully fill it in. The full analysis, ruling, product rule and screen-wording rule are in the banner at the top of this file and in `docs/wo/WO-TRAVEL-DOC-EVIDENCE-REVIEW-QUEUE-01_Spec.md`.

**A FIFTH decision came out of the Phase 4 smoke itself. Decision 5 -- decisions are one-way, and the rule now lives where the write happens.** `candidate_decide()` never read the candidate's current state, so an `accepted` candidate could be re-decided as `rejected`, which cleared `photo_id` and stranded the promoted `photos` row -- the same stranding Decision 4 refused DELETE over, reachable through a route that shipped in WO-1. Chris ruled a strict guard in the repository rather than only in the UI, because WO-3's Picker and Takeout are the next callers and neither goes through the screen. `pending` is the only decidable state; anything else is **409 and nothing changes**. A mirror guard in `candidate_promote()` blocks the same stranding from the other end and is flagged as beyond the literal ruling. Full write-up, including the one test whose intent was inverted, is in the spec.

1. **Placement granularity.** 0037 has no `proposed_trip_day_id` /
   `_region_id` / `_stop_id`, so filing is trip-granularity only.
   *Recommendation: stay there, do not write 0038 yet.* Those columns hold what
   an importer proposed, and there is no importer until WO-3/WO-4 -- building
   them now means committing to a payload shape before its producer exists,
   and 0037 already paid that bill once when the plan's `review_status` /
   `operator_decision_json` lost to the shipped names.

2. **`changed` and `skipped`.** Neither is a 0037 state; both are refused 400.
   *Recommendation: neither becomes a sixth state.* `changed` is the diff
   between what was proposed and what was saved -- an audit fact that
   `state_reason` / `reviewed_by_user_id` / `reviewed_at` already hold, and
   making it a state means a row can be `changed` **or** `accepted` but not
   both. `skipped` means two things that are already covered: undecided is
   `pending`, decided-no is `rejected`. If the real want is *ask me again
   later*, that is a defer flag, not a state.

3. **Who materializes the `photos` row on accept.** **NEW -- this was not in
   the recorded WO-2 prerequisites and it is the largest structural gap.**
   `candidate_decide(state='accepted')` requires a `photo_id` of a row that
   already exists and is already owned by the candidate's person, and the
   repository deliberately never creates one. Nothing in the import lane
   materializes a photo, so **the queue can display, reject, mark duplicate,
   mark error and file to a trip, but cannot accept**. Phase 5's smoke only
   accepted because it pointed at a pre-existing photo. *Recommendation: (B),
   a separate `POST /candidates/{id}/promote`* -- the only option that does not
   weaken a shipped, tested guarantee. **Honest caveat:** a candidate holds an
   `external_id` or a filename, not bytes, so (B) probably ships in two pieces
   -- the route and the local/manual path now, the Picker/Takeout fetch when
   those importers exist.

## Post-unification epic, WO-1 -- WO-TRAVEL-DOC-IMPORT-PROVENANCE-FOUNDATION-01 (✅ **COMPLETE 2026-07-26 -- Phases 0, 1, 1.1, 2, 3, 4 and 5 all done; the Phase 5 live smoke is GREEN at 66/66 against the serving stack. WO-2, the Evidence Review Queue, is now OPEN above.**)

Spec: [`docs/wo/WO-TRAVEL-DOC-IMPORT-PROVENANCE-FOUNDATION-01_Spec.md`](docs/wo/WO-TRAVEL-DOC-IMPORT-PROVENANCE-FOUNDATION-01_Spec.md)
Audit output: [`docs/reports/identity_preflight_20260726.console.txt`](docs/reports/identity_preflight_20260726.console.txt)
Wipe rehearsal: [`docs/reports/christopher_wipe_rehearsal_20260726.console.txt`](docs/reports/christopher_wipe_rehearsal_20260726.console.txt)
Phase 2 live UI verification: [`docs/reports/phase2_ui_verification_live.console.txt`](docs/reports/phase2_ui_verification_live.console.txt)
Phase 5 live smoke: [`docs/reports/phase5_import_provenance_smoke.console.txt`](docs/reports/phase5_import_provenance_smoke.console.txt)
Live wipe: [`docs/reports/christopher_wipe_commit_live.console.txt`](docs/reports/christopher_wipe_commit_live.console.txt) | [`christopher_wipe_verify_live.console.txt`](docs/reports/christopher_wipe_verify_live.console.txt) | [`identity_preflight_after_wipe_live.console.txt`](docs/reports/identity_preflight_after_wipe_live.console.txt)

Work order 1 of the 8 in Chris's Epic Plan. It is first because Google Photos
Picker, Google Takeout, upload hardening and the Evidence Review Queue all need
the same landing zone, and because it is the one that has to settle what a
provenance row is keyed to.

| Phase | Status |
|---|---|
| 0 -- recon | ✅ **DONE 2026-07-26.** Eight findings, cited to file and line. Greenfield foundation (no `import_batch`/`import_candidate` anywhere; next migration is 0037), and an identity fault line under it: `photos.narrator_id` has no FK, `trips.person_id` does, six code sites equate them by convention, `trip_photo_links.photo_id` has no `REFERENCES` and is scoped by trip rather than owner, and `people` is flat with no merge tooling. The failure mode is silent -- an empty photo list, not an error. |
| 1 -- identity pre-flight (read-only) | ✅ **DELIVERED AND RUN 2026-07-26.** `scripts/audit_identity_preflight.py`, `mode=ro`, five report sections, exit 0/1/2. Ran against a byte-identical copy of the live database now that `C:\hornelore_data` is a connected folder. **Exit 1 -- IDENTITY SPLIT DETECTED.** Both Christopher rows carry data; new photo activity is landing on the id with zero trips; crossing-link and empty-cluster queries clean today by placement alone; **all orphan counts zero**, so the FK rebuild is measured-safe. |
| 1.1 -- resolve the two Christopher rows | ✅ **DONE 2026-07-26 -- executed live, `VERIFY: CLEAN`, exit 0.** Chris's call: **delete `e7fdb578` (`Christopher`), keep `a4b2f07a` (`Christopher Todd Horne`)**. No merge, no canonical pointer, no `merged_into`, no person-merge tooling. `scripts/wipe_narrator_identity.py` -- one id in `ALLOWED_TARGETS`, four in `PROTECTED_IDS`, **delete by id with the display name as an abort-only condition**, `--commit` gated on `--i-acknowledge` + a verified `--backup`, delete delegated to the app's own `hard_delete_person()`. Rehearsed dry-run/commit/verify-only/preflight on throwaway copies, exit 0 throughout: 21 dependent rows gone, residue 0, orphan sweep clean, `foreign_key_check` empty, four protected narrators unchanged. `audit_identity_preflight.py` still exits 1 on five forbidden-to-clean harness clusters, so **`--verify-only` (exit 0) is the gate**; the preflight was deliberately left alone. **Chris ran it live 2026-07-26** against `/mnt/c/hornelore_data/db/hornelore.sqlite3` with backup `backup_pre_christopher_wipe_20260725_205530.sqlite3`: identical to the rehearsal on every number, `status: hard_deleted`, people 37 -> 36, one Christopher row left. A first attempt was **refused** because the backup path was still the literal placeholder -- the gate fired before any write connection opened. |
| 2 -- migration 0037 | ✅ **LANDED 2026-07-26.** `0037_import_provenance_foundation.sql`, Option B. FK hardening on 0034's two-phase pattern -- orphan DELETE with FKs **ON** so cascades fire, table rebuild with them **OFF**, then re-arm and `foreign_key_check` -- giving `photos.narrator_id -> people(id)` and `trip_photo_links.photo_id -> photos(id)`, both CASCADE. Plus `import_batch` (18 cols) and `import_candidate` (25 cols) with `hidden`/`hidden_at`; `photo_id` is SET NULL, not CASCADE, so losing a photo cannot destroy the record of how it arrived. **No `narrator_ready`, no `include_in_memoir`, no token columns -- the absence is the enforcement of *intake is not approval*.** 37 new tests green, 539 green across 20 affected suites, applied through the real runner against pre-wipe and post-wipe copies of the live DB with zero row loss and a clean second-run no-op. **Applied to the live DB 2026-07-26 02:54:47 and verified there read-only** -- both FKs CASCADE, both tables present with all six FKs, `foreign_key_check` empty, `integrity_check ok`, `import_candidate` 25 cols with `narrator_ready` / `include_in_memoir` / token columns absent, row counts exact. **UI-verified on the running stack** ([`phase2_ui_verification_live.console.txt`](docs/reports/phase2_ui_verification_live.console.txt)): 13 photo links / 13 distinct photo ids / 0 null, `photo-links -> photos -> people` resolving onto `a4b2f07a`, `/thumb` and `/image` both `image/jpeg`, memoir-preview + travelogue-preview + narrator-photo-links + date-confirmations + sources + tree + days all 200, Photos tab rendering `All (13)`, zero console errors. Read-only pass; no write of any kind. |
| 3 -- repository layer | ✅ **DONE 2026-07-26.** `server/code/api/services/import_repository.py` + `tests/test_import_repository.py`, **50 tests green** (path note: there is no root-level `services/`; it lives beside `trip_repository.py`). Migration 0037 states four rules in the only language a schema has -- presence and absence of columns. Three of them SQLite cannot state at all, and this is where they become procedure. **Intake is not approval:** `candidate_create` takes no `state` and no flag, every candidate is born `pending`, `'accepted'` requires a `photo_id` that already exists and is owned by the candidate's person, every other decision refuses one, and the module never creates a photos row or sets `narrator_ready` / `include_in_memoir`. A runtime `PRAGMA table_info` guard fires if either column ever appears, so the schema's silence stops being load-bearing the moment it breaks. **Boundary:** a candidate copies its batch's `person_id` rather than accepting one; no batch or candidate may reach a trip or photo owned by anyone else; person-filtered lists never leak. **No raw tokens:** `ya29.` / `1//` / GitHub / OpenAI / JWT / `Bearer` / credential-URL shapes are refused on every operator string and recursively over `match_reason` keys -- and real Google Photos media ids and Takeout archive names still pass, which is what keeps the guard from being disabled later. **Reversible:** no `DELETE FROM` and no `DROP TABLE` anywhere in the module, asserted by a test that reads its own source; hide is a flag and hidden rows still count; counters are recomputed, never incremented. `candidate_create` is idempotent on `(batch_id, external_id)`, honouring 0037's UNIQUE index. A person hard-delete cascades the whole landing zone through the 0037 FKs, so neither table needs adding to `hard_delete_person`'s explicit list. Baseline 279 -> 329; the 0037 lock re-ran clean at 37. |
| 4 -- minimal verification surface | ✅ **DONE 2026-07-26.** `server/code/api/routers/import_provenance.py` + `tests/test_import_provenance_routes.py`, **65 tests green**, registered in `server/code/api/main.py`. 15 routes under `/api/import-provenance` -- batch create/list/get/counts/trip/close/reopen/hidden, candidate create/list/get/trip/decision/hidden, and an enums route -- **gated default-OFF behind `HORNELORE_IMPORT_PROVENANCE=1`**, answering **404 rather than 403** when off, because a disabled surface should not advertise that it exists. **No database logic of its own**; it is the HTTP boundary over the Phase 3 repository. **Boundaries re-checked at the route layer** rather than forwarded and trusted: a trip owned by another person, a photo owned by another narrator, or a `person_id`/`trip_id` pair that disagree all return 409 at the edge -- the mismatched pair deliberately an error and not an empty list, since *this person has nothing on that trip* and *that trip is not theirs* must not share an answer. **Every repository exception mapped:** 404 not-found, 409 boundary/lifecycle, 400 refused token or invalid state; only the base `ImportRepositoryError` (real schema drift) reaches 500. Ten tests assert a refused token returns 400 **and that the token text appears nowhere in the response**. **The 0037 column names win over the Epic Plan's working names** (`state` / `state_reason` / `reviewed_by_user_id` / `reviewed_at`, not `review_status` / `operator_decision_json`), and the plan's `needs_review` / `changed` / `skipped` are refused with 400 rather than quietly mapped. **Deliberately absent:** no `@router.delete`, no `person_id` on candidate-create, no route that can set `narrator_ready` or `include_in_memoir`, no Google Photos, no Takeout, no Evidence Queue UI, no Lori. **A Phase 3 defect was found here and fixed there:** `_now()` has whole-second precision, so `ORDER BY created_at` degraded to uuid order inside a second -- which for a real import is the whole batch; both list queries now tiebreak on `rowid`, locked by three tests added to the Phase 3 suite (50 -> 53). Baseline 329 -> 397. **Two WO-2 inputs recorded, not acted on:** 0037 has none of the plan's `proposed_trip_day_id` / `_region_id` / `_stop_id` columns, so placement is trip-granularity only until a future 0038; and `changed` / `skipped` have no 0037 state. |
| 5 -- live smoke | ✅ **DONE 2026-07-26 -- 66 assertions, 66 pass.** Run over HTTP against the **serving** stack from the operator UI origin, not through the unittest harness: `.venv` (tests) is fastapi 0.136.1 / starlette 1.0.0 while `.venv-gpu` (serving) is 0.135.1 / 0.52.1, so the smoke verified the code that actually answers requests. **A real defect was found and fixed here:** flag-off, `POST /batches` and `POST /candidates/{id}/decision` returned **422 with the names of their required fields** instead of 404, because FastAPI validates the body before calling the handler and the gate was the first line *inside* it -- a disabled surface disclosing both its existence and its schema. Fixed by promoting the gate to a **router-level dependency** (`dependencies=[Depends(_require_enabled)]`), per-handler calls kept; two tests added (every body-taking route x `{}`/junk/no-body -> 404, and `router.dependencies` non-empty so it cannot be demoted back). Route suite **65 -> 67**, baseline **397 -> 399**. Flag-off: all 15 routes 404 on well-formed, empty, junk and absent bodies. Flag-on: valid requests reach the repository, invalid bodies validate, every repository refusal maps to its deliberate 400/404/409, and `/enums` returns exactly the shipped vocabulary -- **`skipped` and `changed` absent, asserted**. Smoke coverage: two batches and four candidates in three shapes (Picker-style `external_id`, Takeout-style filename, manual); born `open`/visible/null-trip; person inherited from the batch and never accepted from the caller; oldest-first order stable across equal-second timestamps; `match_reason` round-trip; **four token-shaped inputs refused 400 with the token absent from the response and no row created**; accept against an existing same-person photo; reject; duplicate; hide/unhide preserving decision, reviewer and reason; counters; close/reopen with a closed batch refusing 409; cross-person 409 with no partial apply; cross-trip 4xx on batch, candidate and list; **no DELETE on either lane**. **Deliberate deviation:** batch A is Christopher's own person, because all four photos in the database are his and the accept step requires a same-person photo; batch B is Kent's and supplies the cross-person case; everything is tagged `PHASE5-SMOKE` and both batches were left `hidden=1`. **Blast radius, proved read-only (`mode=ro`) before and after:** `photos` approval subset still `6c4c6a07e2...`, `trip_photo_links` still `e3b0c44298...` (full-row `photos` baseline going forward: `8e5f7a577a...`), all four photos still carrying April `updated_at` including the one the accept pointed at, `narrator_ready` still `1,1,0,1`, every `date_approved_for_lori` / `location_approved_for_lori` still 0, photos/trips/trip_photo_links still 4/0/0 -- **no route created a photo and no route touched an approval flag** -- and zero rows in either import table contain token-shaped text. **Two false alarms run to ground rather than reported as defects:** keys returning alphabetized is `json.dumps(..., sort_keys=True)` and the round-trip is deep-equal exact, and `stored_rejected_count` 2 vs live `rejected` 1 is `_refresh_batch_counters` counting `rejected` **and** `duplicate` into the schema's `rejected_count` -- `batch_counts()` returns both on purpose. **Deferred, recorded, not worked:** the `.venv` / `.venv-gpu` dependency drift belongs to a later harness/environment work order, per Chris; `tests/` cross-module contamination means `unittest discover` is not a usable gate and pytest is not installed. Transcript: [`docs/reports/phase5_import_provenance_smoke.console.txt`](docs/reports/phase5_import_provenance_smoke.console.txt). |

**Both decisions are closed (2026-07-26). Recorded, not open:**

1. **The two Christopher rows -- delete `e7fdb578`, keep `a4b2f07a`.**
   `a4b2f07a` (`Christopher Todd Horne`) holds the trips, the photo sessions and
   the older library, so it survives untouched; `e7fdb578` (`Christopher`) held
   the bio_facts and every photo added since 2026-07-25 and is treated as bad
   seed. Not merged, not canonicalised, and **no person-merge tooling was
   written** -- Chris ruled that out in favour of the cheaper reset, because
   Import Provenance writes a durable `person_id` onto every candidate row and
   provenance recorded against the wrong Christopher is worse than none, it
   looks correct.
2. **`import_candidate`'s identity key -- Option B.** A plus the missing FKs on
   `photos.narrator_id` and `trip_photo_links.photo_id`, measured safe by Phase
   1 (every orphan count zero) and landed as migration 0037. C -- defer -- was
   rejected on the grounds that the first thing the epic builds would then be
   the first thing needing migration again.

**Scope wall carried forward, in Chris's words:** *do not spend another session
on identity architecture.* No merge tooling, no canonical pointer, no general
duplicate-person cleanup, no Walt/Test/Kent harness cleanup. The Christopher
deletion was the unblocker, not the project.

## Travel Doc unification -- WO-TRAVEL-DOC-UNIFY-01 (✅ **CLOSED 2026-07-25**, all six phases landed, Phase 6 live smoke 37/37 green)

Spec: [`docs/wo/WO-TRAVEL-DOC-UNIFY-01_Spec.md`](docs/wo/WO-TRAVEL-DOC-UNIFY-01_Spec.md)

**The problem this closes.** Two divergent Travel Doc implementations means the
operator's day-to-day surface is whichever one they happened to open — day cards
in one, route editing in the other. That cost a working session on the Bismarck
trip: the six `trip_days` rows existed and were correct, but the panel reachable
from the shell does not render day cards, so the work looked lost.

**Direction (decided, do not relitigate): the Lab absorbs the Documenter.**
Either direction rewrites the losing file at roughly equal cost; only this one
keeps the newer render architecture (state object + `renderAll()` loop +
preserved scroll + dirty-guard) instead of landing on `template()` →
`innerHTML`. `renderCurrent()` in the Lab is a deliberate placeholder — the
merge socket, not a feature. **The backend needs no changes at all.**

| Phase | Status |
|---|---|
| 1 — make the Lab mountable (`window.lvTravelDocMount(hostEl, opts) -> {destroy()}`) | ✅ **CODE-LANDED 2026-07-24.** Behaviour-neutral. `node --check` clean, 142 tests green across the five suites that read the lab source, mount lifecycle driven in a headless browser with `BroadcastChannel` instrumented (load → 1 channel; 2nd mount → 2; `destroy()` → 1 with the first still fully rendered; repeat `destroy()` a no-op). NOT formal-verified in the shell — see Phase 2. |
| 1.1 — mount liveness (hardening gate before Phase 2) | ✅ **CODE-LANDED 2026-07-24.** Chris's Phase 1 review: `destroy()` tore down but nothing stopped an in-flight async callback from repainting the cleared host. The file has exactly one of each guardable thing — one `fetch(` (in `api()`), one repaint entry (`renderAll()`), one channel handler, one socket `onmessage`, one timer, one `document` listener — so six guards cover all 54 `.then(` sites; the tests pin those counts so a seventh path fails loudly. `api()` returns a never-settling promise when dead, so neither `.then()` nor `.catch()` fires. Socket-identity pinning also fixes a **live-mount** bug: `loriPane.reset()` runs on every trip switch, so a queued Trip A token could append into Trip B's transcript. Also unbinds the `document` keydown listener — the leak the review did not name. 154 tests green; headless proof `scripts/ui/run_travel_doc_mount_liveness.js` (control row repaints, three destroyed rows do not); two negative controls confirmed red. |
| 2 — coexist in the shell tab behind a toggle | ✅ **CODE-LANDED 2026-07-25.** The tab now holds two hosts and a surface switch; unified is the default, legacy is one click away, and exactly one is ever mounted — each surface owns a BroadcastChannel, a `document` keydown listener and a Lori socket, so two live mounts is a correctness bug, not untidiness. Lab CSS rescoped from `:root`/`.tdl-body` to `.tdl-root` (custom-property inheritance is DOM-based, so the three `position: fixed` overlays still resolve through the host and were left alone). Launcher block deleted; `?person_id=`/`?api=` quarantined to the standalone page. Closed two pre-existing leaks: the shell was discarding the Documenter's handle, and the Documenter never closed its trip-update channel. 192 tests green; `scripts/ui/run_travel_doc_shell_mount_liveness.js` 22/22 on the real shell. It caught three defects static tests could not, including a `window._lvTravelDocSurface` cache that overwrote the same-named function. **Behaviour change:** leaving the tab destroys the mount, so trip/day selection does not survive a tab round-trip. |
| 3A — trip force-delete impact-review gate | ✅ **CODE-LANDED 2026-07-25.** Normal delete first; a 409 loads `e.body.detail` into an in-panel review; force delete arms only on the exact trip title or trip id; zero native dialogs. `api()` now attaches `err.status`/`err.body` — without that the gate could not read the payload at all. Renders **ten** lanes; production renders nine. Three blanket never-DELETE test guards narrowed, not dropped. 205 tests green; ten-step live smoke green. |
| 3B — trip create/edit + region/stop CRUD | ✅ **CODE-LANDED 2026-07-25.** Trip create/edit (title, dates, summary, days/sync warnings), region create/edit/delete (label, area, start, end, base, summary, with a soft out-of-range date warning), stop create/edit/delete (eight-value `STOP_TYPES`, region selector, reparenting with subtree exclusion). Insert-at-position preserved via `insertContext`/`insertHint` plus a guard that drops a stale context when the drawer is retargeted — production had no such check. `window.confirm()` did **not** port: both deletes are in-panel reviews, and the region path fixes a production dead-end (the backend 409s on a non-empty region unless forced; production neither forces nor handles it, so the delete silently does nothing). Also wired `st.daysWarning`, read by the Trip Plan tab but never set. 220 tests green; thirteen-step live smoke green with a functional socket/channel/listener census. |
| 3C — photo / source upload + cluster | ✅ **CODE-LANDED 2026-07-25.** Built, not ported — the lab had zero `FormData` and zero file inputs, so `api()` grew a `FormData` branch ahead of its JSON branch. One upload drawer with an explicit trip / region / stop scope selector and a target line that names the destination in prose; production instead reads the ambient `editorScope()` at submit time and can silently retarget. The drawer never repaints between choosing files and uploading, because a `FileList` cannot be restored by script — a test pins that absence. Source upload preserves `source_type` and `title` and never promotes: no `include_in_memoir`, no `trip_day_id`. Cluster renders in-panel and states the caveat production hides — the endpoint clusters the narrator's whole photo library. Evidence lanes unchanged, no new DELETE. Also fixed a same-scope bug: three suites used a string-blind comment stripper and went blind on `files.accept = "image/*"`. 236 tests green; sixteen new gates mutation-tested; twelve-step live smoke green across all three upload scopes. |
| 3D — route board | ✅ **LANDED 2026-07-25 (smoke-accepted).** Rows render in server order (client-side `.sort(` / `.reverse()` banned on the board path) with per-row up/down arrows that disable at the ends and while a move is in flight. **A stop move posts two ids to `/stops/{id}/move`, not production's full permutation to `/stops/reorder`** — a permutation is exactly as stale as the tree it was built from, and `/move` is the existing shape production already uses for cross-region moves. Regions still post a permutation because `/regions/reorder` is the only endpoint, so a refusal is surfaced in-panel plus a bundle reload rather than dead-ending. `region_id` and `parent_trip_stop_id` are echoed back unchanged so a reorder can never reparent. `routeBusy` serialises moves, `routeError` is the in-board failure surface, both clear with the trip and with a deleted row. Region rows made selectable, which revived Phase 3C's unreachable `defaultScopeKey()` region arm. Evidence badges at zero fetch cost. **The live smoke caught what nineteen static gates could not:** both movers pre-stringified a body that `api()` already stringifies, so every arrow press answered 422; fixed with a raw object at both call sites plus two gates banning `body: JSON.stringify` file-wide while pinning the encoding inside `api()`. 255 tests green (was 236); nineteen gates mutation-tested, nineteen mutants killed. **Live smoke green on all eleven steps** — reorder both directions, insert before/after including on a substop, cross-region move, cycle rejection at both layers, order surviving a refresh, both delete ladders, `FileList` node identity, the force-delete gate, a clean mount/socket census, zero native dialogs. |
| 4 — retire the legacy fallback | ✅ **LANDED 2026-07-25 (smoke-accepted).** Removal only. The surface toggle and its whole support cast came out of `app.js`, the two legacy asset tags plus the switch row and `#lvTravelDocHost` came out of the shell, the `.lv-td-surface-*` / `.lv-td-host-off` / `body.lv-td-focus` rules came out of `lori80.css`, and the route board's `prodTravelDocUrl()` deep-link foot-note came out with its CSS rule. **This phase deliberately narrowed the plan written here in Phase 0.** The old row said "remove `travel-documenter.js` / `.css`, `travel-doc-lab.html`" and "rename survivors"; Chris's actual Phase 4 order said the opposite on all three counts, and it is right on all three. Requirement 7 forbids removing anything a backend endpoint still serves, and `ui/travel-documenter.html` still mounts that module — unmounting a surface from the shell and deleting a module are different acts. `travel-doc-lab.html` is **kept and labelled `DEV-ONLY`**, because it is the only caller of `lvTravelDocMount()` outside the shell and the only one that mounts without the shell's identity flag; delete it and that branch rots untested. The rename is parked as churn that would bury a real diff. **Five on-path fixes the order did not name**, chief among them a `lv80SwitchPerson` teardown fallback that was nulling the retired marker and had become a silent no-op, and an ungated, operator-visible photo-picker empty state pointing at a production Travel Doc that no longer exists. **A file-wide `assertNotIn("legacy")` on the shell is invalid** — 19 unrelated occurrences — so the gate is scoped to the panel and the mount block; a stronger one replaced it, since the shell now holds zero **raw** occurrences of the old module's name. Tests rewritten not deleted: five out, thirteen in, seven narrowed. 262 tests green (was 255); eight mutants, eight killed; the liveness harness rewritten for one surface with a `singleSurface` probe and its unrunnable negative controls marked rather than dropped. **Live smoke green on all fifteen of Chris's steps.** One surface and no fallback toggle in the shell (0 `[data-td-surface]`, 0 `.lv-td-surface-switch`, no `#lvTravelDocHost`, `lvTravelDocSetSurface` undefined), zero requests for the retired module across 190 resources, and zero visible occurrences of *legacy*, *production Travel Doc*, *UI Lab*, *experimental*, *lab-only*, *removable* or *documenter* anywhere in the shell's rendered text. Region and stop create, edit and reorder verified server-side through `/api/trips/{id}/tree` rather than off the DOM, with a region's stops travelling with it and no reparenting. Photo and source upload both green with `FileList` node identity held across the drawer; cluster correct including its whole-library caveat. Lori opened at `surface: travel_doc_modal` scoped to the trip. **The mount census across three leave/re-enter round trips is the load-bearing one for this phase:** instrumenting `BroadcastChannel`, `WebSocket` and `document` keydown showed each entry opening exactly what the previous exit closed -- opens 3 / closes 2, keydown +3 / -3, sockets 0 net, `.tdl-root` pinned at 1 at every sample -- so retiring the second surface did not leave a second listener behind. Force delete rendered all ten impact lanes, refused a title one character short, armed only on the exact title, and completed. Zero native dialogs across the whole run, recorded by a spy rather than by inspection, and zero console errors across a full reload plus a sweep of all eight Travel Doc tabs. The standalone harness still mounts. One observation, not a defect: the dev harness page's *rendered* copy still says "Travel Doc UI Lab" and "This experimental lab needs a narrator" and still paints the `UI Lab - experimental` badge, while the page's own HTML header declares it `DEV HARNESS -- DEV-ONLY, REMOVABLE`. All three strings sit behind `embedded ? ... : ...` ternaries (`travel-doc-lab.js` lines 1096, 1100, 1591), so they can only ever render on the standalone page and never in the shell -- which is why the shell-path copy gate came back clean. Requirement 5 is met by quarantine rather than removal, exactly as written. The harness now calls itself two different things; reconciling that is a copy edit, parked rather than taken unilaterally after the code had already been committed. Smoke residue: the disposable trip and its uploaded source went with the cascade, but the uploaded photo did not -- photo *links* are deleted, photos are not -- so narrator `e7fdb578`'s library now holds five smoke photos, up from the four already accepted as documented residue. |
| 5 — test consolidation | ✅ **LANDED 2026-07-25.** Tests and docs only. `tests/travel_doc_surfaces.py` is the one map of the eleven Travel Doc surfaces (path + prose role + `on_operator_path`) plus the comment stripper and the two shell-region extractors; `tests/test_travel_doc_surface_gates.py` proves the cross-cutting doctrine once instead of once per originating work order. Six suites retargeted onto the map with **no test body moved**. The boundary did invert as predicted, but Phase 4 had already narrowed the assertions -- the only genuinely stale thing was the NAME `test_lab_does_not_import_production_module`, renamed `test_the_unified_module_never_loads_the_retired_one` with its assertion intact. The retired module's two native confirm calls are pinned at two rather than repaired or hidden. 279 green (was 262); twelve mutations, twelve killed, one of which exposed a real hole in a new gate. **Did NOT fold both suites into one `tests/test_travel_doc.py`** -- they guard two different modules that both still exist, and the fold would move every line number in both to produce a file whose name overclaims. Available as a mechanical follow-up if Chris wants it. |
| 6 — live smoke | ✅ **LANDED 2026-07-25 -- and this WO CLOSES here.** Live verification and docs only; **no code written at all**. Thirty-seven steps, thirty-seven green, zero defects. Interaction ran through a page-side helper resolving elements by text and structure at call time, because refs churn on every re-render inside the workspace; every persistence claim was read back from `/api/trips/{id}/tree` server-side rather than off the DOM; and dialogs, listeners, sockets, console errors and non-2xx traffic came from an instrumentation spy installed before the mount, including a `WebSocket.prototype.send` wrap so **step 28 was proved from the actual outbound `start_turn` frame** carrying `surface: "travel_doc_modal"` rather than from the on-screen label. The tab mounts the unified workspace directly -- no toggle, no retired UI, and zero rendered occurrences of *legacy*, *production Travel Doc*, *UI Lab*, *experimental*, *lab-only*, *removable* or *documenter*. Two regions and nine stops built including a three-deep substop group, edited at all four levels, reordered, insert-before/after on a top-level stop and on a substop, a cross-region move through the edit drawer, substop arrows confined to their sibling group, order surviving a full refresh. `FileList` node identity held across a scope retarget; cluster stated its whole-library caveat. **The census across three leave/re-enter cycles is the load-bearing lifecycle proof** -- read as deltas, every entry opened exactly what the previous exit closed, never more than one live channel or one live keydown listener, and the Lori socket opened exactly once in the entire run. Zero native dialogs, zero console errors, zero page errors. **The force-delete gate rendered all ten lanes with `bio_suggestions` HOT at 1** -- the Phase 3A finding reproducing live, since production's nine-lane grid would have shown that evidence nowhere while the backend refused the delete -- and arming was tested against a fifteen-case matrix rather than a happy path: case folding, prefix, suffix, one character short and one character long all refused; only the exact title, the exact id, or those with surrounding whitespace armed. The dev harness still mounts; the retired Documenter is still served and still unreachable from the shell, with all six remaining textual references inside comments. 279 tests green; both Playwright harnesses PASS (14/14, 23/23). Five findings, none a defect -- chief among them that **neither liveness harness can run on Chris's machine**, because `node_modules` does not exist in the working tree. |

**WO-TRAVEL-DOC-UNIFY-01 is CLOSED.** The post-unification backlog below is carried forward from the Phase 6 close-out; each item needs its own decision or its own work order, and **the new epic is a separate session.**

**Post-unification backlog (from the Phase 6 close-out):**

1. **`travel-doc-lab.js` -> `travel-doc.js` rename** -- pure churn against a 240K file that would bury every future diff. Parked since Phase 4; still the right call until something else forces the file to move.
2. **Delete the retired standalone Documenter** -- `ui/travel-documenter.html`, `ui/js/travel-documenter.js` and its stylesheet still exist and are still served, and Requirement 7 is met by quarantine. Deleting them is also the trigger that would make the Phase 5 test fold into `tests/test_travel_doc.py` mechanical and correct.
3. **`bio_suggestions` force-delete-from-birth** -- every trip is born with at least one bio suggestion, so every trip is force-delete-only from the moment it exists. A backend/product question, not a UI one: either the lane should not count toward `requires_force`, or the ladder is right and production's nine-lane grid was simply wrong.
4. **Selection persistence across a tab round-trip** -- leaving the tab destroys the mount, so trip and day selection do not survive re-entering. Known since Phase 2 and accepted then; still a real cost for an operator who tab-hops.
5. **Smoke-photo residue** -- narrator `e7fdb578`'s library now holds **six** disposable smoke photos (photo *links* are deleted with a trip, photos are not). **Taken as documented test rows 2026-07-26**, the second of the two options this item offered. The cleanup pass is deferred, not declined: the live database sits outside the repository at `$DATA_DIR/db/$DB_NAME` -- `/mnt/c/hornelore_data/db/hornelore.sqlite3` per `.env` -- which for every prior session neither the cloud container nor the device bridge could reach. **That changed 2026-07-26:** `C:\hornelore_data` is now a connected folder, and the identity pre-flight identified the six rows exactly -- narrator `e7fdb578`, uploaded 2026-07-25 between 03:33 and 23:31, all six with an empty `date_value`. Reading them is now an agent-reachable operation; **deleting them is still not**, and remains a decision plus a write Chris makes against the live database himself.
6. **`WO-EVIDENCE-LIFECYCLE-TRIP-FORCE-01` live smoke** -- its gate was exercised end-to-end here as part of the Travel Doc path, but that WO has never had a live smoke run against it in its own right.
7. **The post-unification epic** -- Import Provenance Foundation, Evidence Review Queue, Google Photos Picker, Google Takeout import, Lori Review Assistant, Lori Narrator Trip Story, Export Pipeline.
8. **Two person records read as the same narrator** (promoted from Phase 6 Finding 4, 2026-07-26) -- `e7fdb578` is `Christopher`, `a4b2f07a` is `Christopher Todd Horne`. Pre-existing data, untouched by the unification, and harmless while trips are created by hand. The epic changes that: Import Provenance Foundation writes provenance against a `person_id`, and Photos/Takeout import against one too, so two rows that read as one human in every picker is precisely the failure that splits an import across two narrators without erroring -- each half internally consistent, the pair silently wrong. Cheaper to settle before the first import path exists than after two of them do. Decide which record is canonical, or confirm they are two real people. **MEASURED 2026-07-26** by `scripts/audit_identity_preflight.py` against the live database: this is not a latent risk, it is an active divergence. Both rows carry real data -- `a4b2f07a` has 13 photos, 6 photo_sessions, 3 story_candidates and **both trips**; `e7fdb578` has 6 photos and 9 bio_facts -- and the six photos on `e7fdb578` all arrived on 2026-07-25, while `a4b2f07a` has received none since 2026-07-13. New photo activity is landing on the id that owns zero trips. Nothing has broken yet only because both trips and all thirteen linked photos sit on `a4b2f07a`; one trip created under `e7fdb578` produces a successful cluster run with zero matches. Promoted to Phase 1.1 of WO-TRAVEL-DOC-IMPORT-PROVENANCE-FOUNDATION-01. **CLOSED 2026-07-26 by operator decision: delete `e7fdb578`, keep `a4b2f07a`.** Not merged and not canonicalised -- Chris cut the scope explicitly to stop the epic turning into identity architecture. Tooling is `scripts/wipe_narrator_identity.py`, rehearsed to exit 0 and **run live by Chris on 2026-07-26 with the same result**. Closed. **Scope wall carried forward: do not spend another session on identity architecture, and do not take the remaining duplicate-name harness clusters (Walt, Trip Canary, Kent-harness, Esteban) -- they are explicitly forbidden work.**

**Resolved 2026-07-26 by WO-HARNESS-DEPS-01:** both liveness harnesses now run on Chris's machine, so the harness half of the proof chain is reproducible where the code lives. The dependency was always declared in `package.json`; only the gitignored `node_modules/` was missing, and the browser cache turned out to be present already and to match Playwright 1.58.2 exactly on all five binaries, so this cost a JavaScript-only install rather than a ~500 MB browser download. Versions pinned exact against caret drift, `package-lock.json` committed, and `scripts/ui/README.md` now documents the Node toolchain and both `.js` harnesses -- which it had never mentioned. See the 2026-07-26 CLAUDE.md changelog entry.

**Blocking constraints carried forward (each already cost real debugging
once, or would):**

- **`window.confirm()` must not port.** `travel-documenter.js` lines 2131
  (region delete) and 2153 (stop delete) both call it. They must land as
  in-panel review matching the trip force-delete gate, or they regress the
  posture `WO-EVIDENCE-LIFECYCLE-TRIP-FORCE-01` established. The Lab is
  currently clean.
- **The delete gate's error envelope** is pinned at `e.body.detail`, not
  `e.body`, because FastAPI nests structured payloads under `detail`.
  ✅ **CLOSED 2026-07-25 in Phase 3A** — the pinning assertion landed in the same
  commit as the port, and `api()` now attaches `err.status`/`err.body` so there
  is a body to read. Do not let it lapse in Phase 5's test consolidation.
- **The impact grid has ten lanes, not nine.** `_TRIP_DEPENDENT_TABLES` includes
  `bio_suggestions`, which `trip_timeline_bridge.sync_trip_to_life_record`
  writes on every trip create — so **every trip is force-delete-only from
  birth**. Production's nine-lane grid renders that as all zeros next to a
  refused delete. The unified port renders all ten. Whether a self-generated
  bio suggestion should block an unforced delete is a **backend/product
  decision** and was out of Phase 3A's frontend-only scope.
- **Upload is a capability to build, not a control to port.** The Lab contained
  zero `FormData` and zero file inputs; production has three photo-upload
  scopes plus source upload. The Lab was never a complete Travel Doc because
  uploads were always somebody else's job.
  ✅ **CLOSED 2026-07-25 in Phase 3C** — all three photo scopes plus source
  upload and cluster landed, with scope resolved inside the drawer rather
  than read from the ambient selection at submit time.
- **One Lori socket, not two.** Both panels open an operator socket on
  `source_surface=travel_doc_modal`. Merged, there must be exactly one
  connection and one modal scope, with the full `modal_scope` field list
  preserved. Check for a double-connect when the Lori tab and the Lori overlay
  drawer are both reachable in the same module.
- **The role boundary holds.** Travel Documenter = operator tool. Travels shelf
  = narrator surface. The merged panel keeps the 12-test boundary gate green.

**Defect found on the way in, fix in Phase 2:** the
`WO-TRAVEL-DOC-LAB-LAUNCH-BUTTON-01` block in `hornelore1.0.html` (~3600–3617)
has **zero CSS anywhere in `ui/`** for `lvTravelDocLabBtn`, `lv-td-lab-launch`,
or `lv-td-lab-hint` — which is why it renders as plain text and could not be
found. It is deleted in Phase 4 regardless; if Phase 2 keeps it reachable, it
needs a rule.

## Travel Doc Lab finish-pass — CLOSED, live-smoked (2026-07-23)

The finish-pass ran as one batch and passed a 16-canary live smoke on the
restarted stack. **CLOSED:** native `window.prompt()` removal (in-panel drawers);
"Lori will say…" preview aligned with the backend spoken trim (OCR untrimmed);
evidence text editing (Edit → PATCH revokes approval + clears memoir); refresh of
day/public-context counts after evidence actions; context patch/delete
trip-scoping (wrong trip_id → **409** live); unbounded conv-cache cap;
`travel-documenter.js` double-send guard; **mortality/988 reply leak** (live:
mortality → no escalation/no 988, acute → still triggers + 988). **C1b**:
composed-chain test code-landed AND live WebSocket smoke passed (indirect
ideation → safety_triggered + 988 + warm reply). Live smoke also: boot clean, 0
`wrapper raised`, OCR text/textless fail-safe, both drawers, real+blocked lookup,
approval ladder, no article dump / "I can see" / coordinates.

Public-lookup supersede: CLOSED + live-smoked 2026-07-23. Fresh lookup retires
prior UNAPPROVED drafts of the same source_type (sibling of OCR supersede);
approved rows untouched, rejected-not-deleted. Live: 2nd same-URL lookup
`retired_drafts=1`, only newest draft alive, approved row survived, blocked-URL
no row, scope 409 held. "One active unapproved draft per source_type per photo"
confirmed intended.

Remaining queue (unchanged priority): `.env.example` audit → sysBubble dignity
pass → Gate 7 truth-pipeline observability → Track D measurement-first.

## Travel Doc Lab — original get-it-working batch (2026-07-23, superseded by the CLOSED block above)

Organized by **restart-need**, to kill the stop/start churn: do all FRONTEND
items first (browser reload only, no stack restart), then ONE backend batch +
ONE restart that also picks up the three already-committed-but-not-yet-live
backend fixes (mortality-988 exception, modal spoken-trim, trip days_created).

**✅ Working, live-verified (2026-07-23):** trip lifecycle (create/patch/delete +
auto-days), OCR (tesseract eng+deu+ita+hrv+slv, confidence-gated, fails safe),
public lookup (url_only, SSRF-blocked, title-clean at storage), approval ladder
(draft → approved → memoir, edit revokes), Lori modal wording (provenance, no "I
can see"/coords), capture (modal turns → trip notes, scope-tagged), day-card
generate/reconcile.

**⚙️ Off by config (not bugs — deliberate):** vision (`HORNELORE_PHOTO_VISION=0`;
operators use manual "Add draft observation" instead), trip narration
(`HORNELORE_TRIP_NARRATION=log`, dry-run — flip to `1` when ready to bank), all
command adapters empty (`OCR_CMD` / `VISION_CMD` / `LOOKUP_CMD`).

**FRONTEND-ONLY (reload, NO restart) — do freely:**

1. **Replace `window.prompt()` in the Lab evidence panel** (`travel-doc-lab.js`
   L2089 + L2104 — "Add draft observation" + "Infer place from context") with an
   in-panel text input. Violates the Lab's own locked no-native-dialog doctrine.
2. Laptop-width UX: rail-collapse persists (done); re-verify the photo gallery /
   inspector at ≤1440px after any layout change.

**BACKEND (batch → ONE restart):**

1. **Context patch/delete route trip-scoping** — `patch_photo_context` (L2464) /
   `delete_photo_context` (L2496) / `patch_public_context` (L2127) /
   `delete_public_context` (L2172) accept `context_id` alone. Single-tenant so
   not a security hole, but a stale FE cache could patch the wrong trip's row.
   Add a body/query trip_id scope check.
2. **`chat_ws._TRIP_PREV_LORI` + `_TRIP_LAST_CAPTURE` unbounded dicts** — cap or
   LRU-evict; memory-leak on a long-running process.
3. **`travel-documenter.js` modal double-send guard** — parity with the
   2026-05-07 chat-path `_loriIsBusy` fix.
4. Restart picks up these + the 3 already-committed backend fixes above.

**DEFERRED (not this pass — spec-only or gated):** vision provider wiring;
PaddleOCR / Brave / SearXNG; `WO-TRAVEL-DOC-ACCORDION-TIMELINE-01` (spec only);
`WO-TRAVEL-DOC-OPERATOR-DRAFT-ASSISTANT-01` (spec only, deferred 2026-07-08);
`WO-TRIP-PHOTO-LIFEMAP-PROJECTION-01` (TODO: timeline-bridge photo meta, cover-
photo control); `story_candidates` Path 2 (Track D).

---

**MEDIUM open items (not blocking, but named so they don't drift):**

- Context patch/delete route trip-scoping — see the Travel Doc Lab batch above.
- `chat_ws._TRIP_PREV_LORI` + `_TRIP_LAST_CAPTURE` unbounded module-level dicts (memory-leak on long-running processes).
- `travel-documenter.js` modal double-send guard (parity with the 2026-05-07 chat-path fix).
- 7 named misses in `narrative_cue_detector` eval pack (33/40 = 82.5%).
- Travel Doc Lab evidence panel — replace native `window.prompt()` for draft observation + place-from-context with an in-panel editor / drawer — see the Travel Doc Lab batch above (frontend-only).

**Older README-open items to triage (decide active / parked / superseded / closed):**

- `WO-AUDIO-NARRATOR-ONLY-01`
- `WO-STT-HANDSFREE-01A`
- `WO-MEDIA-WATCHFOLDER-01`
- `WO-MEDIA-OCR-01`
- `WO-MEDIA-ARCHIVE-CANDIDATES-01`

These sit in the historical README status text and should not stay there forever.

---

## Where things live now

```
docs/
  architecture/                      strategic ADRs + this session's brief
    HORNELORE-UNIVERSAL-PIVOT-STRATEGY.md
    LORI-RUNTIME-ARCHITECTURE.md
    MEMORY-EXERCISE-DECISION.md
    COWORK-HANDOFF.md
  wo/                                active WO specs (build from these)
    WO-LORI-SAFETY-LLM-CLASSIFIER-01_Spec.md
    WO-LORI-SOFTENED-MODE-PERSISTENCE-01_Spec.md
    WO-LORI-PHASE-9-DISCLOSURE-UPDATE-01_Spec.md
    WO-LORI-STORY-FIRST-PHASE-1-01_Spec.md
    WO-LORI-ORAL-HISTORY-DEFAULT-01_Spec.md
    WO-LORI-BIO-BUILDER-UNIVERSAL-01_Spec.md
    superseded/
  archive/
    workorders-pre-pivot/            114 pre-pivot specs (history only)
    handoffs-pre-pivot/              pre-pivot HANDOFF / MORNING / LAPTOP / CHECKLIST docs
  reports/                           eval reports, WO completion reports, .docx history
  (existing subtrees preserved: research/, specs/, observations/, voice_models/, …)
```

Root carries operational files only: `README.md`, `CLAUDE.md`, `CONTRIBUTING.md`, `AGENT_CONTRACT.md`, `LICENSE`, this checklist, `.env`, `*.bat` launchers, top-level dirs (`launchers/`, `scripts/`, `server/`, `ui/`, `data/`, `docs/`, `tests/`).
