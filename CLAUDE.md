# CLAUDE.md — Hornelore Agent Env Notes

**Read this first at the start of every session.** These are persistent operational facts, not a task log.

## Mission

Lorevox is a privacy-first conversational memory system that helps older adults preserve life stories, supports cognitive engagement, and provides structured legacy outputs for family. The broader goal is a digital companion for aging populations — supporting memory recall, emotional processing, and intergenerational storytelling.

**This north star is more important than any single WO.** When a decision trades operational tidiness against narrator dignity, narrator dignity wins. The narrator is not an interview subject, not a data source, not a knowledge graph to populate — the narrator is the author of their own story, and the system exists to help them tell it. Operator-side tooling, eval harnesses, and extractor improvements all serve that. None of them outrank it.

**Hornelore** is the working implementation Chris uses with his own family (Kent and Janice Horne, his parents). The patterns shipped here graduate to **Lorevox**, the public product.

## Design principles (locked)

- **No dual metaphors.** Life Map is the only navigation surface. The river metaphor (Kawa, Memory River) was a useful theoretical lens early; it's retired as system, UI, and logic. Kept as a research citation only.
- **No operator leakage.** Anything a narrator can see or interact with must be designed for narrators. No Return-to-Operator buttons, no diagnostic surfaces, no operator-only controls in the narrator flow. Every UI element passes a role check.
- **No system-tone outputs.** Anything visible to the narrator sounds like a person talking, not a database query result. "(not on record yet)" disappears in narrator-facing output. "Based on: interview projection, session notes" never reaches the narrator. Source-of-truth attribution is operator-side.
- **No partial resets.** Reset Identity clears all narrator-scoped state in one operation, atomically. No lingering memoir cache, no surviving runtime softened-mode, no localStorage remnants. If a reset doesn't reset everything, it isn't done.

These four principles are checked against every UI element, every data write, and every WO acceptance criterion.

**Canonical extractor architecture reference:** `docs/specs/LOREVOX-EXTRACTOR-ARCHITECTURE-v1.md`. Consult this when scoping any extractor-lane WO, prompt experiment, or eval. Core Law: *Extraction is semantics-driven, but errors arise from failures in causal attribution at the binding layer.* Five-layer pipeline: Architectural / Control / Binding (primary failure surface) / Decision / Evaluation. Type A/B/C question typology LOCKED.

## Environment

- **OS**: Windows 11 + WSL2 (Ubuntu). Chris works from WSL.
- **Repo path (WSL)**: `/mnt/c/Users/chris/hornelore` — NOT `~/hornelore`.
- **Agent workspace mount**: `/sessions/<session-id>/mnt/hornelore`. Edits here are live on Chris's repo.
- **Git is NOT accessible from the sandbox mount.** `git status`, `git add`, `git commit`, `git diff --stat`, `git log` from the sandbox either fault with "not a git repository" or "unable to read <oid>". This is permanent — do not retry. File reads and edits work; only the git tooling is broken. All commit/branch/log operations must be handed to Chris as copy-paste blocks that he runs from `/mnt/c/Users/chris/hornelore`.
- **GPU**: NVIDIA RTX 50-series (Blackwell). Local LLM serves from this machine.

## Stack ownership

- **Chris starts and stops the API and full stack himself.** Do NOT include `./scripts/start_all.sh` or `./scripts/stop_all.sh` in copy-paste blocks.
- The API is assumed to be running at `http://localhost:8000` whenever an eval is asked for.
- **Cold boot takes ~4 minutes** — the HTTP listener comes up in ~60–70s but the LLM weights + extractor warmup continue for another 2–3 minutes after that. A `curl /` health check is NOT sufficient; it only proves the socket is listening, not that the extractor can serve a real request in <30s. This is why Chris owns start/stop: the agent-run combined blocks that restart the stack and immediately kick off evals cold-start the first case into a 90s read-timeout (observed on cg_001 during narrative-field r5c, 2026-04-21).
- If Chris ever explicitly asks for a combined restart+eval block, gate the eval behind an extractor-warmup probe (POST a trivial extract and loop until round-trip is <30s), not a bare `curl /` loop.

## Git hygiene gate

**Before any code-changing work starts, the tree must be clean** (`git status` shows nothing uncommitted). "Code-changing work" = edits to `server/`, `scripts/`, `ui/`, or any file the extractor / eval harness reads at runtime. Docs-only sessions are exempt but should still commit before EOD.

If the tree is dirty when new code work is requested, the agent's **first action** is to flag it and produce a copy-paste commit plan — NOT to start the code work. No "I'll commit after" — uncommitted state compounds silently across sessions because the sandbox can't run git, and a dirty tree destroys the bisect surface that every eval-gated patch depends on (today's 13-item pile-up — overnight WO batch + r5e1 reports + Phase 1 instrumentation all tangled — is the codifying example, 2026-04-22).

Agent action when tree is dirty:

1. Ask Chris to paste `git status && git diff --stat` from `/mnt/c/Users/chris/hornelore`.
2. Group changes into logical commits with **code isolated from docs** (so a regressing code change reverts cleanly without undoing doc work, and vice versa).
3. Produce copy-paste commit blocks using specific file paths (never `git add -A` or `git add .`).
4. Wait for Chris to confirm clean state before starting the next code change.

Exception: throwaway probe outputs under `.runtime/` that regenerate on every run. If in doubt, commit.

## Standard eval command (copy-paste ready)

When asked to run or re-run a master eval, emit exactly this block, rotating the output suffix:

```bash
cd /mnt/c/Users/chris/hornelore
./scripts/archive/run_question_bank_extraction_eval.py --mode live \
  --api http://localhost:8000 \
  --output docs/reports/master_loop01_<SUFFIX>.json
grep "\[extract\]\[turnscope\]" .runtime/logs/api.log | tail -40
```

(Eval runner moved from `./scripts/` to `./scripts/archive/` on 2026-04-25 to keep the start/stop folder clean. See `scripts/archive/README.md`.)

The eval script auto-writes `docs/reports/master_loop01_<SUFFIX>.console.txt` next to the JSON — no shell `| tee` needed (was silently producing 0-byte files under WSL pipe-buffer conditions; r4h's empty console triggered the fix on 2026-04-19).

Suffix convention: latest **locked baseline is `r5h`** (2026-04-22, 70/104, v3=41/62, v2=35/62, mnw=2 known). Earlier ladder: `r4h` TURNSCOPE v2 (#72 closed) → `r4i` #67 date-field (R4 floor 55/104) → `r4j` PROMPTSHRINK (measured, not adopted; flag in-tree for SPANTAG Pass 2) → `r5a–r5e1` NARRATIVE-FIELD (r5e1 floor 59/104) → `r5e2` ATTRIBUTION-BOUNDARY (REJECTED, in-tree behind `HORNELORE_ATTRIB_BOUNDARY=1`) → `r5f` SILENT-OUTPUT Phase 1+2 (69/104) → `r5g` #119 turnscope greatGrandparents (null, closed complete-with-caveat) → `r5h` WO-SCHEMA-ANCESTOR-EXPAND-01 Lane 1 trial annotations (current). Next: `r5i` = full Lane 1 (case_033 + case_039) or next extractor-lane patch.

The grep at the end rotates — change the tag to whatever filter is being tested (`turnscope`, `negation-guard`, `R4-E`, etc.) or drop it when not needed.

## Stubborn-pack diagnostic eval (copy-paste ready)

When the master eval moves but we need to know why stubborn cases (the frozen fail set) did or didn't shift, run this alongside. The master above stays the decision gate; this layer is diagnostic only.

```bash
cd /mnt/c/Users/chris/hornelore
HORNELORE_PROMPTSHRINK=1 ./scripts/archive/run_stubborn_pack_eval.py \
  --tag <SUFFIX> \
  --runs 3 \
  --api http://localhost:8000 \
  --master docs/reports/master_loop01_<SUFFIX>.json
```

Writes `docs/reports/stubborn_pack_<SUFFIX>_run{1,2,3}.json` plus a cross-run `stubborn_pack_<SUFFIX>_stability.json` + `.console.txt`. The stability console includes the master topline (when `--master` supplied) and buckets the 15 stubborn cases into stable_pass / stable_fail / unstable with per-case VRAM-GUARD truncation flag, failure-category change count, and field-path shape-change flag across the 3 runs.

Stubborn pack (15 cases, fixed): `case_008, case_009, case_017, case_018, case_053, case_075, case_080, case_081, case_082, case_083, case_084, case_085, case_086, case_087, case_088`.

Drop the `HORNELORE_PROMPTSHRINK=1` prefix when running the legacy prompt path — the wrapper itself is env-flag-agnostic.

## Standard post-eval audit block

After every eval that follows a code change, report this exact block before declaring any movement real:

- total pass count
- v2 contract subset
- v3 contract subset
- must_not_write violations
- named affected cases (newly passed, newly failed)
- pass↔fail flips
- scorer-drift audit on every flip (eyeball the truth zones — does the score change reflect a real extraction change, or a scorer/expectation drift?)

## Where files live

| Kind | Path |
|---|---|
| API log | `/mnt/c/Users/chris/hornelore/.runtime/logs/api.log` |
| Eval JSON reports | `/mnt/c/Users/chris/hornelore/docs/reports/master_loop01_*.json` |
| Eval console readouts | `/mnt/c/Users/chris/hornelore/docs/reports/master_loop01_*.console.txt` |
| Stubborn-pack reports | `/mnt/c/Users/chris/hornelore/docs/reports/stubborn_pack_*.json` (+ `_stability.console.txt`) |
| Eval case source | `/mnt/c/Users/chris/hornelore/data/qa/question_bank_extraction_cases.json` |
| Extract router | `/mnt/c/Users/chris/hornelore/server/code/api/routers/extract.py` |
| WO specs | `/mnt/c/Users/chris/hornelore/WO-*_Spec.md` (repo root) |
| WO reports | `/mnt/c/Users/chris/hornelore/docs/reports/WO-*_REPORT.md` |
| Canonical architecture spec | `/mnt/c/Users/chris/hornelore/docs/specs/LOREVOX-EXTRACTOR-ARCHITECTURE-v1.md` |
| SECTION-EFFECT Phase 1 output | `/mnt/c/Users/chris/hornelore/docs/reports/WO-EX-SECTION-EFFECT-01_ADJUDICATION.md` (pending) |
| SECTION-EFFECT Phase 3 output | `/mnt/c/Users/chris/hornelore/docs/reports/WO-EX-SECTION-EFFECT-01_CAUSAL.md` (pending) |

All of these are readable from the agent workspace mount via the session prefix. After an eval runs, read the console + JSON directly — do not ask Chris to paste them.

## Current phase

**LOOP-01 R5.5 — active baseline `r5h` (70/104, v3=41/62, v2=35/62, mnw=2).** Two known mnw offenders (pre-r5e1 carryover, both WO-LORI-CONFIRM-01 targets): `case_035` faith turn (`education.schooling` narrator-leak) and `case_093` spouse-detail follow-up (`education.higherEducation`). r5h delta vs r5f (69/104) is scorer-side only: +1 case_081 via `alt_defensible_paths` annotation adding `greatGrandparents.ancestry` as acceptable path for `grandparents.ancestry` truth zone. parse_success_rate 93.3%, truncation_rate 0.0%. ATTRIBUTION-BOUNDARY / NARRATIVE / PROMPTSHRINK exemplar blocks remain in-tree behind default-off env flags; default live behavior is the r5f composite.

r5h hallucination prefix rollup: parents 11, family 10, siblings 7, greatGrandparents 5, education 5, grandparents 4, laterYears 2, residence 1. The parents/family/siblings cluster is the dominant hallucination vector now, not greatGrandparents (that class dropped post-r5f).

Active sequence (reordered 2026-04-23 after #95 PARK):

1. **#144 / WO-SCHEMA-ANCESTOR-EXPAND-01** — two-lane. Lane 1 = scorer-only `alt_defensible_paths` for case_033 (greatGrandparents.military* → military.*) and case_039 (parents.occupation → community.role/organization); zero-extractor-risk. Lane 2 = schema expansion (greatGrandparents.firstName/lastName/relation/ancestry); gated behind Lane 1 evidence. Spec: `WO-EX-SCHEMA-ANCESTOR-EXPAND-01_Spec.md`.
2. **#97 / WO-EX-VALUE-ALT-CREDIT-01** — value-axis alt-credit under the same ≥0.5 fuzzy gate. r5h evidence: case_087 ancestry='French' vs 'French (Alsace-Lorraine), German (Hanover)'. Parallel cases 075, 088. Spec: `WO-EX-VALUE-ALT-CREDIT-01_Spec.md`.
3. **#90 / WO-EX-SPANTAG-01** — two-pass extraction, **promoted to active** by #95 PARK. Pass 2's controlled-prior prompt keeps `current_section` + `current_target_path` only and **drops** `current_era` / `current_pass` / `current_mode` per matrix evidence (Q1=NO at n=4 with zero within-cell variance). Existing Pass 1 / Pass 2 scaffolds landed as #90 Commits 1–2; this is Commits 3–6 per `WO-EX-SPANTAG-01_FULL_WO.md`.
4. **Post-SPANTAG gate** — SPANTAG default-on opens R5.5 Pillar 2 (entity-role binding); else iterate.
5. **WO-LORI-CONFIRM-01** — parked. v1 = 3-field pilot (`personal.birthOrder` / `siblings.birthOrder` / `parents.relation`). dateRange v1.1 REACTIVATION UNLOCKED — canon-grounded corpus expansion to 24 cases (#111 closed 2026-04-23) added 3 stubborn date-range cases (cg_019, cg_020, cg_021), exceeding the ≥2 gate.
6. **WO-INTAKE-IDENTITY-01** (Chris's lane) — v3 spec FINAL, pending Chris's decisions on rollback-flag data path + idempotency-guard legacy-key scan before endpoint impl.

Parallel cleanup lanes (do not block the main sequence): **#152 / WO-EX-BINDING-01** (Binding Layer fix per Architecture Spec v1 §7.1; **Option A primary re-lock 2026-04-23 evening** — binding rules delivered inside the SPANTAG Pass 2 prompt / binding contract via PATCH 1–5, not a standalone rule layer; smoke-test target case_082 V1/V6; scope: three routing conflation pairs (residence vs personal.placeOfBirth, residence.period vs education.gradeLevel, siblings vs narrative references) + minimal scalar guard for `personal.placeOfBirth`; eval tag `r5g-binding` layered on `r5f-spantag`; `[extract][BINDING-01]` log marker required; spec: `WO-EX-BINDING-01_Spec.md`); **WO-EX-FIELD-CARDINALITY-01** (deferred separate lane per Architecture Spec v1 §7.3 — opens only if BINDING-01's minimal scalar guard proves insufficient; not named FIELD-DISCIPLINE-01, #142 owns "discipline"); **#141 WO-EX-FAILURE-PACK-01** CLOSED 2026-04-23 (cluster-JSON sidecar wired at `scripts/run_question_bank_extraction_eval.py:39,2281–2295`; verified against r5i/r5j outputs); **#142 WO-EX-DISCIPLINE-01** (run-report header: git SHA, flag state, model hash, api.log freshness window, stack uptime); **#111 canon-grounding corpus expansion** CLOSED 2026-04-23 at 24 cases / _version=2 (8/8/8 narrator balance, 3 stubborn date-range — LORI v1.1 gate exceeded).

Each WO reports the standard audit block (extended with `truncation_rate`) before being called done.

**Closed (suffix → outcome):** `r4h` #72 TURNSCOPE; `r4i` #67 date-field; `r4j` #81 PROMPTSHRINK (measured, not adopted — flag in-tree for SPANTAG Pass 2); `r5e1` WO-EX-NARRATIVE-FIELD-01 (Phase 2 tighten + CLAIMS-02 role-exempt + Fix A/C composite); `r5f` WO-EX-SILENT-OUTPUT-01 Phase 1+2 (salvage-preamble-tolerance at `extract.py` L3481–5 + 5-counter silent-root cascade L6609–6927); `r5g` #119 turnscope greatGrandparents (complete-with-caveat — EXPAND fires but items die at schema validation; real unlock is #144); WO-STT-LIVE-02 fragile-fact transcript guard (backend + frontend, byte-stable with eval harness); **#95 SECTION-EFFECT Phase 3 PARK** (matrix ran clean 72/72 at tag `se_r5h_20260423`; Q1=NO; Q2=YES stratified by Type A/B/C; Q3=YES stratified by Type A/B/C. Cases split as Type A target-anchored-abstract (case_008), Type B overdetermined-factual (case_018), Type C weakly-constrained-narrative (case_082). Bumper sticker: **Extraction is semantics-driven, but errors are binding-driven** — the real axes of variation live in the question-evidence pair, and Type C's failures are present in V1 (the "ideal" configuration), which means the bottleneck is extractor-internal binding, not runtime context. Era/pass/mode carry no independent signal; SPANTAG Pass 2 drops them from the controlled-prior block. Follow-ups: #90 SPANTAG promoted, new #152 BINDING-01 lane minted for Type C schema-binding failures, cardinality lane naming deferred.); **#141 WO-EX-FAILURE-PACK-01** (cluster-JSON sidecar wired into master eval).

**Deferred (long tail):** Hermes 3 / Qwen A/B (post-SPANTAG, attribution clean-up first); KORIE staged-pipeline (conditional on SPANTAG ≥20% lift or ≥3 stubborn flips); #35 V4 scope axes; R6 Pillars 2 & 3; **#68 case_053 wrong-entity** (disposition memo, deferred to R6 Pillar 2).

## Chris's working preferences

- Honest critique over flattery. Push back on ideas when warranted.
- Tight readouts, not walls of text.
- Do NOT relitigate things already decided.
- When three agents (Claude/Gemini/ChatGPT) converge on the same answer, act on it; don't re-argue.
- Do not regenerate command blocks from memory — copy from this file.
- Read logs and reports directly from the workspace mount; don't ask Chris to paste.

## Companion stack (Lori — the point of the whole system)

The extraction pipeline is one output surface; **Lori is the companion** — designed around older-adult narrators with possible cognitive decline. When planning UI, interview flow, or LORI-CONFIRM work, remember the narrator may not be able to correct, clarify, or re-engage on a standard cadence.

**WO-10C Cognitive Support Mode (landed, dementia-safe):** six behavioral guarantees — protected silence, invitational re-entry, no correction, single-thread context, visual-as-patience, invitational prompts. Silence timing stretched 30s/55–75s → 120s/300s/600s. Re-entry bypasses confidence gates. **Known gap: no operator UI toggle yet — flag must be set programmatically.** Report: `Hornelore-WO10C-Cognitive-Support-Report.docx`.

**Facial awareness stack (browser-only, zero video transmission):** MediaPipe FaceMesh (468 landmarks) → geometry rules → affect labels (steady / engaged / reflective / moved / distressed / overwhelmed). Never ships video, raw landmarks, or raw emotion vectors — only derived `affect_state` + confidence + duration. `facial-consent.js` persists consent in `localStorage['lorevox_facial_consent_granted']`. `emotion.js` L348 has a load-bearing SIMD→non-SIMD WASM redirect (SIMD build crashes at `loadGraph` on Chris's stack). `cognitive-auto.js` v7.4C policy: visual can *accelerate* but not *cause* a mode transition; text has veto.

**Camera / mic / TTS state-machine interactions:** activation chain is `toggleEmotionAware()` → `FacialConsent.request()` → `LoreVoxEmotion.init()` → `LoreVoxEmotion.start()` → `cameraActive=true` + `state.inputState.cameraActive=true` + `window.lv74.showCameraPreview()`. Two truth sources for `cameraActive` (global at `state.js:407` and mirror at `state.js:283`) can desync on narrator switch. Perm-card path flips `emotionAware=true` only — it does NOT call `startEmotionEngine`. `camera-preview.js` reuses the emotion-engine's hidden video `srcObject`; if absent, falls back to a second `getUserMedia` (can double-prompt or fail silently). WO-MIC-UI-02A's 4-state visual (LISTENING / OFF / WAIT amber / BLOCKED) × WO-10H turn-claim state machine × WO-10C stretched silence can interact badly when TTS on 8001 errors mid-stream (mic stuck amber).

**Diag panel first.** For any camera/mic/TTS bug, open the in-app diag at `app.js:5730–5819` (`lv10dSyncHeaderControls`) — it already emits warnings for "Camera active but preview DOM not created", "emotionAware=true but facial consent declined", "Turn state stuck in awaiting_tts_end", plus live `lv10dBpFacialConsent` / `lv10dBpConsentStored` / `lv10dBpCamPreview` / `lv10dBpTts` / `lv10dBpSignalAge` readouts. Check these first; the fault surface collapses to one branch.

**Parallel/supporting subsystems:**

- **Kawa (RETIRED 2026-05-01)** — the river metaphor was a useful theoretical lens early on; gave the project vocabulary for client-as-theorist framing and "river of memories" visualization. The implementation has converged on the canonical 7-era life spine + Life Map UI, and a second river metaphor confused both the model and the user (the broken "narrator-room Memory River view tab" in the 2026-04-30 audit was the trigger). Decision: **Life Map is the only navigation surface; Memory River is removed as a UI.** Kawa is kept as a research citation only — the four papers in `Research/Kawa/` (OTI2023-2768898, TST.2024.9010104, The_Dynamic_Use_of_the_Kawa_Model_A_Scop, newbury-lape-2021-well-being-aging-in-place) support the academic framing of "narrator-as-theorist of their own life" for write-ups. No Kawa engine, no Kawa rendering, no River-of-Memories pre-generation.

- **Pheno (PARKED)** — lived experience + wisdom extraction, separate from truth fields. DESIGN COMPLETE spec, not wired. Doesn't conflict with anything currently shipping. Reactivate when extractor lane settles.

- **WO-STT-LIVE-02 fragile-fact transcript guard** is landed (7-pattern classifier + 30s staleness cap + typed-input fallback).

## Changelog

- 2026-04-19: Created. Captures WSL path correction and eval-command template.
- 2026-04-19 (late): r4h confirms WO-EX-TURNSCOPE-01 closes. Eval script now auto-writes `.console.txt` (shell `| tee` dropped after r4h produced an empty file). Standard post-eval audit block codified. #67 staged but not yet landed.
- 2026-04-19 (later): #67 landed (r4i: 55/104, case_011 + case_012 flipped green). #68 disposition memo written and deferred to R5 Pillar 2. #81 PROMPTSHRINK patch landed behind `HORNELORE_PROMPTSHRINK=1` flag — topic-scoped dynamic few-shot selection (3–8 per call vs 33 static), legacy path byte-stable when flag off. Stubborn-pack diagnostic wrapper added (`scripts/run_stubborn_pack_eval.py`) — 15-case pack, 3 runs, VRAM-GUARD truncation attribution, stable_pass/stable_fail/unstable buckets. Next eval = `r4j` = master + stubborn-pack with PROMPTSHRINK on.
- 2026-04-20: r4j measured (54/104, one regression `case_012`, 15/15 stubborn truncation). **PROMPTSHRINK closed as "measured, not adopted"**; flag stays off by default, in-tree for possible SPANTAG Pass 2 pairing. **r4i locked** as active baseline (`POST-R4-BASELINE-LOCK.md`). **#82 post-R4 memo** written. **WO-EX-SPANTAG-01 rewritten** from single-pass stub to two-pass (evidence + bind). Then three-agent convergence (Chris / Claude / ChatGPT) flagged **section-conditioned schema coercion** as a distinct failure mechanism on case_008 / 009 / 082 — SPANTAG spec sharpened with explicit controlled-prior inputs + subject-beats-section rule + first-class dual-path emission. **#63 promoted** from cleanup to prerequisite WO (WO-EX-SECTION-EFFECT-01): adjudicate stubborn-15 labels, extend extraction payload with `currentEra`/`currentPass`/`currentMode`, run causal matrix. **SPANTAG implementation paused** pending SECTION-EFFECT-01 Phase 1 signoff.
- 2026-04-20 (later): **#92 SECTION-EFFECT Phase 1** landed (`WO-EX-SECTION-EFFECT-01_ADJUDICATION.md` + `alt_defensible_paths` patched into cases_008/009/018/082). Classification tally: truncation_starved 7, dual_answer_defensible 4, scorer_drift_suspect 2, subject_only_defensible 1, wrong_entity_bind 1, section_only_misapplied 0. **SPANTAG spec narrowed** — primary sub-pack is 4 cases, not 15; ship-blocker on #94. **#94 SECTION-EFFECT scorer policy** landed — scorer credits `alt_defensible_paths` on must_extract zones (value must still fuzzy-match ≥0.5); classifier adds `defensible_alt_credit` (informative, non-penal). r4i re-scored with patched scorer: 55/104 → 57/104 (+2: case_008, case_009); 0 crashes; 0 newly-failing. **#93 SECTION-EFFECT Phase 2** landed — life-map stage fields (`current_era` / `current_pass` / `current_mode`) threaded from interview runtime (`ui/js/interview.js` reads `state.session`) through extraction payload (`ExtractFieldsRequest`) into `[extract]` log lines (attempt + summary both carry era/pass/mode now). Eval harness synthesizes stage context per case via new `_phase_to_era()` mapping (all 104 cases mapped cleanly, no unmapped phases). Pure plumbing; no extractor-behavior change. Next tag = **r5a** (master with life-map logs; Phase 3 causal matrix is #95).
- 2026-04-21: Stack-ownership rule sharpened — cold boot is **~4 minutes** (HTTP listener ~60–70s, then 2–3 min model warmup). The agent-issued combined restart+eval block on 2026-04-21 cold-started cg_001 into a 90s read-timeout because the wait loop only checked the listener, not the extractor. Codified the warmup-probe requirement on any future combined block, but default remains: agent does NOT restart the stack.
- 2026-04-21 (mid-day): **WO-EX-NARRATIVE-FIELD-01 Phase 2 tighten landed** (commits `3fec639`, `9706a87`) — anti-consolidation SCALAR CO-EMISSION rule + NARRATOR-IDENTITY PRIORITY rule appended to `_NARRATIVE_FIELD_FEWSHOTS` (extract.py L1537–1584), gated behind `HORNELORE_NARRATIVE=1`. Fixes the Phase 2 regression where narrative fewshots pulled narrator scalars (`personal.dateOfBirth`, `personal.placeOfBirth`) into parent-prose buckets on case_049. Targeted smoke: case_049 → 1.000. **CLAIMS-02 role-exempt** landed (commit `291cbb8`) — added `"role"` to `_SHORT_VALUE_EXEMPT_SUFFIXES` (extract.py L4547). Fixes CLAIMS-02 validator dropping `community.role='OT'` as <3-char on case_040; this was pre-existing, masked by r5c/r5d length-luck (LLM randomly emitted `'OT (Occupational Therapist)'`). **Phase 3 Fix A landed** (commit `77e5b52`) — `_ROLE_ALIASES` + `_role_alias_match` in `scripts/run_question_bank_extraction_eval.py` L149–168, wired into `score_field_match` at L181. Bidirectional alias for 6 healthcare/therapy initialisms (OT/PT/RN/LPN/NP/SLP). **Measurement-side change** — on every r5e flip involving a `.role` field, scorer-drift audit must attribute whether extraction changed or scorer credited a value it wouldn't have before. **Phase 3 Fix C landed** (commit `74f7b0d`) — DATE-RANGE PREFERENCE RULE appended to `_NARRATIVE_FIELD_FEWSHOTS` (extract.py L1586–1595): for `*.yearsActive`/`*.dateRange`/`*.servicePeriod` fields, prefer explicit range (`1997-2026`) over duration phrase (`twenty-five years`); expand professional-role abbreviations in context. Next eval = **r5e** (master + Phase 2 tighten + role-exempt + Fix A + Fix C composite). Methodology flag: 5 stacked commits, one scorer-side — audit flips rigorously on landing. r5d floor = 58/104 v2=30. r5e prediction: 59–62/104 v2=31–33.
- 2026-04-21 (evening): **r5e2 master REJECTED** (56/104 v3=35/62 v2=29/62 mnw=0). Three-agent convergence (Chris / Claude / ChatGPT): the ATTRIBUTION-BOUNDARY RULE did clean up forbidden writes (mnw 2 → 0) and hit its named targets (case_093 0.70 → 0.90, case_005 0.00 → 1.00), but friendly-fired on 7 clean passes (case_003/018/022/034/067/075/088 all regressed to ≤0.50, three of them 1.00 → 0.00), **including case_075 which is exactly the mother_stories class the rule was meant to protect**. `noise_leakage` tripled (4 → 12). Net -3 vs r5e1 floor. Action taken: **(b) + (c) combined** — r5e1 re-locked as the performance floor; ATTRIBUTION-BOUNDARY exemplar block kept in-tree but gated behind a new default-off `HORNELORE_ATTRIB_BOUNDARY=1` flag (paired with `HORNELORE_NARRATIVE=1`). extract.py change: `_NARRATIVE_FIELD_FEWSHOTS` truncated at end of DATE-RANGE PREFERENCE RULE; ATTRIBUTION-BOUNDARY block extracted into a separate `_ATTRIBUTION_BOUNDARY_FEWSHOT` constant; both append sites (L929, L1491) now consult `_attribution_boundary_enabled()` as a secondary condition. Default live behavior is now byte-identical to r5e1. The real fix for the attribution/ownership class is parked as **WO-LORI-CONFIRM-01** (interview-engine confirm pass + kinship skeleton, spec at repo root), sequenced behind SECTION-EFFECT Phase 3 (#95) + SPANTAG (#90) default-on/off decision. `FAILING_CASES_r5e1_RUNDOWN.md` authored today: 45-case Q/A/era/issue rundown, surfacing birth-order arithmetic cluster, pet salience, silent-drop explicit-answer class, and the attribution class that WO-LORI-CONFIRM-01 now targets. Active baseline = r5e1 (59/104, mnw=2 known, case_035 + case_093 the two offenders). Next tag = **r5f** (post-SECTION-EFFECT Phase 3 or next extractor-lane patch), unrelated to the rejected r5e2.
- 2026-04-21 (overnight, doc-only): Three overnight artifacts from yesterday evening's assignment delivered: (1) **WO-EX-SECTION-EFFECT-01_PHASE3_WO.md** — Boris-style execution pack for task #95 (causal matrix on cases 008/018/082, 6×4 variant matrix + 2 controls, new `scripts/run_section_effect_matrix.py`, 4 dispositions ADOPT/PARK/ITERATE/ESCALATE, zero extract.py touch); (2) **WO-EX-SPANTAG-01_FULL_WO.md** — execution companion to the design spec, Commits 3–6 scoped (pipeline wiring, eval harness, first eval `r5f-spantag`, conditional), friendly-fire guardrail baked into the 8-criteria gate (0/4 regressions on primary sub-pack); (3) **WO-EVAL-MULTITURN-01_Spec.md** — parked spec for multi-turn harness to validate WO-LORI-CONFIRM-01, 15-case first pack, 6 dispositions, GREEN/YELLOW/RED gate, canned-narrator not LLM. Phase 4 of `docs/reports/WO-EX-NARRATIVE-FIELD-01_REPORT.md` filled with real r5e/r5e1/r5e2 numbers + per-flip scorer-drift audit. Later in the night (Chris: "work on the LORI WO more, prep for it"): **WO-LORI-CONFIRM-01_PREP_PACK.md** authored — companion to the parked LORI spec filling the concrete gaps (kinship_skeleton.json draft, 4 confirmation_banks/*.json drafts, dispatcher pseudocode, canon-collision reconciliation algorithm with explicit Rules 1-7, per-field decision trees tied to r5e1 cases, SPANTAG/SECTION-EFFECT integration matrix, 15-case pilot map, quantitative pilot acceptance matrix, ~2.75-day implementation cost estimate). **`docs/drafts/multiturn_cases_r5e1_pilot.json`** authored — 15-case (6 target, 9 control) multi-turn eval pack conforming to MULTITURN schema. **Question-bank tagging audit** completed (`docs/reports/question_bank_tagging_audit.md` + `.json`) — Chris's 10-tag vocabulary applied to all 104 cases; top finding: `silent_output_risk` is the operational emergency (26 cases, 12% pass rate, 23/45 of failures carry it) — recommend standing up `WO-EX-SILENT-OUTPUT-01` as a separate lane to separate parse-failure / max-new truncation / prompt collapse / rules_fallback fallthrough. `birth_order_confirm` tag is 0/4 pass — perfect LORI-target signal. `date_range_confirm` has only 1 corpus case — recommend authoring 3 date-range cases in next corpus-expansion pass (#111). **Zero extract.py changes, zero eval runs, stack untouched.** Active baseline: still r5e1 (59/104, v3=38/62, v2=32/62, mnw=2 known).
- 2026-04-22 (morning, doc-only): Chris's morning read of the overnight batch delivered two decisions folded back into in-tree artifacts. (1) **Date-range DESCOPED from LORI v1 pilot.** `WO-LORI-CONFIRM-01_PREP_PACK.md` top-of-file v1 SCOPE UPDATE banner added; §2.4 `dateRange.json` bank header already marked DESCOPED FROM LORI v1 PILOT with reactivation criteria (≥2 stubborn date-range cases after #111 corpus expansion); §6 SPANTAG integration matrix Date-range row updated to "DESCOPED from v1 pilot. Parked in-tree, reactivates conditional on #111 corpus expansion adding ≥2 stubborn date-range cases." Revised v1 = 3-field confirm pass (`personal.birthOrder`, `siblings.birthOrder`, `parents.relation`); revised implementation cost ~2.25 days (was 2.75); revised pilot acceptance math PRIMARY_GREEN = ≥3 of 5 primary targets improved ≥0.3 AND 0 regressed (drops from 6 targets; `mt_case_006`/case_079 re-classed CONTROL for v1, primary-target again only if v1.1 reactivates). Chris's rationale (verbatim): *"One corpus case with zero failing is not enough to justify keeping it in the first confirm pilot. Keep the bank draft parked, but do not let it expand scope right now."* (2) **WO-EX-SILENT-OUTPUT-01 flagged as highest-leverage next investigation.** Audit finding `silent_output_risk` 26 cases / 12% pass rate / 23 of 45 failures carry the tag is the biggest single lever available. Chris placed the triangulation on his own action list (Chris + ChatGPT next) — agent action: **DO NOT** author the WO pre-emptively; wait for triangulation output. Sequencing note: if SILENT-OUTPUT lane opens a separate track, it does not displace SECTION-EFFECT Phase 3 → SPANTAG → LORI, but may run in parallel as a cleanup lane. No extract.py changes, no stack touch, no eval runs. Active baseline: still r5e1 (59/104, v3=38/62, v2=32/62, mnw=2).
- 2026-04-22 (late morning): **Git hygiene gate codified.** New top-level section added after "Stack ownership" (before "Standard eval command"). Rule: tree must be clean before any code-changing work starts; if dirty, agent flags it and produces a copy-paste commit plan before touching code. Trigger: today's 13-item uncommitted pile-up (overnight WO batch + r5e1 report fills + Phase 1 silent-output instrumentation + corpus tagging audit + failure-clusters report all tangled into one `git status`). Sandbox can't run git, so uncommitted state compounds silently across sessions — a dirty tree destroys the bisect surface every eval-gated patch depends on. Rule also specifies: group commits with code isolated from docs, specific-path `git add` (never `-A`), exception for `.runtime/` throwaways.
- 2026-04-22 (midday): **WO-EX-SILENT-OUTPUT-01 Phase 1 instrumentation landed.** Five-counter cause-cascade wired into `extract.py` around the primary extraction loop (L6609–6927): `silent_llm_return`, `silent_parse_count`, `silent_post_validate_count`, `silent_fallback_count`, plus a terminal `[extract][silent-root]` log line classifying silent events into one of `llm_empty` / `parse_drop` / `validator_drop` / `fallback_miss` / `rules_filter_drop`. Raw-output dump (`raw_output[:300]`) gated behind default-off `HORNELORE_SILENT_DEBUG=1` flag. `probe_01` (`scripts/run_silent_output_probe.py`, 5-case diagnostic pack: case_005/045/065/077/081) + console confirmed instrumentation plumbed cleanly end-to-end. Characterization: `parse_drop` dominant (4 of 5 silent events) — LLM was returning text, parser was rejecting it. Review note: the `fallback_miss` branch in the cause cascade is logically subsumed by branches 1–3 (residual always falls to `rules_filter_drop`); not a correctness bug, but naming is aspirational — worth a TODO to tighten if the classifier ever sees cases that should distinguish "validator dropped everything AND fallback also produced nothing" from "rules filter subsequently dropped the surviving items." Zero extractor-behavior change from Phase 1 (pure instrumentation). **WO-EX-SILENT-OUTPUT-01 Phase 2 salvage patch landed** — one-line tolerance fix to `_parse_llm_json` at `extract.py` L3481–3485: replaces strict `if not text.startswith("["): return []` with a preamble-tolerant cascade (`text = raw.lstrip(); bracket_idx = text.find("["); if bracket_idx == -1: return []; text = text[bracket_idx:]`). Handles the common failure mode where the LLM wraps its truncated JSON array in a preamble like `"Here is the extracted JSON array:\n\`\`\`\n[\n  {..."`. Byte-identical behavior when `raw` starts with `[` or contains no `[` at all; the brace-balanced salvage walker (`_salvage_truncated_array`) does the rest, recovering any complete `{...}` objects from the prefix regardless of wrapping. `probe_02` (same 5-case pack) confirmed the patch works: **0/5 → 2/5 pass, avg score 0.140 → 0.620**; 4 of 5 silent events flipped `parse_drop` → successful parse (the 5th was already producing items via fallback). New `validator_drop` signal revealed on case_077 `great_grandparents.*` / case_081 `grandparents.ancestry` — maps perfectly to pending task **#119 turnscope `allowed_branches` fix for greatGrandparents subtree**, which now has fresh evidence. Next tag = **r5f** (master with Phase 1 + Phase 2 composite, running at time of this writeup). Prediction (Chris, three-agent convergence): 61–63/104, v2 33–35, mnw 0–2 (no worse than r5e1 floor), with silent-output class dropping sharply and being replaced by validator/turnscope failures on #119's target class. Methodology flag: r5f is a 2-commit stack (Phase 1 instrumentation-only + Phase 2 one-line parser tolerance) landed on top of r5e1 baseline; any flips attributable to Phase 1 are impossible by construction (zero extractor-behavior change), so any flip is traceable to the parser-tolerance patch. Salvage patch is byte-stable on the "clean JSON" path, which means r5e1's 59 passing cases should remain at least 59; flips downward would indicate an unexpected interaction worth rollback — flips upward are the expected outcome and should be audited per the standard post-eval block before adoption. WO-INTAKE-IDENTITY-01 (v3 spec, Chris's lane) queued behind r5f decision; execution-order note captured in the spec that grep-for-partial-DOB-consumers must precede endpoint implementation and a NEW `normalize_dob_intake` function must be isolated from the existing `_normalize_date_value` (Patch H / Fix C reference it).
- 2026-04-22 (afternoon): **r5f ADOPTED, new active baseline locked (69/104, v3=41/62, v2=35/62, mnw=2).** WO-EX-SILENT-OUTPUT-01 Phase 1+2 composite delivered a +10 newly-passed / 0 newly-failed result, blowing through the three-agent prediction of 61–63 by +6. Newly-passed cases (all `r5e1_cats ∈ {schema_gap, field_path_mismatch, llm_hallucination}` → `r5f_cats=[]`): `case_005, 023, 025, 064, 072, 077, 082, 085, 086, 103`. Score-up (still same bucket): `case_045, 046, 065, 066, 080, 084` (0.00 → 0.33–0.60). Score-down: `case_091` 1.00→0.80 (still v3 pass; v2 flipped via new `noise_leakage` — patch-attributable, salvage now preserves LLM items previously silent-dropped alongside good ones), `case_068` 0.53→0.00 (LLM stochasticity, not patch-attributable; r5e1 already parsed cleanly on this case so preamble-tolerance path never fired). **Scorer-drift audit on all 10 flips: ZERO drift** — truth-zone totals identical between r5e1 and r5f on every flip; only `hit` counts rose and `miss` counts fell; must_not_write stayed 2 (same offenders case_035 + case_093, pre-existing r5e1 carryover). Method distribution: 97 llm / 6 fallback / 1 rules_fallback. parse_success_rate=93.3%, parse_failure_rate=0.0%, truncation_rate=0.0%. Stubborn-pack primary sub-pack: 4/4 passed (100%, `case_008, 009, 018, 082`). Invalid-fieldpath hallucinations top-10 now concentrated on `case_028` (8 offenders parents/grandparents), `case_033` (5 offenders greatGrandparents), `case_035` (4), `case_044` (4), `case_069` (4) — all in the greatGrandparents/parents/grandparents schema_gap cluster that #119 turnscope `allowed_branches` fix targets. **Active-sequence reorder:** #119 promoted to top of the queue (from #2), now justified by probe_02 signal + r5f master evidence on the same class. SECTION-EFFECT Phase 3 (#95) drops to #2, SPANTAG (#90) #3, WO-LORI-CONFIRM-01 #5, WO-INTAKE-IDENTITY-01 #6. The ATTRIBUTION-BOUNDARY flag and the NARRATIVE/PROMPTSHRINK flags all remain in-tree behind their default-off env gates; default live behavior is the r5f composite only.
- 2026-04-22 (late afternoon): **r5g master eval — NULL result, #119 turnscope fix complete-with-caveat.** Run: `master_loop01_r5g.json` = 69/104 v3=41/62 v2=35/62 mnw=2 — byte-identical to r5f topline. Zero forward flips, zero backward flips, two score jitters (`case_046` 0.333→0.667 up, `case_087` 0.50→0.00 down — both LLM-sampling stochasticity, not patch-attributable). api.log confirms the EXPAND is firing (`[extract][turnscope] EXPAND base=grandparents expanded=grandparents,greatGrandparents reason=119_ancestor_subtree`) across the target cluster (case_028, 033, 035, 044, 069, 087). **Diagnosis:** #119 was necessary but not sufficient — the EXPAND lets greatGrandparents.* items through the turnscope filter, but they then die at schema validation because greatGrandparents.* identity fields (firstName, lastName, relation, ancestry) aren't in the schema. The r5h hallucination rollup still shows greatGrandparents.* at 5 offenders across 2 cases (case_033, case_087), confirming this diagnosis. **#119 closed complete-with-caveat.** Real unlock tracked as new WO-SCHEMA-ANCESTOR-EXPAND-01 (#144), spec authored today — two-lane: (1) scorer-only `alt_defensible_paths` annotations for cases where LLM emits ancestor-level paths with semantic alts elsewhere (case_033 greatGrandparents.military* → military.*, case_039 parents.occupation → community.role/organization); (2) schema expansion adding greatGrandparents.* identity fields to reduce raw hallucination count (case_033, 087 targets). Lane 1 zero-extractor-risk; Lane 2 requires schema+router changes. Zero extract.py changes in r5g/r5h; all movement is eval-harness / test-annotation side.
- 2026-04-22 (evening): **r5h ADOPTED — new active baseline (70/104, v3=41/62, v2=35/62, mnw=2).** Delta vs r5f: +1 case (case_081 0.00→1.00), zero regressions. The flip is scorer-side, not extractor-side — added `alt_defensible_paths: ["greatGrandparents.ancestry"]` annotation to `data/qa/question_bank_extraction_cases.json` for case_081 and case_087 on the `grandparents.ancestry` truth-zone entry (trial-running Lane 1 of WO-SCHEMA-ANCESTOR-EXPAND-01 on 2 cases before the full spec lands). case_081 passed because its LLM-emitted ancestry value fuzzy-matched the expected value under the ≥0.5 gate from #94. case_087 score rose 0.00→0.50 but stayed failed because its LLM value 'French' fuzzy-matches <0.5 against expected 'French (Alsace-Lorraine), German (Hanover)' — this escalates to #97 (WO-EX-VALUE-ALT-CREDIT-01, spec authored today) which adds value-axis alt-credit for exactly this value-drift pattern. **Scorer-drift audit on the case_081 flip: ZERO drift** — truth-zone totals identical r5f vs r5h; only the `hit` count rose (alt-path matched) and the `miss` count fell. must_not_write stayed 2 (same two offenders: case_035 faith turn, case_093 spouse-detail follow-up). Hallucination prefix rollup r5h: parents 11, family 10, siblings 7, greatGrandparents 5, education 5, grandparents 4, laterYears 2, residence 1 — the parents/family/siblings cluster is now the dominant hallucination vector, not greatGrandparents (which dropped from r5f). **Active-sequence reorder (recorded above):** #144 promoted to top (the real unlock for the greatGrandparents class), #97 at #2 (value-axis drift, case_087 fresh evidence), #95 SECTION-EFFECT Phase 3 at #3, #90 SPANTAG at #4. Parallel cleanup lanes opened: **#141 WO-EX-FAILURE-PACK-01** (cluster-JSON output for each master eval, spec authored today) and **#142 WO-EX-DISCIPLINE-01** (run-report discipline header with git SHA, flag state, model hash, api.log freshness — spec authored today). Also authored: `docs/reports/FAILING_CASES_r5h_RUNDOWN.md` (successor to the r5e1 rundown; 34 failed cases categorized by narrator × phase × failure_category × score bucket). No extract.py changes; only eval-harness annotations + docs.
- 2026-04-23: **#95 SECTION-EFFECT Phase 3 — PARK disposition.** Authored `scripts/run_section_effect_matrix.py` (6-variant × 4-run × 3-case diagnostic, 72 extractions, rides the live eval harness via `score_case` / `_phase_to_era` / `_phase_to_spine_phase` import; zero extract.py touch). Matrix ran clean at tag `se_r5h_20260423` — 72/72 attempted, 0 errors, 0 retries. **Q1 (era/pass/mode vary, target held): NO** — V2/V3/V4 produced ZERO path-set diffs vs V1 across all three cases (0+0+0 each). The within-cell stochastic floor was so low it didn't even produce cosmetic reshuffling; emission counts, methods, and scores were bit-identical. **Q2 (target stripped, section held): YES, case-dependent** — V6 vs V1 = 1 diff on case_008 (catastrophic: LLM path fails, method flips llm→fallback, 0/4 pass), 0 diffs on case_018 (completely invariant), 2 diffs on case_082 (cosmetic: adds education.gradeLevel, drops residence.period; score still 1.0 via alts). **Q3 (strip all): YES, driven by target-strip not era/pass/mode-strip** — V5 vs V6 deltas isolating the era/pass/mode contribution after target is already gone are near-zero (case_008 identical fallback, case_018 identical invariance, case_082 V5 adds one extra path over V6). Disposition: **PARK** per WO §8 (Q1=No, Q3=Yes-but-target-driven). **Operational consequence:** SPANTAG Pass 2's controlled-prior prompt drops era/pass/mode and keeps current_section + current_target_path only. Phase 2 payload plumbing stays landed for observability. One extra finding: case_008's target-stripped behavior is a parse-or-fallback cliff (LLM produces output the parser rejects and fallback emits zero items), not a shape drift — reframes its Phase 1 `dual_answer_defensible` label as target-conditional. No ESCALATE trigger. Readout: `docs/reports/WO-EX-SECTION-EFFECT-01_CAUSAL.md`. Tasks #95 and #63 closed. Also closed today: **#111 canon-grounded corpus expansion** (18→24 cases, _version=2, 8/8/8 narrator balance, 3 stubborn date-range cases cg_019/020/021 exceeding LORI v1.1 ≥2 gate — reactivates LORI dateRange v1.1 when v1 completes) and **#141 WO-EX-FAILURE-PACK-01** verified landed (`scripts/failure_pack.py` + wire at `scripts/run_question_bank_extraction_eval.py:39,2281–2295`, outputs confirmed for r5i and r5j). No extract.py changes; scripts/ + data/ + docs/ only.
- 2026-04-23 (late): **CAUSAL.md rewrite + WO-EX-BINDING-01 minted + diagnosis upgraded post-triangulation lock.** Three-agent triangulation on the #95 readout surfaced a typological refinement the path-set matrix alone didn't make explicit: Type A (target-anchored abstract narrative; case_008), Type B (overdetermined factual; case_018), Type C (weakly-constrained narrative; case_082). Rewrote `docs/reports/WO-EX-SECTION-EFFECT-01_CAUSAL.md` around this typology. Q1 unchanged (NO, clean); Q2 and Q3 answers upgraded from "case-dependent" / "target-driven" to "YES, stratified by Type A/B/C" (same evidence; sharper label). **Diagnosis upgraded from "Type C gets worse without target" to "Type C is mis-bound under ideal conditions"** — V1 per-item read confirms case_082 emits four `personal.placeOfBirth` items in the baseline configuration (prompt-invariant, not a V6 regression). §4 reasoning reframed around bumper sticker **"Extraction is semantics-driven, but errors are binding-driven"** — real axes of variation live in the question-evidence pair; Type C failures are extractor-internal schema binding, not routing. Era/pass/mode carry no independent signal. **Disposition: PARK** — sustained ITERATE pushback per WO §8 (within-cell variance zero on 16 of 18 slots; §8 ITERATE means "re-run with n=8," not what our matrix needed). Follow-ups locked: (1) promote existing **#90 SPANTAG** to active (no new SPANTAG WO — updates the existing one); Pass 2's controlled-prior prompt keeps section + target_path and DROPS era/pass/mode; (2) mint new **#152 WO-EX-BINDING-01** as a parallel cleanup lane — two-layer spec: Layer 1 routing rules (residence vs personal.placeOfBirth, residence.period vs education.gradeLevel, sibling vs narrative references), Layer 2 cardinality enforcement (scalar fields emit ≤1 value; excess candidates demote via remap-to-list-field or drop); smoke-test target case_082 V1/V6; Option B (standalone rule layer, `HORNELORE_BINDING=1`) primary, Option C (fewshot) fallback, Option A (SPANTAG Pass 2 prompt) rejected for primary BINDING delivery to protect eval attribution; spec at `WO-EX-BINDING-01_Spec.md`; (3) **SPANTAG → BINDING sequenced, not coupled** — SPANTAG first alone (r5f-spantag clean span-separation signal), BINDING-01 layered on top (r5l clean binding delta under `HORNELORE_BINDING=1`); no collapsed-variable eval; (4) no successor "FIELD-DISCIPLINE-01" lane minted — cardinality is folded into BINDING-01 Layer 2; if Layer 2 proves insufficient a future lane will be scoped on its own evidence with a different name (#142 owns "discipline"). Pulled `section_effect_matrix_se_r5h_20260423_case_082_V1_run1.json` and `_V6_run1.json` to ground the per-item read before finalizing BINDING-01. No extract.py changes; docs + new spec + CLAUDE.md only.
- 2026-04-23 (evening, re-lock + architecture spec stood up): Chris pasted a Boris-style BINDING-01 WO plus a canonical **Lorevox Extractor Architecture Spec v1** ("Refining Extraction Behavior Research Frame" framing). Three decisions folded back in-tree: (1) **BINDING-01 Option A/B re-locked — Option A primary.** Binding rules delivered inside the SPANTAG Pass 2 prompt / binding contract via PATCH 1–5 (Type C binding rules, negative examples, childhood_moves tie-break, optional post-bind scalar guard, `[extract][BINDING-01]` log marker). Option B (standalone rule layer, `HORNELORE_BINDING=1`) demoted to conditional follow-up — opens only if SPANTAG lands cleanly but binding still fails. Option C (fewshot-only) rejected. Rationale: keeps causal attribution clean; aligns with #90 being active; avoids a parallel mechanism with its own env-flag guardrail + eval channel. Sequencing preserved via distinct eval tags: **`r5f-spantag` first (SPANTAG only) → `r5g-binding` second (binding layered on SPANTAG)**. Eval tag `r5l` replaced with `r5g-binding`. Scope contracted: cardinality in BINDING-01 is now **only** a minimal scalar guard for `personal.placeOfBirth` (PATCH 4, optional). (2) **WO-EX-FIELD-CARDINALITY-01 minted as a deferred separate lane** (Architecture Spec v1 §7.3) — broader scalar-vs-multi-value discipline across other identity fields; opens only if BINDING-01's minimal guard proves insufficient. Explicitly not FIELD-DISCIPLINE-01 (#142 collision). (3) **`docs/specs/LOREVOX-EXTRACTOR-ARCHITECTURE-v1.md` authored** as a canonical reference for every extractor-lane WO. 12 sections + two appendices (system-map causal-flow diagram + one-page quick reference). Core Law LOCKED: *Extraction is semantics-driven, but errors arise from failures in causal attribution at the binding layer.* 5-layer pipeline: Architectural / Control / Binding (PRIMARY FAILURE SURFACE) / Decision / Evaluation. Type A/B/C question typology LOCKED. Fix → WO mapping through BINDING-01 / SPANTAG / FIELD-CARDINALITY-01 / #144 / #97. CLAUDE.md top-of-file now points to this spec as canonical. CAUSAL.md §4 follow-ups #2–#4 rewritten; third revision-history entry added. WO-EX-BINDING-01_Spec.md rewritten (v2) with the new PATCH 1–5 structure, r5g-binding eval tag, friendly-fire probe (§B) and identity-damage regression guard (§C) acceptance tests. No extract.py changes; no scripts/ changes; no data/ changes. Files changed: `docs/specs/LOREVOX-EXTRACTOR-ARCHITECTURE-v1.md` (new), `WO-EX-BINDING-01_Spec.md` (v2 rewrite), `docs/reports/WO-EX-SECTION-EFFECT-01_CAUSAL.md` (follow-up #2–#4 edit + third revision entry), `CLAUDE.md` (canonical-spec pointer at top + BINDING-01 row rewrite in parallel-cleanup-lanes block + files-live row + this entry).
- 2026-04-23 (late evening, SPANTAG Commit 3 landed default-off): **#90 WO-EX-SPANTAG-01 promoted to active — pipeline wired, narrow scope.** Commit 3 landed in `server/code/api/routers/extract.py`: new `_extract_via_spantag()` function (Pass 1 → Pass 2 → down-project pipeline; falls back to `_extract_via_singlepass` on empty raw, zero Pass 1 tags, or Pass 2 returning both zero writes and zero no_writes) + `_down_project_spantag_writes()` helper (strips SPANTAG-only keys into private `_spantag_*` prefixed keys so the rails / guardrails see legacy `{fieldPath, value, confidence}` cleanly, but the scorer can still read priority / sourceSpan / sourceTagIds / disagreement_reason / alt_path_section_driven). `_extract_via_llm` dispatch now consults `flags.spantag_enabled()` before `flags.twopass_extract_enabled()` before falling through to singlepass. **Narrow-scope re-lock per Chris's directive (the locked rule from #95 Phase 3 Q1=NO):** Pass 2's controlled-prior block now carries `current_section` + `current_target_path` ONLY; `current_era` / `current_pass` / `current_mode` kwargs are explicitly dropped from both `prior_lines` generation (`extract.py` ~L3006–3019) and the system-prompt enumeration (~L3057–3060). Kwargs remain on the `_build_spantag_pass2_prompt` signature for backward-compat but are silently discarded (`_ = current_era; _ = current_pass; _ = current_mode`). **What this commit is NOT:** no BINDING-01 rules, no broad cardinality rules, no new eval logic, no unrelated prompt changes — just pipeline wiring + the era/pass/mode drop. `tests/test_spantag_pass2.py:test_prompt_carries_priors_explicitly` split into `test_prompt_carries_section_and_target_path` (positive assertion) + `test_prompt_drops_era_pass_mode_from_priors_block` (negative assertion using unique sentinels `SENTINEL_ERA/PASS/MODE` to prove no leak into the user prompt, plus `current_era/pass/mode` substring absence to lock the priors-block description). **Verification:** all 56 Pass 2 + 36 Pass 1 unit tests green; extract.py + test_spantag_pass2.py parse clean under `ast.parse`; byte-stability smoke green — `HORNELORE_SPANTAG=0` default preserves singlepass, down-project drops malformed writes (empty fieldPath, missing value) and preserves metadata under private keys, Pass 2 prompt surface excludes the three sentinel era/pass/mode values. **Ready for `r5f-spantag` eval:** Chris cycles the stack with `HORNELORE_SPANTAG=1` set in the server env (`HORNELORE_NARRATIVE=1` stays on; `HORNELORE_ATTRIB_BOUNDARY` stays off), waits the ~4 min cold-boot, then runs the standard eval block with suffix `r5f-spantag`. **Decision criteria per Chris's directive — judge by Type A/B/C, not a general regression gate:** Type A (case_008) must not regress vs r5h; Type B (case_018) must stay stable; Type C (case_082) should become more legible (cleaner separation between detection and binding) even if not fully fixed — Type C's remaining binding failures are BINDING-01 / `r5g-binding`'s lane, not SPANTAG's. Success on Type C is *separation of detection from binding*, not a full fix. **Next:** after `r5f-spantag` banks, BINDING-01 PATCH 1–5 layers on top under eval tag `r5g-binding`; Commits 4–6 of `WO-EX-SPANTAG-01_FULL_WO.md` (eval harness additions, conditional tighten) remain open but are not blockers since the master eval harness already supports the server-side flag + the existing post-eval audit block. Files changed: `server/code/api/routers/extract.py` (3 edits: Pass 2 prior_lines + system-prompt enumeration + new Commit 3 block + dispatch wiring), `tests/test_spantag_pass2.py` (`BuildPass2PromptTests` docstring + test rename/split), `CLAUDE.md` (this entry). Zero schema changes, zero eval-harness changes, zero scripts/ changes.
- 2026-04-20 (evening): **#99 / WO-STT-LIVE-02** landed — STT-agnostic fragile-fact guard + transcript safety layer. Backend: `ExtractFieldsRequest` gains 6 Optional transcript fields (`transcript_source`, `transcript_confidence`, `raw_transcript`, `normalized_transcript`, `fragile_fact_flags`, `confirmation_required`); `ExtractedItem` gains `audio_source` / `needs_confirmation` / `confirmation_reason`; `ExtractFieldsResponse` gains `clarification_required: List[Dict]`. New fragile-field classifier (narrator identity + spouse + indexed parents/siblings/children/grandparents/greatGrandparents prefixes with leaf filter). New `_apply_transcript_safety_layer(items, req)` stamps `audio_source` on every item (Pass 1) and downgrades fragile fields to `writeMode=suggest_only` + emits clarification envelope when `confirmation_required=True` (Pass 2). `[extract] Attempting` and `[extract][summary]` log lines extended with `stt_src` / `stt_conf` / `confirm_req`. Frontend: new `ui/js/transcript-guard.js` IIFE (`window.TranscriptGuard`) with 7-pattern fragile-fact classifier, `populateFromRecognition` (Web Speech → min-over-segments confidence), `markTypedInput`, `reconcileForSend` (30s staleness cap + substring match → "typed" fallback on hand-edit), `buildExtractionPayloadFields` (returns `{}` when nothing staged, preserving byte-stability). `state.js` gains `lastTranscript` object. `app.js` wires `recognition.onresult` and `sendUserMessage()` typed-input staging. `interview.js` merges STT fields into extraction payload and dispatches clarifications (custom handler → shadow-review → console fallback). **Byte-stable with r5a eval** — eval harness doesn't populate transcript fields; pre-WO callers behave identically. Smoke: 68/68 assertions green (38 frontend + 30 backend via AST-slice). Full landing report at `docs/reports/WO-STT-LIVE-02_REPORT.md`. Deferred: live round-trip in browser (Chris's stack cycle), WO-STT-LIVE-03 backend Whisper migration, UI surface for fragile clarifications (three-tier dispatch already in place).
- 2026-04-27 (afternoon, r5f-spantag-on-v3 REJECTED + post-extraction null-safety patch + warmup defensive fix): **SPANTAG default-on REJECTED at v3 attempt — clean run, real signal, real regression.** First clean full-master run with `HORNELORE_SPANTAG=1` actually firing server-side: `master_loop01_r5f-spantag-on-v3` = 31/104 v3=11/62 v2=8/62 mnw=1, vs r5h baseline 70/104 v3=41/62 v2=35/62. Net **-39 cases**. SPANTAG mechanically works (parse_success_rate 90.4%, truncation_rate 0.0%, method_distribution `llm=94 http_500=6 fallback=3 rules_fallback=1`) — this is decisively the binding-layer hallucination cluster: failure_categories `field_path_mismatch=44 schema_gap=39`, top hallucination patterns include `parents.notableLifeEvents.eventDate`, `grandparents.memorableStory.siblings.0.name`, `childhood_moves.places.0.name`, `father.name.firstName` — Pass 2 LLM is inventing field paths the schema doesn't accept. **Type A/B/C all FAIL** — case_008/018/082 all score 0.00, stubborn-pack primary 0/4. Confirmation that what we suspected as "binding-driven errors" per the architecture spec is exactly what plays out at scale once SPANTAG truncation is removed (1024/1536 caps cleared the truncation that was hiding ~50% of binding failures behind silent fallback in earlier r5f-spantag attempts). **Decision: SPANTAG default-on REJECTED; r5h re-locked as baseline; BINDING-01 promoted to top of active queue.** **Three patches landed today bundled into the SPANTAG decision commit:** (1) `server/code/api/routers/extract.py:7006` — value-coerce wrapper around the `ExtractedItem(...)` construction site. Root cause of the 6 HTTP 500s on extract-fields: SPANTAG Pass 2 LLM occasionally returns `value` as bool (`True`), int (`2024`), or list (`['Gretchen', 'Amelia', 'Cole']`) instead of a string, which fails Pydantic `value: str` validation and bubbles up as ASGI 500. Patch defensively coerces (`bool→"yes"/"no"`, `int/float→str(...)`, `list→", ".join(...)`, `dict→json.dumps(...)`, `None→drop`), logs each coercion via new `[extract][value-coerce]` log marker (so BINDING-01 can survey production occurrences), and wraps the construction in try/except so a single bad item drops the item rather than killing the whole response. The principled fix (split list values into N items, surface bool sentinels via the no_writes channel, etc.) belongs in BINDING-01; this is containment. (2) `server/code/api/api.py:50` — defensive HF cache offline mode. Sets `HF_HUB_OFFLINE=1` + `TRANSFORMERS_OFFLINE=1` at module init when MODEL_PATH contains slashes (i.e., is a local directory). Fixes `huggingface_hub.errors.HFValidationError: Repo id must be in the form 'repo_name' or 'namespace/repo_name': '/mnt/c/models/hornelore/Meta-Llama-3.1-8B-Instruct'` raised by `/api/warmup` when `_load_model()` re-call invokes `try_to_load_from_cache(repo_id)` with the local path. `local_files_only=True` on `from_pretrained` is not enough on its own — the cache lookup runs first. Defensive; primarily fixes the warmup endpoint. (3) `.env` — `HORNELORE_SPANTAG=1 → 0`, with explanatory comment. SPANTAG cap env vars (`SPANTAG_PASS1_MAX_NEW=1024`, `SPANTAG_PASS2_MAX_NEW=1536`) stay in `.env` so they're available the moment SPANTAG re-enables under BINDING-01. **Active sequence reorder:** #152 BINDING-01 promoted to TOP of queue (was parallel cleanup lane); SPANTAG demoted from active extractor lane to "evidence pipeline" — SPANTAG default-on remains gated until BINDING-01 lands null-safety + binding hallucination containment. #97 VALUE-ALT-CREDIT and #144 SCHEMA-ANCESTOR-EXPAND drop one slot. **Active baseline unchanged: r5h (70/104, v3=41/62, v2=35/62, mnw=2).** Files changed: `server/code/api/routers/extract.py` (value-coerce patch at L7006), `server/code/api/api.py` (HF offline init at L50), `.env` (SPANTAG=0 + comment). The companion eval-script path-resolution fix (`scripts/archive/run_question_bank_extraction_eval.py:42` and `scripts/archive/run_stubborn_pack_eval.py:78-79`) and launcher SPANTAG diagnostic (`launchers/hornelore_run_gpu_8000.sh`) and eval timeout bump 90→300s (`scripts/archive/run_question_bank_extraction_eval.py:1026`) all landed earlier today as setup for the v3 eval run; they're tracked but not behavior-changing on this commit. WO-DOCS-REORG-01 spec authored same day, parked behind SPANTAG decision (now unblocked).
- 2026-04-23 (night, r5f-spantag test battery INCONCLUSIVE): Four SPANTAG eval attempts, zero usable 104-case Commit 5 baselines. (1) `r5f-spantag` (SHA 27635e0, 17:10 local) ran with `HORNELORE_SPANTAG=0 (default)` per discipline header → 70/104 v3=41/62 v2=35/62 mnw=2, method_distribution `llm=98 fallback=6` with **zero SPANTAG methods** in emitted items. Byte-stable parity with r5h confirms Commit 3's default-off gate passes cleanly; produces no SPANTAG signal. (2) `r5f-spantag-on` (SHA dcf0677, 21:36 local, header still shows flag=0) → 25/104, method_distribution `llm=52 error_ReadTimeout=40 http_500=5 error_ConnectionError=3 fallback=4` — 48/104 HTTP-layer failures from stack collapse mid-run. (3) `r5f-spantag-probe` (single case `--case-ids case_082`, 2026-04-24T03:21Z / 21:21 local) → Connection refused on port 8000; stack was dead, zero extractions ran. (4) `r5f-spantag-on-v2` (raised caps `SPANTAG_PASS1_MAX_NEW=1024`, `SPANTAG_PASS2_MAX_NEW=1536` exported before stack start) aborted mid-run. **Real SPANTAG evidence came from an ad-hoc session at 18:34–19:15 local** (grep of api.log): 12 extractions — 4 Pass 1 parse_fails (all truncated at 1246–1352 chars `{"tags": [` cut off mid-array → singlepass fallback), 2 Pass 2 parse_fails → fallback, 6 clean Pass 2 successes (items 3–8 returned per call; legal-tag-id guard correctly dropped hallucinated IDs t11/t13). **End-to-end fallback rate ≈ 50%, dominated by Pass 1 token truncation.** Intervention is env-override only, not a code change: `SPANTAG_PASS1_MAX_NEW=1024` (default 512, read at `extract.py` L3599 via `os.getenv`), `SPANTAG_PASS2_MAX_NEW=1536` (default 1024, L3634). **Env-variable discipline note:** all four SPANTAG-related env vars (`HORNELORE_SPANTAG`, `SPANTAG_PASS1_MAX_NEW`, `SPANTAG_PASS2_MAX_NEW`, plus `HORNELORE_NARRATIVE`) are read server-side per-request; they must be exported in the shell that runs `start_all.sh` so the uvicorn child inherits them. Setting them only in the eval-script shell doesn't reach the router. Discipline header's flag-line reads from the eval-script env, not the server — decisive server-side check is `grep "\[extract\]\[spantag\] flag ON" .runtime/logs/api.log` after any extraction fires. **State:** r5h (70/104, v3=41/62, v2=35/62, mnw=2) remains the locked 104-case baseline. Commit 5 decision still blocked on one clean full-master run with flag confirmed firing server-side (via `[extract][spantag][summary]` log lines spanning the run window). Probe artifacts (`docs/reports/master_loop01_r5f-spantag-probe.{json,console.txt,failure_pack.json,failure_pack.console.txt}`) archived in this commit pair for negative-evidence reference (shows discipline-header + connection-error path on dead-stack probe). No extract.py changes; docs + report artifacts only.
- 2026-04-27 (late evening, code review during v4 eval — chat_ws import bugs FIXED + WO-EX-EVAL-WRAPPER-01 spec authored + parity audit corrections): While r5f-spantag-on-v4 eval ran in background, did focused code review per Chris's directive ("look for bugs along the way that you can fix or report"). **Three broken late imports fixed in `server/code/api/routers/chat_ws.py`** (L163, L180, L181): all using `from api.prompt_composer` / `from api.memory_echo` (broken — no top-level `api` package since launcher cd's to `server/` and runs `python -m uvicorn code.api.main:app`); changed to `from ..prompt_composer` / `from ..memory_echo` matching the working pattern at L39. These late imports broke at runtime when memory_echo or correction code paths executed — confirmed root cause of memory echo crashes flagged by parity audit. AST parse green; eval-safe (extract path not affected). **Satisfies the import-fix portion of WO-LORI-SESSION-AWARENESS-01 Phase 1**; Peek-at-Memoir consultation + warm composer composer remain. **Authored `WO-EX-EVAL-WRAPPER-01_Spec.md`** (parallel cleanup lane, non-blocking) — single-command extractor eval workflow eliminating today's 6-step manual ritual that wasted multiple cycles. `./scripts/run_extractor_eval.sh <suffix> [--spantag on|off]` with refusal-on-failure gates: .env flip + verify, stack restart with idempotency, HTTP listener wait, LLM warmup probe with extract-roundtrip threshold (not just `/health`), fresh-only log filter for SPANTAG verification (no more stale-log false confidence), eval execution, inline post-eval summary (topline + value-coerce/spantag greps + failure categories + named regressions). Wraps existing eval scripts; adds NO new eval logic. 3–5 hours implementation; build before next BINDING-01 eval cycle. **Two parity audit corrections:** (1) "SSE bare prompt" finding (DUPLICATED #5) was wrong — the bare string in `api.py:250-251`/`386-387` is a fallback `ui_base` parameter passed INTO `compose_system_prompt()` which then runs full safety rules. SSE path DOES use the full composer. False alarm. (2) `LV_ENABLE_SAFETY` flag confirmed truly vestigial (only appears in docs/spec + `.env`/`.env.example`, never in server code). Other `LV_*` flags status: `LV_DEV_MODE` IS read at chat_ws.py:12 (used for system-prompt debug logging — keep), `LV_SHOW_DEBUG_PILLS` / `LV_ENABLE_AFFECT` / `LV_ENABLE_CAMERA` / `LV_ENABLE_TTS` confirmed vestigial (only in `.env.example`). Recommended cleanup: either wire as kill-switches (Option A, useful for dev) or delete from `.env`/`.env.example` (Option B). Sequenced behind WO-LORI-SAFETY-INTEGRATION-01 Phase 7. **No urgent FIXMEs/HACKs/XXXs in active server code** (good code hygiene). Files changed: `server/code/api/routers/chat_ws.py` (3 import fixes), `WO-EX-EVAL-WRAPPER-01_Spec.md` (new), `MASTER_WORK_ORDER_CHECKLIST.md` (added wrapper WO + import-fix landing note), `CLAUDE.md` (this entry).
- 2026-04-29 (full day + overnight, parent-session-readiness lane): **WO-CANONICAL-LIFE-SPINE-01 landed end-to-end (Steps 3a-fix → 8, eight atomic commits banked by Chris during the day).** Migrates frontend + backend off legacy era keys (`early_childhood`, `midlife`, etc.) onto canonical 7-bucket era_ids (`earliest_years`, `early_school_years`, `adolescence`, `coming_of_age`, `building_years`, `later_years`, `today`) with self-healing read/write. Files touched: `ui/js/state.js` (Step 3a-fix `_canonicalEra` + `_fallbackCanonicalEra` helpers, `getCurrentEra` repair-on-read, `setEra` routes through `_canonicalEra`), `ui/js/lv-eras.js` + `server/code/api/lv_eras.py` (canonical `LV_ERAS` registry), `ui/js/life-map.js` + `ui/js/projection-map.js` (Step 3b IIFE deriving from `LV_ERAS`, eraTags migrated), `ui/js/interview.js` (Step 3c `renderRoadmap` label-as-ID fix, `prettyEra` delegation, `ERA_PROMPTS` keys, prompt builders, `_getNextRepeatableQuestion` canonicalization), `ui/js/bio-builder-family-tree.js` + `ui/js/app.js` (Step 3d `ERA_ROLE_RELEVANCE` / `ERA_THEME_KEYWORDS` keys, `TIMELINE_ORDER` + `ERA_AGE_MAP` from `LV_ERAS`, `activeFocusEra` subsystem off "era:Label" antipattern, `_lvInterviewConfirmEra` Step 7 popover), `server/code/api/prompt_composer.py` (Step 4 + 5 + memory_echo Phase 1a), `server/code/api/routers/chronology_accordion.py` (Step 4 `TIMELINE_ORDER` from `LV_ERAS`, today ghost at current_year), `server/code/api/life_spine/engine.py` (Step 4 `CATALOGS` keys canonical), `server/code/api/routers/extract.py` (Step 4 `_normalize_date_value` docstring, `[extract][era-normalize]` log marker, `_BIRTH_CONTEXT_SECTIONS` dual guard), `ui/hornelore1.0.html` (Step 6 + 8 `_lv80MakeSection` helper, new `lv80RenderAdolescence` 7th renderer, `lv80BuildMemoirSections` reordered with Today last, `_memoirBuildTxtContent` structured-state branch with `data-era-id` selectors), `ui/css/lori80.css` (Step 6 + 7 `memoir-section-subtitle`, `memoir-placeholder-section`, `lv-interview-confirm-*` popover styles). **Live-verified on Christopher Horne narrator + 1000-word test sample**: 7 era_ids load, Life Map renders 7 buttons routed through Step 7 confirm popover, Memoir Peek shows 7 sections with warm heading + literary subtitle (Adolescence subtitle correctly suppressed when warmLabel === memoirTitle), backend `[extract][era-normalize]` boundary fires. **BUG-312 protected_identity gate landed** (`ui/js/projection-sync.js` L91–130) — protected fields (`fullName`/`dateOfBirth`/`placeOfBirth`/`preferredName`/`birthOrder`) now require trusted source (`human_edit` / `preload` / `profile_hydrate`) for **ANY** write, not just overwrites. Closes the gap where first-write to blank protected field succeeded with garbage; **fixes BUG-309 DOB regression upstream** (extracted "January 1, 2026" Type C binding error now routes to `suggest_only` candidates instead of overwriting Christopher's preload `1962-12-24`). Marked task #309 completed-via-#312. **BUG-311 reclassified** (`docs/reports/BUG-311_INVESTIGATION_2026-04-29.md`) — initially filed as "Lori-text leakage into narrator-extract candidates", investigation traced root cause to `ui/js/test-harness.js:65-98`: the 1000-word "clean" SAMPLES dict is verbatim narrator-side text — Lori never authored it. The extracted noise spans ("still learning how to" → fullName, "Play" → parents.firstName, "The Organ" → parents.lastName) ARE in the narrator's own answer ("I am still learning how to navigate family dynamics" / "play the organ"). No chunking leak; the bug is the EXTRACTOR's span-binding logic taking arbitrary noun-phrases from narrative prose and routing them to identity-field schema slots. Reclassified as extractor span-binding hallucination, lane = WO-EX-BINDING-01 (#152) Type C binding error per Architecture Spec v1. Even though BUG-312 now routes these to `suggest_only` (not truth), Shadow Review fatigue (62.5% noise on the 1000-word sample) still warrants the BINDING-01 work. **Pre-laptop-migration backup landed** at `/mnt/c/hornelore_data/backups/2026-04-28_2340_before-laptop-migration-canonical-reset/` after fixing `scripts/archive/{backup_lorevox_data,restore_lorevox_data,restart_api,test_stack_health}.sh` `source common.sh` paths to climb `../` (common.sh stayed in `scripts/` when archive subfolder was created). SQLite integrity check OK; WAL checkpoint clean. **WO-SCHEMA-DIVERSITY-RESTORE-01_Spec.md authored** — three-phase plan: Phase 1 = template port (26 files: 17 named diverse narrators + 9 doppleganger validation files + manifest.json from `/sessions/.../mnt/lorevox/ui/templates/`), Phase 1.5 = personal-data enrichment (LANDED), Phase 2 = array-shape normalization adapter (~1 day, accepts spouse/marriage as both `{}` and `[]`, internal runtime always operates on array; touches projection-sync, projection-map, bio-builder-family-tree, hornelore.html memoir, extract.py, prompt_composer), Phase 3 = sensitive identity capture (2-3 days code + 1-2 days policy: identity block with visibility/source state, Lori auto-ask prohibition, splits overloaded `culture` field, plus kinship + animal-role refinement GAP-13/GAP-15). **Companion Lorevox parity audit doc** (`docs/reports/LOREVOX_TEMPLATE_PARITY_2026-04-29.md`) — Hornelore holds 6 templates (Horne family ×3 + Shatner + Dolly + narrator-template); Lorevox holds 23+ (17 named diverse narrators including david-alan-mercer, dolores-huerta, donald-trump, eleanor-mae-price, elena-rivera-quinn, fred-korematsu, grace-hopper, harvey-milk, jane-goodall, katherine-johnson, maya-angelou, mark-twain, mel-blanc, wilma-mankiller, plus 9 doppleganger validation files). Schema permissively accepts both singular dict and array shapes for spouse/marriage but only `donald-trump.json` + `elena-rivera-quinn.json` + `jane-goodall.json` + 3 dopplegangers exercise the array form (5 of 23+); Hornelore inherited zero array-shape templates. **Five different marriage-link styles found** across Lorevox templates (`spouseReference`/`spouseIndex`/`spouseName`/`spouseNumber`/prose-only) — Phase 2 needs a relationship-link normalization adapter. **Six sensitive identity fields NEVER BUILT in either codebase**: genderIdentity, sexualOrientation, religiousAffiliation, spiritualBackground, culturalAffiliations[], raceEthnicity[] (currently jammed into single overloaded `culture` string); plus per-field `visibility/provenance` state ("not_asked" / "declined" / "private" / "known") missing. **Phase 1.5 template enrichment landed** (overnight, "to be confirmed by operator" placeholder discipline per ChatGPT's "don't let vague memories become over-specific facts" rule): `ui/templates/janice-josephine-horne.json` adds childhood pet pig (name + period blank, narrator-confirmed); `ui/templates/christopher-todd-horne.json` adds 5 lifetime pets placeholders (Dog, Cat, Fish, Snake, Frog with empty names) + stepchildren placeholder entry (Melanie's previous-relationship children, names + DOBs blank) + `relation: "biological"` annotation on existing 3 children (Gretchen, Amelia, Cole). **`compose_memory_echo` Phase 1a improvement** (`server/code/api/prompt_composer.py`) — surfaces `speaker_name` in body, renders explicit "(not on record yet)" instead of silent omission, adds `profile_seed` surface (childhood_home / parents_work / heritage / education / military / career / partner / children / life_stage), adds source citation footer ("Based on: {sources}"). Awaits Chris stack restart for live re-test of "what do you know about me?". **WO-LORI-ACTIVE-LISTENING-01_Spec.md authored** — one-question interview discipline + active reflection. Behavior contract: max 90 words/turn, 1 question, no nested questions, no "and also" follow-ups, no menu offers ("would you rather"/"or we could"), direct-answer-first when narrator asks, reflect ONE concrete detail from prior turn. Two-layer defense: Layer 1 = `LORI_INTERVIEW_DISCIPLINE` system-prompt block (primary, default-on); Layer 2 = `_trim_to_one_question` runtime filter with refined regexes (compound + nested + menu-offer detection, default-off env flag, enable after Phase 1 + eval shows prompt-only path insufficient). Eval metrics ride existing `WO-LORI-RESPONSE-HARNESS-01` Test Type A (six new metrics: `question_count`, `nested_question_count`, `word_count`, `direct_answer_first`, `active_reflection_present`, `menu_offer_count`). Companion to `WO-LORI-SESSION-AWARENESS-01` Phase 2; same evaluation harness, different surface. Blocks parent sessions for older narrators (Janice + Kent need single-question discipline before first contact — high cognitive-load risk). **Companion code review** (`docs/reports/CODE_REVIEW_2026-04-29.md`, subagent) — 60% parent-session readiness; 8 risk-rated regressions; flags one CRITICAL edge case in memory_echo Phase 1a list-rendering at `prompt_composer.py` L671-672 (currently dead code — Phase 1b deferred); Pydantic field-assignment fragility note in `extract.py` L7193; docs drift on TXT-export comment at `hornelore1.0.html` L6990; clean assessment of BUG-312 protected-identity fix and era canonicalization triple-layer defense (state.js `_canonicalEra` + lv-eras.js registry + extract.py `_BIRTH_CONTEXT_SECTIONS` guard); blocking factors for full readiness are session-lifecycle bugs (BUG-310 binding still open) and missing dry-run validation. **Active queue update:** Lori-behavior lane sequencing tightened — WO-LORI-SESSION-AWARENESS-01 Phase 1 (memory_echo + import fix LANDED 2026-04-27) → SESSION-AWARENESS-01 Phase 2 (interview discipline composer guard) ↔ WO-LORI-ACTIVE-LISTENING-01 (specs the discipline rules + filter for that phase; land both together) → WO-LORI-SAFETY-INTEGRATION-01 Phase 1 (chat_ws.py safety.py wiring, biggest functional gap). All three are pre-parent-session blockers. WO-SCHEMA-DIVERSITY-RESTORE-01 Phase 1 + Phase 1.5 landed today; Phase 2 (~1 day) + Phase 3 (~3-5 days) parked behind extractor lane (#152 BINDING-01) and parent-session readiness. Files changed today: `ui/js/state.js`, `ui/js/lv-eras.js`, `server/code/api/lv_eras.py`, `ui/js/life-map.js`, `ui/js/projection-map.js`, `ui/js/interview.js`, `ui/js/bio-builder-family-tree.js`, `ui/js/app.js`, `ui/js/projection-sync.js`, `server/code/api/prompt_composer.py`, `server/code/api/routers/chronology_accordion.py`, `server/code/api/life_spine/engine.py`, `server/code/api/routers/extract.py`, `ui/hornelore1.0.html`, `ui/css/lori80.css`, `ui/templates/janice-josephine-horne.json`, `ui/templates/christopher-todd-horne.json`, `scripts/archive/backup_lorevox_data.sh`, `scripts/archive/restore_lorevox_data.sh`, `scripts/archive/restart_api.sh`, `scripts/archive/test_stack_health.sh`, `WO-SCHEMA-DIVERSITY-RESTORE-01_Spec.md` (new), `WO-LORI-ACTIVE-LISTENING-01_Spec.md` (new), `docs/reports/BUG-311_INVESTIGATION_2026-04-29.md` (new), `docs/reports/LOREVOX_TEMPLATE_PARITY_2026-04-29.md` (new), `docs/reports/CODE_REVIEW_2026-04-29.md` (new), `CLAUDE.md` (this entry). Zero extract.py extractor-behavior changes (only docstring + log marker + dual-guard); the canonical-spine work, BUG-312 fix, and template enrichment are the load-bearing landings.
- 2026-04-27 (evening, r5h-postpatch CONFIRMED + Lori-behavior lane opens): **r5h-postpatch eval clean — three patches from e579ed0 byte-stable when SPANTAG=0.** Eval at SHA `5b94a89` (clean tree) ran `master_loop01_r5h-postpatch` = 70/104 v3=41/62 v2=35/62 mnw=2, identical topline to r5h baseline. Method distribution `llm=99 fallback=5`, parse_success_rate 95.2%, truncation_rate 0.0%, rules_fallback 4.8%. Stubborn primary 4/4 (case_008, 009, 018, 082), secondary 1/1, truncation_starved 3/7 (unchanged). Same two mnw offenders (case_035 + case_093 — pre-r5e1 carryover, WO-LORI-CONFIRM-01 targets). Hallucination prefix rollup unchanged: parents/family/siblings cluster dominant (case_028 8 offenders, case_033 5, case_035 4, case_044 4, case_069 4) — BINDING-01's lane. Discipline header working cleanly (WO-EX-DISCIPLINE-01 payoff): git_sha + flag state + warmup probe + scorer/case-bank versions all reported. Warmup roundtrip 10s = "hot" (no cold-start drama). **Active baseline preserved: r5h (70/104, v3=41/62, v2=35/62, mnw=2).** Three patches from e579ed0 are dormant when SPANTAG=0 by design; their value gets proven the moment SPANTAG re-enables under BINDING-01. **BINDING-01 fully unblocked at top of extractor queue.** **Lori-behavior lane opens parallel to extractor lane.** Three artifacts authored today (docs-only, parent-session-driven): (1) **`WO-LORI-SESSION-AWARENESS-01_Spec.md`** — four-phase active-listener WO. Concept: Adaptive Narrator Silence Ladder. Values clause locked at top: *Lori is a companion, not an instrument. She is a listener that knows when not to speak. The system computes no clinical score, builds no diagnostic profile, and produces no longitudinal report.* Banned-vocabulary list enforced as structural constraint (cognitive decline, MCI, dementia, diagnostic, severity, clinical signal, drift score, impairment, CDTD, decline detector). Phases: (1) memory echo fix + Peek-at-Memoir consultation [chat_ws.py import-fix `from api.prompt_composer` → `from ..prompt_composer` + warm grounded composer], (2) interview discipline composer guard [intent-aware tiers: memory_echo 100w/1q, interview_question 55w/1q, attention_cue 25w/0–1q, repair 30w/1q, safety 200w/0q exempt — with two-layer defense: LORI_INTERVIEW_DISCIPLINE system-prompt block + runtime filter with refined regexes (would-you-like-to requires "or" clause; and-clauses require wh-verb pattern; wh-word counter per question segment catches compound)], (3) MediaPipe attention cue [passive_waiting requires all 4 inputs: gaze_forward + low_movement + no_speech_intent + post_tts_silence; tiered cue types Tier 0 silent → Tier 1 affirmation → Tier 2 soft offer; veto rule: engaged/reflective blocks cue under WO-10C 120s mark; face_missing ≠ passive_waiting], (4) Adaptive Silence Ladder [per-narrator × prompt_weight rolling window p75/p90/p95 → tiers; 25s hard floor; cold-start <10 turns falls back to WO-10C 120s/300s/600s; window persists locally only, no surface, no trend]. Acceptance: PASS if Lori adapts without nagging, interrupting, diagnosing, or over-questioning. **Phases 1–2 block parent sessions** (~3 days out). (2) **`WO-LORI-SAFETY-INTEGRATION-01_Spec.md`** — renamed from WO-LORI-SAFETY-01 after audit revealed substantial existing safety infrastructure. **Original WO accidentally proposed rebuilding what already exists; revised WO scopes the actual gaps.** Existing infrastructure (do NOT rebuild): `server/code/api/safety.py` (50+ pattern regexes, 7 categories, 0.70 threshold, false-positive guards, soft-mode helpers, RESOURCE_CARDS); `server/code/api/routers/interview.py:269-307` (interview-path integration); `server/code/api/prompt_composer.py:108-193` (ACUTE SAFETY RULE — 988-first / 911-first templates, hard-forbidden phrases including outdated 273-TALK); `ui/js/safety-ui.js` + `safety.css` (overlay modal, tap-to-call, age-aware <18); DB tables `interview_segment_flags` + `interview_sessions.softened_turn`. Values clauses (paired): *Lori is a companion, not a clinician* + *Lori does not pretend not to hear*. Acuity-tier mapping locked: ACUTE (suicidal_ideation pattern → ACUTE SAFETY RULE response template, 988 first), IDEATION (LLM second-layer indirect ideation → warm-first + 988 + Friendship Line gently), DISTRESSED (distress_call/cognitive_distress → presence-first, no resources yet), REFLECTIVE (LLM past-tense filter → normal warm response, safety event still logged for operator awareness), ABUSE TIERS (sexual/child/physical/domestic/caregiver → existing templates + operator notify). Phases: (0) light pattern audit no-modify; (1) **CRITICAL — wire safety.py into chat_ws.py** (currently zero deterministic safety on chat path, only prompt-side ACUTE rule; biggest functional gap); (2) LLM second-layer classifier additive (catches indirect ideation pattern misses); (3) operator notification surface — Bug Panel real-time banner + between-session digest, never narrator-visible, no scores no severity no trends; (4) prompt-side IDEATION/DISTRESSED warm-first response block additive to existing ACUTE rule (do NOT modify ACUTE rule); (5) discipline filter exemption + WO-10C softened-mode yield + memory-echo filter on segment-flagged turns + family_truth ingestion guard; (6) extend RESOURCE_CARDS with Friendship Line (1-800-971-0016, 60+ specific); (7) wire vestigial LV_ENABLE_SAFETY flag as kill-switch (Option A — useful for dev); (8) red-team pack with zero-false-negative gate on suicidal_ideation across both chat AND interview paths; (9) operator runbook + onboarding consent disclosure as non-negotiable acceptance gate. **Blocks parent sessions alongside SESSION-AWARENESS-01 Phase 1+2.** Lab/gold: chat-path wiring + operator surface promote to Lorevox; resource library generalizes by region. (3) **`README.md` updated** — new "Local-first AI commitment" section between Data Isolation and Quick Start. Codifies STT/LLM/facial/acoustic/TTS all running on narrator's machine as a standing rule (not a default), with future model exploration table mapping today's local stack to future local swaps as hardware advances, plus contribution invitation gated on the must-stay-local guardrail. Cross-references `WO-AFFECT-ANCHOR-01_Spec.md` (parked, blocked by BINDING-01). Reconciled with assignment-based license by separating code contributions from research/model/benchmark contributions. **Active queue update:** extractor lane unchanged (BINDING-01 #1, #144 #2, #97 #3, #95 #4, etc.). Lori-behavior lane parallel: WO-LORI-SAFETY-01 Phase 1 + WO-LORI-SESSION-AWARENESS-01 Phase 1+2 are both pre-parent-session blockers. WO-LORI-CONFIRM-01 (3-field v1 pilot) remains parked behind extractor lane. Files changed: `WO-LORI-SESSION-AWARENESS-01_Spec.md` (new), `WO-LORI-SAFETY-01_Spec.md` (new), `WO-AFFECT-ANCHOR-01_Spec.md` (new — multimodal affect anchoring parked behind BINDING-01 + parent-session readiness), `README.md` (Local-first AI commitment section). Zero extract.py changes; zero scripts/ changes; zero data/ changes. Five new tasks created in tracker (#284–#288 for SESSION-AWARENESS-01 phases). Tasks for SAFETY-01 phases pending creation. ChatGPT's WO-LORI-DISCIPLINE-01 was offered as a separate WO but folded into SESSION-AWARENESS-01 Phase 2 to keep composer enforcement at one point — three legs (echo/cue/question) cannot voicing-fragment. Two pushbacks on ChatGPT's regex set: `would you like to` blanket block softened to menu-form-only; and-clause patterns require wh-verb structure (not bare "and how"). ChatGPT also reintroduced CDTD cognitive decline detector as Phase 7 of his proposed restructure — explicitly rejected per Chris's locked scope ("we are not a decline detector"); the values clause + banned vocabulary in the WO are the structural defenses against future drift back into clinical territory.

- 2026-04-29 (afternoon, BUG-EX-PLACE-LASTNAME-01 + safety-fallback hardening + doc-drift reconciliation): **BUG-EX-PLACE-LASTNAME-01 landed** (SHA `9b9e557`) — deterministic post-LLM regex guard at `extract.py` drops `.lastName` / `.maidenName` / `.middleName` candidates whose value appears in source text only after a place-preposition (`born in X`, `in X`, `from X`, `moved to X`, `outside of X`, etc.) with no explicit name evidence (`last name was X`, `surname X`, `maiden name was X`, `family name X`, optional genitive chain `maiden name of my mother was X`). Helpers (`_PLACE_PREP_PHRASES`, `_NAME_EVIDENCE_PHRASES`, `_PLACE_GUARDED_FIELD_SUFFIXES`, `_looks_like_place_phrase_for_value`, `_has_explicit_last_name_evidence`, `_drop_place_as_lastname`) sit before `_apply_transcript_safety_layer`; integration is one 6-line block at the top of the accepted-item loop in `extract_fields()` before any other processing (so the guard short-circuits before write_mode resolution, Phase G protected-identity, value-coerce, or Pydantic construction). Three required conditions: (a) fieldPath ends in `.lastName` / `.maidenName` / `.middleName`, (b) source text contains value AFTER a place-preposition, (c) no explicit name marker. Six new eval fixtures (`case_105–110`) added to `data/qa/question_bank_extraction_cases.json`: case_105/106 minimal "born in Stanley/Spokane" must-extract birthPlace + must-not-write lastName; case_107/108 compound "Kent Horne born in Stanley" / "Janice Zarr born in Spokane" must keep real surname AND extract birthPlace; case_109 negative locks the explicit-name-evidence escape (`last name was Stanley` → must keep parents.lastName=Stanley, must-not-write parents.birthPlace=Stanley); case_110 spouse-path generalization. Master pack 104 → 110. **Smoke 15/15 PASS** on regex logic before banking (4 positive, 4 keep-the-real-surname, 7 edge/negative including `1962` safe-direction, multi-word place, dict value, empty value). Ships independently of SPANTAG (no env flag) — safe by construction. Eval tag `r5h-place-guard` queued (#323); expected delta on existing 104 cases = 0 (none of them exercise this exact pattern), +6 fixtures behave per guard. Rationale: 2026-04-28 live test on Christopher Horne 1000-word sample produced "in Stanley" → parents.lastName=Stanley and "in Spokane" → parents.lastName=Spokane (Type C binding error per Architecture Spec v1 §7.1). BINDING-01 PATCH 1-5 is prompt-side (SPANTAG Pass 2 binding contract); this is post-LLM regex cleanup, sibling lane that lands without waiting for SPANTAG re-enable. Files changed: `server/code/api/routers/extract.py`, `data/qa/question_bank_extraction_cases.json` (+426 lines, both AST/JSON parse green). **scan_answer() default-safe fallback hardening landed** (#324) — `chat_ws.py` L206-228. Code review 2026-04-29 (subagent) surfaced silent-skip gap: when `scan_answer()` raises (timeout / OOM / encoding / pathological regex input), the existing try/except catches + logs WARNING + sets `_safety_result=None` + the conditional at L213 is then skipped entirely (no segment flag, no softened mode, no UI overlay, no operator notify, no force-to-interview). LLM-side ACUTE SAFETY RULE in `prompt_composer.py:108-193` still fires regardless (so Lori still gets safety guidance), but only the interview/LLM `turn_mode` actually consults the system prompt — `memory_echo` and `correction` composers skip the LLM entirely and would echo distress content back. Fix: new `_safety_scan_failed` flag set in the except handler; new conditional block forces `params["turn_mode"]="interview"` AND emits `[chat_ws][safety][default-safe]` WARNING log marker so ops know the deterministic layer had to fall back. Memory_echo / correction composers cannot be selected on a scan-failure turn anymore; the LLM path is guaranteed to run with the ACUTE SAFETY RULE in the system prompt. ~17 lines added; AST parse green. Companion stress-test pending (#325 — long rambling answer, Unicode-heavy, mixed STT garbage, control characters). **Doc-drift reconciliation** — sandbox-side `git log` reconciliation with the master checklist + CLAUDE.md surfaced ~10 commits the docs missed since 2026-04-25: (1) `8023501 db.py PRAGMA busy_timeout=5000 — fix safety-hook lock contention` (2) `70166b7 narrator-room: add Era tab — sparse chronological timeline` (3) `b9f6294 revert: roll back Era tab — wrong shape for the use case` (4) `f1e0612 WO-INTERVIEW-MODE-01 Phase 1 — focused 3-column interview view` (5) `bc02b32 narrator-room: unified 3-column layout + Memory River removed` (6) `66f41ae eval: r5h-restore master — SPANTAG flag fix verified, 70/104 baseline restored` (7) `8aab2c2 WO-LORI-SAFETY-INTEGRATION-01 Phase 1 — chat-path safety hook` ← Lane 2 #1 had ALREADY shipped before this session opened; master checklist still listed it as "TOP CRITICAL pending implementation" (8) `42accc9 fix(extract): WO-EX-BINDING-01 PATCH 1-4 — SPANTAG binding pre-normalizer + _validate_item re-entry` ← BINDING-01 had ALREADY shipped (9) `3f3deba fix(extract): WO-EX-BINDING-01 micro-patch — extend invented-namespace regex + 9 _FIELD_ALIASES additions from r5g probe` (10) `8564840 eval: r5g-binding-probe-7cases — BINDING-01 first probe (3/7, primary 3/3)` (11) `2ec7de5 eval: r5g-binding-probe v2 + v3 — micro-patch confirmed, v2 was stochasticity` (12) `c547ddb eval: r5g-binding-on-v1 master — BINDING-01 default-on rejected`. **State updates implied by the missed commits:** (a) **WO-EX-BINDING-01 has actually been landed AND eval-rejected at default-on** — same posture as SPANTAG (code in-tree behind a flag, default-off, gated for further iteration). The "TOP OF QUEUE pending implementation" framing in the checklist is stale. Active extractor sequence reorders to: BINDING-01 = "shipped + iterating" (next move = additional binding rules / prompt experiments / second eval cycle), not "next to implement". (b) **SAFETY-INTEGRATION-01 Phase 1 LANDED** — chat-path safety hook live in `chat_ws.py` L196-274. Phase 2 (LLM second-layer classifier) and Phase 3 (operator notification surface) are now the active SAFETY work, not Phase 1. Tasks #289 → completed; #290, #291 remain pending. (c) **WO-INTERVIEW-MODE-01 Phase 1 LANDED** (`f1e0612`) — focused 3-column interview view. Plus narrator-room unified-3-column + Memory River removed (`bc02b32`). The Era tab experiment was tried (`70166b7`) and reverted (`b9f6294`) — wrong shape for the use case. (d) **r5h baseline RESTORED** (`66f41ae`) — SPANTAG flag fix verified; 70/104 holds. (e) **db.py PRAGMA busy_timeout=5000** (`8023501`) — addresses safety-hook lock contention surfaced after SAFETY-INTEGRATION-01 Phase 1 landed; now active. **Tasks updated:** #322 BUG-EX-PLACE-LASTNAME-01 → completed; #310 BUG: place→lastName binding → completed (closed via #322); #311 BUG: Lori-text leakage → completed (reclassified to BINDING-01 lane, investigation-only); #289 SAFETY-INTEGRATION-01 Phase 0+1 → completed (found landed at SHA 8aab2c2); #324 scan_answer default-safe fallback → completed; #323 r5h-place-guard eval, #325 scan_answer stress-test, #326 master checklist refresh remain pending. **Code review companion** (`docs/reports/CODE_REVIEW_2026-04-29.md` already-banked + this session's verification pass): BUG-EX-PLACE-LASTNAME-01 integration verified clean (`req.answer` early-return at extract.py L7303-7305 means `answer` is guaranteed non-empty when the loop hits the new guard at L7463; `or ""` defensive coalesce is unreachable). Era canonicalization fragility note (extract.py L7314-7318 mutates `req.current_era` in place — works in Pydantic 2.x but would silently break if a future config sets `frozen=True`; logged as low-priority for future cleanup, not pre-parent-session). No new bugs filed beyond #324. **Active baseline:** `r5h` 70/104 v3=41/62 v2=35/62 mnw=2 — unchanged from 2026-04-27 lock. SPANTAG=0 default. BINDING-01 in-tree behind PATCH 1-4 (`42accc9`) + micro-patch (`3f3deba`); default-off pending the next iteration cycle. Tree clean; commits ahead of origin = 2 (`9b9e557` BUG-EX guard + the to-come safety-fallback commit). Files changed today (afternoon batch): `server/code/api/routers/extract.py` (BUG-EX guard, +125 lines), `data/qa/question_bank_extraction_cases.json` (6 new cases case_105–110, +301 lines), `server/code/api/routers/chat_ws.py` (default-safe fallback, +17 lines), `CLAUDE.md` (this entry).

- 2026-04-29 (evening, eval-script printer fix + Bug Panel Eval Harness Phase 1 + prompt_composer warmth pass): **Three commits banked in sequence on top of the afternoon batch.** (1) **Eval-script printer crash fix landed** (SHA `4ac71f0`) — `r5h-place-guard` initial run completed the 110-case extraction loop cleanly (75/110 pass, 1.2% mnw, parse_success_rate ~95%) but crashed in `print_summary()` at `_print_breakdown("By era", ...)` because the `current_era` axis carried a `None` key (eval cases without phase mapping) plus mixed legacy keys (`school_years` / `early_adulthood` / `midlife` / `later_life` / `early_childhood`) alongside canonical era_ids — `r.get(key, default)` returned `None` when the key existed with a `None` value (not the default), and the format spec downstream choked. JSON write was downstream of the printer, so the crash lost the entire report. Three patches to `scripts/archive/run_question_bank_extraction_eval.py`: (a) new `_canonical_eval_era()` helper using `api.lv_eras.legacy_key_to_era_id` to canonicalize at insertion in `_breakdown` when key=="current_era"; (b) `_breakdown` itself made None-safe; (c) `_print_breakdown()` defensive None-safe key formatting; (d) JSON write **moved BEFORE print_summary** so future printer crashes never lose reports again, and `print_summary` itself wrapped in try/except. Chris correctly diagnosed this as "harness reporter bug, not extractor failure" before the patch landed. (2) **WO-BUG-PANEL-EVAL-HARNESS-01 Phase 1 landed** (read-only operator cockpit; #332). Backend: new `server/code/api/routers/operator_eval_harness.py` (~20KB, 4 endpoints `/api/operator/eval-harness/{summary,reports,report/{name},log-tail}`, `HORNELORE_OPERATOR_EVAL_HARNESS=0` default-off gate → 404s when off so the surface doesn't advertise itself). Helpers: `_classify_extractor()` PASS at `pass_rate≥0.65` + `mnw≤0.025`, `_summarize_extractor_report()`, `_latest_extractor_report()`, `_list_extractor_reports()`, `_summarize_lori_behavior()`, `_summarize_safety()`, `_summarize_story_surfaces()`, `_detect_recent_crash()` scanning api.log tail for known patterns (TypeError, Traceback, VRAM, KeyError, etc.). Frontend: new `ui/js/bug-panel-eval.js` IIFE (~14KB, 60s polling timer + Bug Panel open + window focus + visibilitychange triggers, 4 card renderers `extractor`/`behavior`/`safety`/`story_surfaces` with status badges, Refresh + Copy run cmd buttons via clipboard API, graceful 404 → quiet "feature disabled" placeholder, `window.lvEvalHarnessRefresh` exposed for manual re-poll). New `ui/css/bug-panel-eval.css` (~7KB, dark Bug Panel chrome with `.eval-card-pass`/`-warn`/`-fail`/`-stale`/`-missing`/`-ready` accents). Wiring: `server/code/api/main.py` adds `operator_eval_harness` to imports + `app.include_router()`; `ui/hornelore1.0.html` mounts `<div id="lv10dBpEvalHarness">` above the safety banner, links the CSS, loads the JS; `.env.example` adds `HORNELORE_OPERATOR_EVAL_HARNESS=0` with documentation block matching the safety-events / db_inspector pattern. **Smoke-tested against real reports** — picked up `r5h-restore` (70/104 PASS, mnw 1.2%, age 0.89d, top failures schema_gap/llm_hallucination/noise_leakage); status taxonomy PASS/WARN/FAIL/MISSING/STALE/READY all render; Phase 1 is read-only, no run buttons. Phase 2 (guarded run buttons) parked behind separate WO. (3) **prompt_composer warmth + legacy-language cleanup landed** (#330). Four narrator-facing string edits to `server/code/api/prompt_composer.py`: (a) `1-800-273-TALK` / `1-800-273-8255` / `273-TALK` no-go list softened from "OUTDATED. Must never appear." to "must never appear in Lorevox responses." with explanatory note that 988 is the current primary U.S. Suicide & Crisis Lifeline and AFSP confirms the older 1-800 number continues to function indefinitely (it just isn't the primary access point Lorevox should surface); (b) memory-echo "What I'm less sure about" reworded for elder narrators — drops system-y "Anything marked '(not on record yet)' — say it and I'll keep it as a working draft" in favor of "Some parts are still blank, and that is completely fine. You can correct or add one thing at a time, whenever you'd like."; (c) NO QUESTION LISTS RULE narrowed from universal to narrator-facing interview mode only (composer dispatches by `turn_mode` upstream, so helper / operator / diagnostic modes can legitimately surface lists when asked); (d) WO-10C cognitive-support framing aligned with SESSION-AWARENESS-01 banned-vocab spec — "cognitive difficulty (dementia or similar)" replaced with "benefits from extra pacing support — simpler, slower conversation, with more silence and less pressure to recall" in both the CSM thread description (line ~351) and the main directive block (line ~1748). The "dementia-safe" code-comment annotation at L1748 left as-is (internal design label, not user-facing output). AST parse green. **Active baseline unchanged: r5h (70/104, v3=41/62, v2=35/62, mnw=2).** SPANTAG=0 default. BINDING-01 in-tree behind PATCH 1-4 (`42accc9`) + micro-patch (`3f3deba`); default-off pending next iteration. Tree clean post all 3 commits. Active queue update: #332 (Bug Panel Eval Harness Phase 1) → completed, #330 (prompt_composer warmth) → completed. Pending parent-session blockers: #318 Reset Identity for Kent/Janice/Christopher (UI clicks), #319 live re-test "what do you know about me?" after Phase 1a memory_echo, #323 r5h-place-guard eval re-run with patched script, #325 scan_answer stress-test. Files changed (evening batch): `scripts/archive/run_question_bank_extraction_eval.py` (3-patch printer fix, banked at 4ac71f0), `server/code/api/routers/operator_eval_harness.py` (new), `server/code/api/main.py` (router include), `ui/js/bug-panel-eval.js` (new), `ui/css/bug-panel-eval.css` (new), `ui/hornelore1.0.html` (mount + link + script), `.env.example` (HORNELORE_OPERATOR_EVAL_HARNESS gate), `server/code/api/prompt_composer.py` (4 narrator-facing string edits), `CLAUDE.md` (this entry). Zero extract.py changes; zero data/ changes; zero scripts/ changes outside the printer fix.
- 2026-04-30 (full day, WO-LORI-STORY-CAPTURE-01 Phase 1A + 1B end-to-end): **Story preservation lane went from spec to live row in production today.** Banked 11 commits across two sequenced WO landings. The big moment: candidate `9bb91643-e2aa-49f6-bd38-b57e3a45c3e0` is the first preserved story_candidate row in the live DB, captured from a Lori session where Janice gave the canonical mastoidectomy text — proving the entire Path 1 pipeline (trigger → preserve → DB) fires correctly behind the `HORNELORE_STORY_CAPTURE=1` flag. **Spec landings (early day):** `WO-LORI-STORY-CAPTURE-01_Spec.md` authored with golfball architecture (CORE/WINDINGS/COVER/PARTNER), 6 LAWs with 4-classification system (RUBBERBAND behavioral / WALL deterministic / STRUCTURAL system flow / INFRASTRUCTURE impossible-to-violate-in-code), Path 1 (preservation, must succeed) decoupled from Path 2 (extraction, best-effort). New `docs/golfball/` lineage folder with README + 2026-04-30 dialogue capturing the architectural origin. **Phase 1A Commits (mid-day):** (1) **Commit 1 d85d517 — schema + db.py stubs + LAW 3 isolation test:** new `0004_story_candidates.sql` migration (id UUID PK, narrator_id, transcript, audio_clip_path, audio_duration_sec, word_count, trigger_reason enum, scene_anchor_count, era_candidates JSON, age_bucket, estimated_year_low/high, confidence, scene_anchors JSON, extraction_status, extracted_fields JSON, review_status, review_notes, reviewed_at, reviewed_by, session_id, conversation_id, turn_id, created_at — 3 indexes on narrator_id + review_status + created_at DESC); 6 db.py accessor stubs raising RuntimeError; new `tests/test_story_preservation_isolation.py` AST-walks story_preservation.py imports transitively, FAILS BUILD if any forbidden prefix (api.routers.extract / api.prompt_composer / api.memory_echo / api.routers.llm_* / api.routers.chat_ws / api.routers.family_truth) appears. Found-and-fixed bug in the AST collector itself during negative verification (was missing `from X import Y` → `X.Y` form). (2) **Commit 2 9cb750f — preservation bodies + age arithmetic + 56 unit tests:** filled the db.py stubs with real sqlite implementations + enum validation; new `server/code/api/services/story_preservation.py` with `preserve_turn` / `update_placement` / `get_unreviewed` / `get_candidate`; new `server/code/api/services/age_arithmetic.py` with DOB+bucket→year arithmetic, multi-era ambiguity preserved as list ("before_school" → ["earliest_years", "early_school_years"]) — bucket→age table covers very_little/before_school/preschool/early_school/in_school/grade_school/older/adolescent/teenager/young_adult/coming_of_age. (3) **Commit 3a 5b6aa7d — story trigger module + env gates:** new `server/code/api/services/story_trigger.py` pure functions, two trigger paths (full_threshold = duration≥30s AND words≥60 AND ≥1 anchor; borderline_scene_anchor = ≥3 anchors). Anchor detection across 3 dimensions: PLACE / RELATIVE_TIME / PERSON_RELATION, each contributes ≤1 to count. Conservative bigram detection — bare "remember", bare "Dad", yes/no answers don't fire. Env-tunable thresholds (`STORY_TRIGGER_MIN_DURATION_SEC=30`, `STORY_TRIGGER_MIN_WORDS=60`, `STORY_TRIGGER_BORDERLINE_ANCHOR_COUNT=3`). `HORNELORE_STORY_CAPTURE=0` master switch added to `.env.example`. (4) **Bare-noun place fix 205d30f:** reviewer caught implementation/docstring divergence — "School was hard" / "Kitchen was tidy" were firing place anchors against stated intent because `_PLACE_NOUNS` was substring-matched. Split into Tier 1 (BARE: hospital/church/factory/plant/mill/fairgrounds — fire on bare mention) and Tier 2 (PREP_REQUIRED: school/kitchen/yard/library/etc. — only fire after place preposition). Word-boundary regex on both tiers (kills the `implant`-matches-`plant` substring bug). +11 lock-in tests. (5) **plant/mill demotion b75a9ba:** reviewer round 2 caught "The plant died" / "They mill around" still overfiring — both nouns are also common nouns/verbs. Demoted plant + mill from Tier 1 to Tier 2. Widened the prep regex's adjective slot to allow optional determiner + 0–2 generic lowercase tokens before the noun (so "at the aluminum plant" still fires while "at home with my plant" does not). +7 lock-in tests. Janice canonical still hits 3 anchors. (6) **Commit 3b ac4c56d — chat_ws wiring + idempotency:** the load-bearing one. Single insertion at `chat_ws.py` L198 between person_id extract and the archive_ensure_session block. Lazy gated imports (`HORNELORE_STORY_CAPTURE=0` keeps preservation modules out of `sys.modules` entirely — runtime LAW 3 INFRASTRUCTURE assertion). Empty/whitespace transcript skipped silently. `trigger_diagnostic()` runs every non-empty turn → `[story-trigger]` log marker carries trigger / words / anchors / place / time / person. `preserve_turn()` only fires when `trigger != None AND person_id present`. Missing-narrator emits `[story-trigger] WARNING` (not silent drop). preserve_turn failure → `[CRITICAL] preserve_turn FAILED ... session continues but story was NOT saved` with `exc_info=True`, NO rethrow — chat turn always continues per LAW 3. **Idempotency:** `turn_id` threads through chat_ws → preserve_turn → db row; new accessor `db.story_candidate_get_by_turn(narrator_id, turn_id)` follows existing house style (`_connect` / `try-finally` / `_row_to_story_candidate`); preserve_turn checks `(narrator_id, turn_id)` at top, returns existing id with `[preserve] dedupe-skip` log marker if found, else fresh insert. turn_id opt-in (no key → no dedupe by design). +4 dedupe tests + 3 accessor tests = 111/111 pass. **Eval landing same day:** `r5h-place-guard-v2` master eval = 75/110 v3=46/68 v2=40/68 mnw=2 (5 of 6 new BUG-EX-PLACE-LASTNAME-01 fixtures case_105–110 pass; case_110 fails on `field_path_mismatch` `family.spouse.*` vs `spouse.*` — separate lane, place-guard ITSELF fires correctly). Underlying 104-case score unchanged at 70/104. **Phase 1A polish (early evening):** 7-patch hardening commit covering the live-runtime bug sweep — (A) SYSTEM_QF directive skip in chat_ws gate (UI sends `[SYSTEM_QF: ...]` as user-role messages; without skip those would write false-positive rows the moment a directive happened to mention 3 anchors), (B) schema-drift sanity check in init_db (PRAGMA table_info compared against `_CRITICAL_SCHEMA` map, logs ERROR on drift, non-fatal — catches hand-edited table mismatches that would otherwise produce cryptic "no such column" errors at INSERT time), (C) transcript soft cap at MAX_TRANSCRIPT_BYTES=50_000 with `" [...truncated]"` sentinel (defensive against runaway STT), (D) dedupe path null-id guard (falls through to fresh insert + WARNING if existing_id is falsy), (E) turn_id whitespace normalization (`(params.get("turn_id") or "").strip() or None`), (F) scene_anchors round-trip already covered by test_preserve_minimal, (G) `datetime.utcnow()` → `datetime.now(timezone.utc)` deprecation cleanup. +3 new tests for C + D = 114/114 pass. Bug sweep readout banked at `docs/golfball/PHASE_1A_BUG_SWEEP_2026-04-30.md` with subagent findings reconciled (pushed back on "duplicate CRITICAL logging" — both layers see different context, keep both — and "no SQL-special character test" — parameterized queries make this tautological). **Phase 1B (later evening):** new `server/code/api/routers/operator_story_review.py` exposes single `GET /api/operator/story-candidates` endpoint (?narrator_id= filter, ?limit= default 50/max 200, returns trimmed shape with `transcript_preview` 200 chars + `transcript_truncated` flag), gated behind `HORNELORE_OPERATOR_STORY_REVIEW=0` default-off (404 when off, mirrors operator_eval_harness posture). New Bug Panel section: `ui/js/bug-panel-story-review.js` IIFE + `ui/css/bug-panel-story-review.css`, mount in `hornelore1.0.html` above existing eval harness section. Per-row display: narrator id (truncated) + trigger badge (full_threshold green / borderline_scene_anchor amber) + anchors=N words=N confidence pill + era_candidates + estimated year range + italic transcript preview. Optional narrator-id filter input + manual Refresh button. Auto-loads on DOMContentLoaded + window focus. Phase 1B is intentionally read-only — promote/refine/discard land in Phase 3. **Real-world friction observed in live test, not yet fixed:** the trigger classifier is too tight for narrator-shaped text — lowercase "spokane" doesn't match the proper-noun regex (requires capital `[A-Z]`); "because of the war" doesn't match the relative-time patterns (only `during/in the war` fires); bare "he" doesn't fire person anchor (only `my dad/mom/etc.`). The Janice-canonical capitalized text fires 3 anchors and lands a row; real narrator turns from STT or quick typing tend to land at 1–2 anchors and miss borderline. Two paths forward sized for the next session: lower `STORY_TRIGGER_BORDERLINE_ANCHOR_COUNT` from 3 to 2 via env (no code change, immediate), OR tighten classifier for case-insensitive proper nouns + add "because of the war" / "after the war" patterns + consider bare "Dad/Mom" with capital-only matching (~15 min code change + tests). **Active tasks updated:** #341 Phase 1A Commit 1, #342 Commit 2, #343 Commit 3a all completed; new completed work for Commit 3b + polish + Phase 1B + bug sweep doc + first-row landing. Pending: classifier tightening lane, Phase 2 (story_capture turn mode in prompt_composer — Lori behavior change to slow down on memorable turns; needs Chris's eyes per spec), Phase 3 (full operator review UI with promote/refine/discard), Phase 4 (5-question questionnaire spine + tone presets), Phase 5 (docs sweep). **BUG-212 still open (#231) but mitigated:** `clear_direct` session style stops the questionnaire-first walk from steamrolling story disclosures — confirmed in live transcripts where Lori actually engaged with the mastoidectomy story instead of plowing past it. The real fix lives in WO-LORI-CONFIRM-01 / SESSION-AWARENESS-01 lanes, but story preservation now patches the *data loss* even when Lori's behavior layer ignores the disclosure. Files changed today (across 8 commits): `server/code/api/services/story_trigger.py` (new), `server/code/api/services/story_preservation.py` (new + polish), `server/code/api/services/age_arithmetic.py` (new + utcnow→now(timezone.utc)), `server/code/api/db.py` (story_candidate_* accessors + Patch B schema-drift guard + new story_candidate_get_by_turn accessor), `server/code/api/routers/chat_ws.py` (Patch A SYSTEM_QF skip + Patch E turn_id strip + Phase 1A Commit 3b gated block), `server/code/api/routers/operator_story_review.py` (new — Phase 1B), `server/code/api/main.py` (operator_story_review router include), `server/code/db/migrations/0004_story_candidates.sql` (new), `tests/test_story_preservation.py` (new + dedupe tests + truncation tests), `tests/test_story_preservation_isolation.py` (new — LAW 3 build gate), `tests/test_age_arithmetic.py` (new), `tests/test_story_trigger.py` (new + bare-noun + plant/mill tests), `ui/js/bug-panel-story-review.js` (new — Phase 1B), `ui/css/bug-panel-story-review.css` (new — Phase 1B), `ui/hornelore1.0.html` (CSS link + Bug Panel mount + script tag for Phase 1B), `.env.example` (HORNELORE_STORY_CAPTURE + STORY_TRIGGER_* + LORI_SESSION_* + HORNELORE_OPERATOR_STORY_REVIEW gates), `WO-LORI-STORY-CAPTURE-01_Spec.md` (new), `docs/golfball/README.md` + `dialogue-2026-04-30.md` (new lineage folder), `docs/golfball/PHASE_1A_BUG_SWEEP_2026-04-30.md` (new sweep doc), `CLAUDE.md` (this entry). r5h-place-guard-v2 4 eval reports also banked. Zero extract.py changes (preservation is its own lane; LAW 3 INFRASTRUCTURE gate green throughout). Active baseline unchanged: r5h-place-guard-v2 (75/110, v3=46/68, v2=40/68, mnw=2). SPANTAG=0 default. BINDING-01 in-tree behind PATCH 1-4 + micro-patch, default-off pending next iteration.
- 2026-05-02 (full day, BUG-EX-PLACE-LASTNAME-FOLLOWUP-01 + BUG-DBLOCK-01 + BUG-LORI-REFLECTION-01 + Patch B REJECTED + clock UI Variant B): Active baseline locked at **r5h-followup-guard-v2 = 77/114, v3=48/72, v2=42/72, mnw=2** (place-as-lastName lane closed GREEN; cases 111/112 rewritten as same-place dual-pob test that passes the must-not-write contract; 113/114 negative fixtures pass clean). **BUG-DBLOCK-01 closed** — chat-turn write rollback hygiene mirrored across `save_safety_event` + 4 `story_candidate_*` writers (43-line patch in `db.py`); 2 days of clean api.log since 2026-04-30 19:13 confirm the FK-cascade-leaks-write-lock root cause was already mitigated by PATCH 1A + 2 + 3 + busy_timeout=5000 from 2026-04-29; this commit closes the defensive symmetry tail. **BUG-LORI-REFLECTION-01 base committed** (`ea13cbf`) — A.1 prompt EXPLICIT REFLECTION DISCIPLINE block (4 hard rules) + A.2 validator base (kinship canon dad/mom/grandma/grandpa/sis/bro → father/mother/etc.; conservative -ing/-ed/-es/-s stemmer; possessive 's strip; question-stem regex boundary widened from `[.!?]\s+` to `[.!?,]\s+` for comma-led inversions). **Lifted golfball comm-control from 2/7 → 4/8 turns passing** (T03 paraphrase miss cleared via kinship+stem; T07 cleared via comma-boundary fix; T04 partially cleared on diagnostic-language). DB locks stayed clean across both runs (25 → 25, delta 0). **WO-INTERVIEW-CLOCK-01 Phase 1 committed** (`2e10aba`) — narrator orientation clock Variant B (slate, 36px time, lower-left of `#lvNarratorConversation` adjacent to mic). New `ui/js/lori-clock.js` IIFE (~200 lines), ~150 lines of `.lv-clock-*` CSS in `lori80.css` (B + C + A all built; B is locked default; C and A operator-toggleable via `localStorage["lvClockVariant"]`), script tag in `hornelore1.0.html`, `LOREVOX_NARRATOR_LOCATION=` in `.env.example` (server-side wiring pending Phase 2; v1 reads `window.LV_NARRATOR_LOCATION` OR `localStorage["lvNarratorLocation"]`, place line collapses cleanly when empty). All 7 WO design rules honored (narrator-visible only, no timezone abbrev, no softened-mode visual change, no narrator-switch reset, once-per-minute setInterval re-deriving `new Date()`, plain DOM, pointer-events:none). Stack restart NOT required for v1 — browser hard-reload picks it up. **BUG-LORI-REFLECTION-01 Patch B REJECTED** (uncommitted, fully reverted via `git checkout -- prompt_composer.py lori_reflection.py`). Patch B added Rule 5 (NEVER SKIP THE REFLECTION) + extended Rule 4 (FORBIDDEN speculation phrases: "I imagine", "I bet", "you must have", "well-known for") to the prompt + validator changes (trivial-response symmetry on missing_memory_echo + broadened pseudo-empathy regex catching `(it|that)\s+sounds/seems\s+like` regardless of suffix). **golfball-postreflect-v2 regressed 4/8 → 1/8** with three turns flipping PASS → FAIL (T01 identity, T03 lowercase-paraphrase, T07 post-safety recovery). Root cause per per-turn diagnostic pull: prompt's "≤25 words for reflection" rule alongside "≤55 words per turn" cap + Rule 5's "never skip" framing produced LLM behavior of "I'll always reflect, and I'll fill the budget" — exactly the failure mode we wanted to suppress. Patch B fully reverted; tree returned to ea13cbf base. **LOCKED PRINCIPLE (do not re-litigate):** *Prompt-heavy reflection rules make Lori worse, not better. The next reflection iteration MUST be runtime shaping, NOT more prompt paragraphs.* Captured as the design constraint preamble in `BUG-LORI-REFLECTION-02_Spec.md` (next iteration: extract one concrete anchor from narrator text → force first sentence to include it → softened mode gets its own cap/shape via runtime `shape_reflection()` post-LLM transformation; no new prompt rules). **Five sub-WO specs scoped today, all parked:** `WO-EX-UTTERANCE-FRAME-01_Spec.md` (lightweight clause-level decomposition between transcript and extractor/Lori/validator consumption — the shared representation layer that BINDING-01 + LANGUAGE-CANON-01 + REFLECTION both consume; case_111/112 motivating example), `WO-LORI-LANGUAGE-CANON-01_Spec.md` (kinship + interview discipline + life-story topic canon, three layers, with the Tjernberg + Smithsonian + UCLA + NEHGS + Freedom Center external grounding folded in + 2026-05-02 Patch B postmortem lessons-learned), `WO-LORI-SAFETY-PASSIVE-DEATH-WISH-01_Spec.md` (sub-WO of SAFETY-INTEGRATION-01 Phase 2 — distinguishes life-weariness / passive death wish / existential distress from acute suicidal ideation per Tjernberg 2020 BMC Nursing; revises golfball T05 expectation away from acute 988 toward softened-presence + HITL operator flag), `BUG-LORI-REFLECTION-02_Spec.md` (this WO — runtime shaping successor with the locked design constraint at the top so future agents don't re-litigate). **Files committed today** (3 commits ahead of origin/main): `392288e` BUG-DBLOCK-01 (db.py rollback hygiene), `ea13cbf` BUG-LORI-REFLECTION-01 base (prompt_composer.py + lori_reflection.py), `2e10aba` WO-INTERVIEW-CLOCK-01 Phase 1 (4 UI/env files). Pending docs commit batches the 4 sub-WO specs + clock_mockups_v1.html + 2 golfball reports as backward-looking iteration evidence. **T05 safety nuance** (passive death wish from "I am so old and I ache so much that I do not want to be alive anymore") captured in the new sub-WO; harness expectation revision lands when SAFETY-INTEGRATION-01 Phase 2 (#290 LLM second-layer classifier) lands. **Active queue update:** #322 BUG-EX-PLACE-LASTNAME-01 + FOLLOWUP-01 done; #344 BUG-DBLOCK-01 closed; new BUG-LORI-REFLECTION-02 + WO-EX-UTTERANCE-FRAME-01 + WO-LORI-LANGUAGE-CANON-01 + WO-LORI-SAFETY-PASSIVE-DEATH-WISH-01 parked. v1's 4/8 reflection floor preserved as the active golfball baseline; clock UI live + parent-presentable; Patch C / runtime-shaping is the next reflection iteration's lane (do not start in same session as next planning).

- 2026-05-02 (continuous-work session, evening — WO-EX-UTTERANCE-FRAME-01 Phase 0-2 LANDED): Per Chris's directive ("move Utterance Frames from parked to active, but with a safe first definition of 'running'") and ChatGPT's structured 10-point plan ("deterministic utterance_frame.py service, first fixture set, unit tests, optional CLI/debug runner, chat_ws logging behind HORNELORE_UTTERANCE_FRAME_LOG=1; do not wire extractor, Lori, validator, safety, or UI consumers yet; no new dependencies; no truth writes; no prompt changes"), promoted WO-EX-UTTERANCE-FRAME-01 from PARKED to ACTIVE Phase 0-2. **Pure-deterministic Narrator Utterance Frame / Story Clause Map builder shipped** at `server/code/api/services/utterance_frame.py` (~615 lines, zero new deps — imports stdlib + `lori_reflection._AFFECT_TOKENS_RX` only). Public surface: `build_frame(narrator_text) → NarratorUtteranceFrame` returns clause-level decomposition with subject_class (self/parent/sibling/spouse/child/grandparent/great_grandparent/community/pet/unknown), event_class (birth/death/move/work/marriage/military/education/faith/illness/loss/leisure/unknown), place, time, object, feeling, negation flag, uncertainty flag, and candidate_fieldPaths hint list. Architectural rules baked in: relational subject patterns checked BEFORE bare self pronoun ("My dad" → parent, not self+possessive); negated clauses suppress all field hints; uncertain clauses still emit hints (extractor decides downgrade); place-prep regex accepts case-insensitive prepositions but capitalized place name ONLY (intentionally rejects re.IGNORECASE on `[A-Z]` to avoid the "Stanley too" / "be alive anymore" false-positive that surfaced in early sanity test); confidence scoring (high/partial/low) gates downstream consumer behavior. **20 hand-authored fixture utterances** at `tests/fixtures/utterance_frame_cases.json` with partial-shape assertion JSON (NOT byte-equality — frame is allowed to fill more slots than the test asserts; it must NOT contradict an assertion). Includes the load-bearing `uf_001` dual-subject motivator ("My dad was born in Stanley, and I was born in Stanley too." → clause_1: parent/birth/Stanley → parents.birthPlace; clause_2: self/birth/Stanley → personal.placeOfBirth) and `uf_002` inverted equivalent for case_111/case_112 lineage; `uf_005` passive-death-wish capture ("I am so old and I ache so much that I do not want to be alive anymore." with negation flag set, no field hints) for SAFETY-PASSIVE-DEATH-WISH-01 future consumption; plus 17 other coverage cases (mastoidectomy childhood story, Shatner career compound, Janice grandmother Norway generation walking, Bismarck sibling birth, war military, marriage with date, pet dog, parent death, education, affect word, low-confidence floor, garbled STT). **20 unit tests + 4 build-gate isolation tests, ALL GREEN.** Tests: PublicApiShapeTest (6) covers public API contract + None-safety + empty-input handling + locked dict keys; DeterminismTest (1) confirms two calls on same input produce identical to_dict() output; FixtureCasesTest (1) loops every fixture and applies the partial-shape assertions; NoInventedFactsTest (1) confirms every place/time/feeling slot is substring-traceable to narrator text (modulo case); SubjectCoverageSmokeTest (8) confirms each public subject_class is reachable. Isolation tests at `tests/test_utterance_frame_isolation.py` mirror the LAW 3 pattern from `test_story_preservation_isolation.py` — AST-walks `utterance_frame.py` imports transitively, FAILS BUILD if any forbidden prefix (api.routers.extract, prompt_composer, memory_echo, llm_api, chat_ws, family_truth, safety, db, story_preservation, story_trigger) appears; allowlist override for `services.lori_reflection` (the deliberately-allowed sibling pure-function module). **Negative-test verified:** deliberately injected `from ..routers import extract` into utterance_frame.py, ran the gate → it failed cleanly with the forbidden-prefix message; reverted the injection → gate passed. Both states required per the docstring rule. Walker hardening included: `(OSError, SyntaxError, UnicodeDecodeError, ValueError)` exception tuple on file-read so binary/non-Python files in the depth-bounded walk don't break the test. **CLI debug runner** at `scripts/utterance_frame_repl.py` exercises four input modes: `--text "..."` inline, `--file path` file, stdin pipe, `--fixtures` runs all 20 cases. Smoke-tested all four; output is JSON-serialized to_dict() of the frame. **chat_ws observability log wired** behind `HORNELORE_UTTERANCE_FRAME_LOG=0` env flag (default-OFF, mirrors story-trigger gate posture). Single insertion at `chat_ws.py` immediately after the existing story-trigger block. Same gate conditions as story-trigger: flag on AND non-empty user_text AND not a `[SYSTEM_*]` directive. Lazy-imports the service (so default-off keeps utterance_frame.py out of `sys.modules` entirely — extra LAW gate). Builds the frame, emits a single compact `[utterance-frame]` log line carrying conv/narrator/conf/clause-count/unbound-flag/per-clause shape (subject/event/place/negation/uncertainty/hints). Frame-build failure logs WARNING but never breaks the turn — pure observability discipline. **`.env.example` entry added** with full usage doc explaining this is a Phase 0-2 instrumentation flag (NOT a feature flag) — frame produces zero behavior change regardless of setting; downstream consumers wire under their own flags in later phases. **Phase 0-2 deliberate scope wall (DEFERRED):** extractor binding consumes frame.candidate_fieldPaths → Phase 3; Lori reflection consumes frame.clauses for grounding → Phase 4; validator consumes frame.negation flags → Phase 5; UI surface for operator inspection → Phase 6; generation-walking ("my mom's mother" → grandparent) and multi-clause subject coreference → WO-LORI-LANGUAGE-CANON-01. Consumer wiring opens after BINDING-01 second iteration evidence so we can compare frame-driven binding deltas against the same baseline that gates BINDING-01 default-on. Spec status flipped from PARKED to ACTIVE Phase 0-2 with full landing summary at top; master checklist refreshed (header date 2026-04-29 → 2026-05-02, baseline updated to r5h-followup-guard-v2 = 77/114, UTTERANCE-FRAME-01 Phase 0-2 added to Lane 1 parallel cleanup section as LANDED). **Active baseline unchanged: r5h-followup-guard-v2 (77/114, v3=48/72, v2=42/72, mnw=2).** Zero extract.py changes. Zero scripts/archive/ changes. Zero data/qa/ changes. Zero behavior change to live narrator sessions when flag is off (default). Files added: `server/code/api/services/utterance_frame.py`, `tests/fixtures/utterance_frame_cases.json`, `tests/test_utterance_frame.py`, `tests/test_utterance_frame_isolation.py`, `scripts/utterance_frame_repl.py`. Files modified: `server/code/api/routers/chat_ws.py` (+58 lines, gated observability hook), `.env.example` (+22 lines, HORNELORE_UTTERANCE_FRAME_LOG=0 with usage doc), `WO-EX-UTTERANCE-FRAME-01_Spec.md` (status PARKED → ACTIVE Phase 0-2 + landing summary), `MASTER_WORK_ORDER_CHECKLIST.md` (header refresh + UTTERANCE-FRAME-01 row added to Lane 1 cleanup), `CLAUDE.md` (this entry). Acceptance: 20/20 unit tests + 4/4 isolation tests green; story_preservation isolation still green (no cross-contamination); CLI runner smoke-tested in all four input modes; AST parse green on all 5 touched code files; .env.example documents the flag posture clearly; chat_ws observability log only fires under flag. **Pre-commit verification before banking: tree clean state requires `git status` from `/mnt/c/Users/chris/hornelore` since sandbox can't run git.**

- 2026-05-02 (continuous-work session, late evening — UTTERANCE-FRAME-01 Phase 0-2 SURVEY GREEN/AMBER + POLISH PATCH LANDED): **Three artifacts banked in sequence: (1) survey harness + spec, (2) live evidence run, (3) polish patch closing the AMBER findings.** (1) **WO-EX-UTTERANCE-FRAME-SURVEY-01 spec + harness landed** (Chris + ChatGPT-authored): new `WO-EX-UTTERANCE-FRAME-SURVEY-01_Spec.md` + new `scripts/run_utterance_frame_survey.py` (~630 lines, pure stdlib). Pairs three signals — golfball traffic-generator JSON + direct `build_frame()` output + live `[utterance-frame]` log lines from `api.log` — and grades the frame layer ONLY (no Lori judgement, no LLM call, no DB write). Status taxonomy: GREEN/GREEN_WITH_POLISH/AMBER_WITH_POLISH/RED. Per-turn assertions pinned for golfball T01-T08 known shapes (T01 self/birth@Montreal+DOB hint, T02 self/illness@Spokane+parent/work, T05 self/illness+negation+empty hints, T07 stated-feeling visibility). Includes 4 targeted probes for known-miss patterns (lowercase Spokane, lowercase Stanley, aluminum plant object, tired/scared feeling). (2) **First live survey ran** — Chris cycled stack with `HORNELORE_UTTERANCE_FRAME_LOG=1`, ran the golfball harness against synthetic narrator, then ran the survey. Result: `AMBER_WITH_POLISH` — strong GREEN core (8/8 live frame logs, db_lock_delta=0, zero builder errors, passive-death-wish negation suppression confirmed working) with 4 polish findings: lowercase_place_miss=3, generic_work_place_plant=4, object_not_visible_in_log_summary=1, feeling_not_visible_in_log_summary=1. The two probes for lowercase Spokane + lowercase Stanley both flagged AMBER on the lowercase-place miss; the aluminum-plant probe surfaced the generic-place issue; the tired/scared probe confirmed feeling was already captured in the direct frame but invisible in the live log shape. **Bumper sticker proof:** "the live log bridge works" — the harness paired golfball turns to api.log frame lines correctly, and the polish findings are exactly what the survey was designed to surface. (3) **Phase 0-2 polish patch landed via `apply_utterance_frame_phase02_polish.py`** (Chris-authored idempotent patch script applied 2026-05-02). Five edits: (a) `utterance_frame.py` lowercase known-place fallback — new `_KNOWN_PLACE_ALIASES` dict (8 entries: spokane/stanley/mandan/bismarck/montreal/oslo/hanover/norway) + new `_extract_known_place_alias(text)` helper using `_PLACE_ALIAS_PREP_RX_TEMPLATE` (`\b(?:in|at|from|to|near|outside\s+of|moved\s+to|came\s+from|born\s+in)\s+{place}\b`, IGNORECASE). Wired into Place extraction AFTER `_PLACE_PREP_RX` (capitalized proper-noun match still wins on capitalized input — preserves uf_001 case_111 byte-stability) but BEFORE bare-noun fallback. Conservative: alias only fires when the lowercase token appears verbatim after a place preposition, no geocoding/state/country inference. (b) `utterance_frame.py` prefer-specific-workplace-object — when the place fallback caught a generic noun (plant/mill/factory/hospital/church/school) AND the object matcher caught the specific narrator phrase ("aluminum plant"/"steel mill"), promote object string into place slot. When place is empty + event is work, copy object into place. Still verbatim narrator text. (c) `chat_ws.py` richer `[utterance-frame]` log shape — extended from `subject/event@place|neg=N|unc=N|hints=...` to `subject/event@place|obj=...|feel=...|neg=N|unc=N|hints=...`. Survey harness can now see object + feeling slots from logs without re-running the builder. (d) `tests/test_utterance_frame.py` fixture-assertion handler extended with `object` + `feeling` exact-match support. (e) `tests/fixtures/utterance_frame_cases.json` 4 new fixtures: uf_021 lowercase dual-subject Stanley (case_111 STT variant, expects place="Stanley" in both clauses), uf_022 lowercase Spokane parent work + aluminum plant object, uf_023 specific-narrator-phrase preference (place="aluminum plant" not "plant"), uf_024 stated feeling visibility (scared from T07 turn). **Behavioral verification (sandbox + direct test):** lowercase `spokane`/`stanley`/`mandan`/`oslo` all produce canonical `Spokane`/`Stanley`/`Mandan`/`Oslo` place slots; "aluminum plant" + "steel mill" now appear in BOTH place and object slots; `scared` surfaces as feeling on T07-style turns; passive-death-wish still neg=1, hints=[] (regression check); case_111 capitalized dual-subject still high-confidence with both `parents.birthPlace` + `personal.placeOfBirth` hints (regression check). **Test gates:** 24/24 green (16 utterance_frame unit + 4 utterance_frame_isolation + 4 story_preservation_isolation — story_preservation isolation re-confirmed: no cross-contamination from the polish patches). **Live survey rerun pending Chris's stack restart** with patched code; expected goal: status=GREEN, lowercase_place_miss=0, object_not_visible_in_log_summary=0, feeling_not_visible_in_log_summary=0, generic_work_place_plant=0. Files added: `WO-EX-UTTERANCE-FRAME-SURVEY-01_Spec.md`, `scripts/run_utterance_frame_survey.py`. Files modified by polish patch: `server/code/api/services/utterance_frame.py` (+~50 lines: lowercase-alias block + place/object preference logic), `server/code/api/routers/chat_ws.py` (log shape +obj/+feel), `tests/test_utterance_frame.py` (object/feeling assertion handlers), `tests/fixtures/utterance_frame_cases.json` (+4 fixtures). **Active baseline unchanged: r5h-followup-guard-v2 (77/114, v3=48/72, v2=42/72, mnw=2).** Zero extract.py changes. Zero behavior change to live sessions when `HORNELORE_UTTERANCE_FRAME_LOG=0` (default). Phase 3+ consumer wiring (extractor binding, Lori grounding, validator, safety routing, UI) still parked — polish moves the survey grade GREEN but does NOT open consumer integration. NOTE for sync hygiene: the 3 docs/reports/ files Chris's first survey produced (`golfball-utterance-frame-log-v1.json`, `utterance_frame_survey_v1.json`, `utterance_frame_survey_v1.console.txt`) live on his local repo with real content; the workspace mount has them as 0-byte upload artifacts and could not be removed from sandbox — Chris's commit should NOT `git add` these from the sandbox-side reports directory if the workspace shows them as 0 bytes; instead `git add` them from his local working tree where they hold the real survey output.
